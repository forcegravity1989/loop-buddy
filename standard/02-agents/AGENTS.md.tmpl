# {{name}} · Agent 协作约定

这个项目按 Builders' Workbench(buddy)的方式运作:一件活对应一张 Issue,
干完最远推到「评审中」,「完成」永远由人显式点。项目全貌见 `PROJECT.md`;
这份文件是给任何进这个仓干活的 agent(Claude Code、Cursor 等)看的操作约
定,不是项目介绍——想了解项目本身,先去看 `PROJECT.md`。

以下几节按「必须先知道的硬约束」在前、「操作细节」在后的顺序排列——agent
工具经常因为上下文预算截断长文件,把硬约束放前面能保证它们被读到。

## 先读什么

开始干活前,依次读完这三份文件,不要凭经验猜:

1. `PROJECT.md` —— 这个项目要做什么、对标谁、三个月要长成什么样。
2. `.bw/metrics.toml` —— 当前的北极星与滞后/引领指标定义,搞清楚手头这
   件活是不是在往某条指标上推。
3. 本周计划 `docs/plan/YYYY-Www.md`(取当前 ISO 周对应的那一份)—— 本周
   目标、本周排的活清单、上周完成情况。找不到本周对应的文件时如实说明,
   不要假装本周没有计划、也不要自己编一份。

## 活怎么做

- 一件活 = 一次 agent 会话 = 一条分支 = 一个合并请求(MR/PR)。不要在一
  条分支里混着做几件不相关的活。
- 干完把状态推到「评审中」为止,到这里就是这次会话的终点——继续往前推
  进到「完成」不是你的事,那一步永远由人显式点,任何情况下都不要自己把
  活标记为完成。
- 提交前对照这件活的类别(原型 / 构建 / 优化 / 运营推广 / 运维)自查有没
  有明显漏做的部分;拿不准算不算做完,就如实写进 MR 说明交给评审者判断,
  不要自己拍板说"已经做完了"。

## 指标怎么碰

- 指标定义只有一份正本:`.bw/metrics.toml`。要改定义(加一条指标、改目
  标值、改采集方式),改这份文件,像改代码一样走评审,不要在别处另开一
  份或在界面里绕过它。
- 指标的实际读数只能来自真实观测——真实跑出来的数字、真实连接器取回的
  数字、或者人手填并标注"手填"的数字。不要为了让灯变绿而手改历史读数,
  也不要编一个"看起来合理"的数字凑数。
- 没有数据就是没有数据,对应的健康灯应当显示未知(灰色),不是绿色。

## 禁止事项

- 不要自己把一件活标记为「完成」——这一步永远等人来点。
- 不要删除或改写 `docs/plan/` 目录下已有周计划文件的历史内容;本周对应
  的文件可以正常编辑,过去的周计划只能保留,不倒着改旧周的记录。
- 不要绕开评审直接把改动合进主分支,即使你确信这个改动是对的。
- 不要删除或绕过 `.bw/` 目录下的配置文件去手工拼凑一个结果——这些文件
  是正本,少了它们后面的人没法知道真实约定是什么。

## 代码图怎么用

如果这个项目已经建过代码图索引(`codegraph` 系列命令可用),优先用它定
位改动范围,而不是从头通读全部源码:`codegraph explore` 看整体结构、
`codegraph node <名字>` 查一个符号、`codegraph callers <名字>` 找调用方、
`codegraph impact <文件>` 估计一次改动的影响面。没有代码图索引时,退回正
常的搜索与阅读,不必现建一份。

## 提交与评审

- commit message 要让没打开任何文档的人也看得懂这次改了什么、为什么改
  ——可以带任务代号,但代号之外必须有一句人话描述。
- MR/PR 说明里写清楚:改了什么、有没有跳过某个环节以及为什么、评审者需
  要重点核对哪里。
- 涉及 `.bw/metrics.toml`、`docs/plan/`、`docs/releases.md` 这类正本文件
  的改动,在说明里单独提一句,方便评审者优先核对。

<!-- 下面这一段是项目自己的补充约定。buddy 铺底第一次写入时这里是占位
     文字;之后不管是铺底重跑,还是规范版本升级,buddy 都只替换上面几个
     带标记的固定章节,不会覆盖或清空这一段——项目团队可以放心把自己的
     规矩写在这里。 -->
## 这个项目的约定(项目自定义,buddy 不改)

(待填——这里写这个项目特有的规矩,比如某个目录的特殊约定、某类活额外要
过的检查项、代码风格上高于通用约定的补充要求。)
