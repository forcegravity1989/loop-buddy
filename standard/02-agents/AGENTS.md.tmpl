# {{name}} · 给 agent 的开发约定

<!-- 这份文件是**这个项目自己的**开发与维护手册,给任何进这个仓干活的 agent
     (Claude Code、Cursor、Codex……)看。它放在仓根,是因为这些工具都在仓根
     找它 —— 不是 buddy 的资产,buddy 只负责把初稿搭出来,之后归项目。

     buddy 只维护下面「本项目由 buddy 管理」那一段(带 bw:managed 标记)。
     其余每一节都是项目自己的,buddy 铺底重跑、规范升级都不会覆盖。 -->

## 30 秒:这个项目是什么

{{brief}}

完整的项目名片(对标谁、北极星、三个月长成什么样)见 `.bw/PROJECT.md`。

<!-- bw:managed start —— 这一段由 buddy 维护,规范升级时会被替换 -->
## 本项目由 buddy 管理

这个仓由 Builders' Workbench(buddy)管着项目节奏。你需要知道的只有三件:

1. **`.bw/` 下的文件是正本**,不是缓存:`.bw/PROJECT.md`(项目名片)、
   `.bw/metrics.toml`(指标定义)、`.bw/plan/YYYY-Www.md`(周计划)、
   `.bw/releases.md`(发版记录)。改它们像改代码一样走评审,不要在别处另开一份。
2. **动到哪份就先读哪份**,别凭记忆猜格式;读不到就停下说明,不要自己编一份。
3. 过去的周计划只保留、不倒着改;本周那份可以正常编辑。

buddy 派你来干活时,会话开头还会给你这件活的具体规矩,以那份为准。
<!-- bw:managed end -->

## 怎么建、怎么跑、怎么测

{{build_commands}}

## 目录导览

{{layout}}

## 提交与评审

- commit message 要让没打开任何文档的人也看得懂这次改了什么、为什么改 ——
  可以带任务代号,但代号之外必须有一句人话描述。
- MR/PR 说明里写清楚三件:改了什么、有没有跳过某个环节以及为什么、评审者
  需要重点核对哪里。
- 拿不准算不算做完,就如实写进 MR 说明交给评审者判断,不要自己拍板说
  「已经做完了」。

## 代码图怎么用

这个项目建过代码图索引(`codegraph` 系列命令可用)的话,优先用它定位改动范围,
不要从头通读全部源码:`codegraph explore` 看整体结构、`codegraph node <名字>`
查一个符号、`codegraph callers <名字>` 找调用方、`codegraph impact <文件>` 估计
一次改动的影响面。没有索引时退回正常的搜索与阅读,不必现建一份。

## 这个项目特有的规矩

(还没填。写这个项目和别处不一样的地方:某个目录的特殊约定、某类改动额外要过
的检查、高于通用约定的风格要求。)
