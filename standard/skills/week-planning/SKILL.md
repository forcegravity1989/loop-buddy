---
name: week-planning
description: 复盘上周、补齐 .bw/metrics.toml、和人交流出本周目标与业务活草稿。适用:运作活①「更新指标 + 制定本周计划」被开工时,别处不用
category: 运作
---

# 更新指标 + 制定本周计划(week-planning)

一周的开头。人只点一下「开始本周」,剩下的是一次对话:先看上周实际发生了什么,再把
绑不上数据的指标接上最便宜的采集路径,最后和人一起敲定这周要干哪几张活。

**人不写周目标——你引导他说出来,然后照着写。** 这条是整篇的重心,搞反了这次会话就
白跑了。

## 何时用

只在运作活①被开工时用。buddy 建这张活的判据是**按标题查**——同名的活还没有(或已经
完成)才建得出来,所以**不要假设本周的 `.bw/plan/YYYY-Www.md` 一定还不存在**:人可以
在计划屏、会话屏手动再建一张,那时候文件可能已经在了。

- 本周文件**不存在** → 正常写一份新的。
- 本周文件**已经存在** → 先读它,原地改写、只补该补的,**不推倒重来**;在会话里说
  清楚你改了哪几段。

`.bw/metrics.toml` 不存在或是旧格式(项目刚接入、或指标还停在 V3 那套采集方案)则第
二步走**完整起草/重定**而不是补齐。

## 第一步 · 复盘上周

读上一周的 `.bw/plan/YYYY-Www.md` 与 `.bw/releases.md`。

- 上周计划的活,哪些真做完了、哪些没动、哪些中途换了方向。
- 上周有没有发版,发的是什么。
- **没有上一周文件就如实说「首次制定周计划,无历史可复盘」**,不要用空话填满这一段,
  更不要根据 git log 编一段"上周大概做了这些"冒充上周计划。

这一步**只读不改**。

## 第二步 · 更新指标

调用子技能 `metrics-refresh`(和本篇同级:`../metrics-refresh/SKILL.md`):补齐缺的引领/滞
后指标、给每条指标判一次「这条指标的历史现在还能不能重新算出来」、能自动采的写成脚本、
跑一次采集。

三条硬性约束,和整个产品的健康推导链是同一条:

- **绝不伪造读数**。采不到就是采不到,指标保持灰。
- **绝不为了点亮而改指标定义**。把"周活跃用户"改成"注册用户总数"因为后者好采——这
  是把管理体系变成自欺,比灰着更糟。
- **每撞到一条手填指标,都要认真问一次「这个数能不能写成脚本」**,并给出当次结论。
  你的价值是帮人把手填升级成能自动重算的,不是每周替人抄一次数。

改动**先不提交**,和第四步合成一次提交。

## 第三步 · 引导出本周(不许自己下结论)

结合指标现状、代码现状(codegraph 索引)、上周记录,以及(若这个项目配了项目群)一份
上周群消息摘要,和人交流出:

- **周目标一句话**。人说的话你来记,不是你写一句让人点头。
- **业务活草稿若干张**,每张:标题 / 说明 / 类别 / 预期推动哪个指标 / 建议的开工工具与
  workflow。

**写完草稿必须停下来等人明确的确认信号**,才能进第四步。人没说"确认"之前不许落文件、
不许建活。

## 第四步(需人确认后才能做) · 落文件

写这几份:

1. `.bw/metrics.toml`(第二步的改动)
2. `.bw/plan/YYYY-Www.md`(本周计划)
3. `docs/metrics.md`(指标说明:每条指标的判定与理由,没数的要有诚实说明)
4. 第二步新写或改过的采集脚本(`.bw/scripts/` 下)

`.bw/plan/YYYY-Www.md` 除周目标、业务活清单外,必须有一段 **「本周指标读数」**:每条
已经能采到的引领/滞后指标各一行——数字 · 来源 · 采集时间。**这一段是这一周的快照,
不是读数的正本**,写的时候注明是快照。保留它的理由是一个真实需求:别人 clone 下这个
仓就能看到数,不用装 buddy、不用跑脚本。**没采到的指标不写行,更不写 0。**

模板里其余几段(本周运作、上周完成情况)照实填,没有内容就留空,不编占位句。

**这些文件产出在这张活自己的工作目录里**,随这次改动提交——不要往人的主检出写一份骨
架。仓是正本,合入才算数。

然后正常提交,`gh pr create` 开 PR,把 PR 地址打屏。**`gh pr merge` 禁止**——合入是人
的动作,和所有 Issue 共用同一条铁律。

## DoD

- 落的每份文件结构合法(TOML 能解析、Markdown 表格列数对得上)。
- 改动是这条活分支上的真实提交,不是只写在工作区里。
- 「本周指标读数」里的每一行都来自这次真采到的数,并注明这一段是快照。
- 仍然没数的指标,每条都有一句诚实说明:为什么采不到、最便宜的下一步是什么。
- 这次会话没有替人合入任何 MR。

## 常见坑

- 把「引导」做成「代写」——人还没说话,周目标已经写好了。
- 跳过复盘直接进第三步:没有上周的账,这周的计划就是拍脑袋。
- 给采不到的指标编一个看起来能跑的采集方案,跑出来的数没人核对得了。
- **上来就给人一串「手填」了事**——手填是结论,不是起手式;每一条都要先问过「能不能
  写成脚本」、先找过有没有带时间戳的接口。
- 人说了"看起来不错"就当成确认——那不是确认信号,再问一句。
