# Builders' Workbench — 领域语言 (Ubiquitous Language)

单人构建者的项目管理工作台。本文件是**唯一的规范词表**:代码、文档、界面、对话都用这里选定的词，别再各叫各的。

> 定义原则:每个词只说它**是什么**(1–2 句)，不写实现;`_Avoid_` 列出被它取代的说法。
> 这是词表,不是规格——设计事实源仍是 [`plan/06-overall-alignment.md`](plan/06-overall-alignment.md);
> 命名冲突的收敛与改名计划见 [`docs/adr/0001-ubiquitous-language.md`](docs/adr/0001-ubiquitous-language.md)。
> 字母数字任务代号(P2、W6、R1……)不属于领域语言,各系列的含义与撞车警示见 [`docs/code-schemes.md`](docs/code-schemes.md)。
> 词表分两部分:前半是**产品语义词**(产品概念本身),文末「工程操作词」一节是**仓库工作方式的行话**(读回、门禁、记账……)。

## Language

### 人与角色

**Builder / 构建者**:
使用工作台的那个人——端到端 own 一个项目的单人系统设计者,产品的主角。全局唯一,专指人类。
_Avoid_: user、owner、开发者;不要用 "Builder" 指构建阶段的角色(那是**构建师**)。

**队友 / Teammate**:
被指派去干活的 AI 人格,有名字、模型、真实战绩与胜率。派活的对象是队友。真实定义形态是一份 **AGENT.md**(frontmatter 声明名字/描述/工具/模型 + 一段指令正文,同 Claude Code 的 subagent 定义格式)。每个项目出生即拥有五角色队友的**自有副本**(存量项目在启动时自动补齐,幂等不重复),战绩从零各立各的账;指派池只见本项目的队友,他项目的队友永不出现。
_Avoid_: Agent(留给运行时执行体)、AgentCard(那是队友在目录里的一行展示)、Squad、workbuddy、multica、机器人;把五角色队友当全局单例(那是 plan/20 之前的旧态)。

**工具集 / AllowedTools**:
一个队友被真实允许调用的工具白名单,和 claude CLI `--allowedTools` 是同一件事。执行时由适配层翻译给底层 CLI,字段本身不绑定某个具体 CLI。
_Avoid_: 把它当装饰性标签——它真实生效,执行时真的会限制队友能用什么。

**Agent CLI**:
真正执行一个队友的智能体框架/CLI(Claude Code / Codex / Cursor……)。今天只有 Claude Code 真能跑;选了别的会诚实报错"本机未安装",绝不假装成功或悄悄退回 Claude Code。
_Avoid_: 和"模型"字段混为一谈(model 是模型标签,Agent CLI 是执行引擎);假装多 CLI 已支持。

**五角色 / Stage Role**:
五个阶段各自的方法论人格:原型师·构建师·优化师·运营推广师·运维师 (Prototyper / Builder-role / Optimizer / Grower / Maintainer)。每个角色带该阶段的打法与开场指令(preamble),是内置方法论,不是某个具体队友。
_Avoid_: 把"五角色"和"聊天发言方 (Author)"混为一谈——后者只有人/AI 两方;构建师的英文别写成裸 "Builder"(与构建者撞)。

**发言方 / Author**:
一条对话消息出自谁——人(Builder)还是 AI(Agent)。纯 UI 归属轴,与"五角色"无关。
_Avoid_: 用 "Role" 单指这个(Role 已被五角色占用;代码里对应类型已改名为 `Author`,见 ADR)。

### 项目生命周期

**阶段 / Stage**:
项目的五个方法论阶段:**原型 → 构建 → 优化 → 运营推广 → 运维**,运维复盘回流到原型(**闭环回流**,所以是环不是流水线)。每阶段自带该问什么、什么节奏、DoD、常见坑。这是通用方法论,不随项目现编。
_Avoid_: 用裸词 "环 / Loop / operating loop" 指五阶段——"环/循环"留给工作流内部的微循环;五阶段的成环性叫"**闭环回流**"这个属性,不当名词用。step/step-N/七步向导(已演进为五阶段)。

**交棒 / Handoff**:
从当前阶段推进到下一阶段的那一次显式动作。只留痕、不拦人——清单没勾完也能交。
_Avoid_: 审批、gate(交棒不是审批门)、阶段切换。

**带险交棒 / Risky handoff**:
DoD 清单未勾完就交棒的情形——永久记下当时缺什么、留了几件活。你有权冒险,但不能无痕。
_Avoid_: 强制完成、跳过。

**DoD / 完成清单**:
一个阶段"做到什么算完"的明确勾选清单。纯人工勾选,永不派生、永不代填。
_Avoid_: Definition of Done 缩写混用时统一写 "DoD";不要叫验收标准(那偏向 Issue 验收)。

**时期 / Maturity Period**:
项目所处的成熟度期:探索期(0→1) · 扩张期(1→N) · 成熟期(Sustain)。用户创建时声明,纯信息性——只影响项目墙的精力配比展示,不驱动任何派生。
_Avoid_: 周期 / Cycle / ProjectCycle(裸词"周期/环"太容易和五阶段闭环、工作流循环撞;代码里对应类型已改名为 `MaturityPeriod`,见 ADR)、生命周期。

**就绪状态 / Readiness**:
项目本身的就绪度:运营中(Running)还是冷启动中(创建流程未完成)。是"状态",不是"阶段"。
_Avoid_: Phase / ProjectPhase(与"阶段 Stage"撞;代码里对应类型已改名为 `Readiness`,见 ADR)。

### 项目与 GitHub(产品信息正本在仓里)

**产品信息 / 过程信息 (Product / Process)**:
一个项目切成两类信息:产品信息(代码、Spec、原型、Issue、指标定义)正本住在 GitHub 仓;过程信息(会话、运行记账、信号、交棒)住在 BW 本地库。BW 是操作台 + 信息转化层,不是又一份正本。
_Avoid_: 把 BW 当项目的唯一数据源;把仓说成"备份"或"同步目标"(它才是正本所在地)。

**仓 / Repo**:
项目背后真实的 GitHub 仓库——新建或接入。Issue、PR、指标定义的正本都挂在它上面;没挂仓的项目(存量项目、纯本地项目)如实留白,绝不假装有仓。
_Avoid_: 和"工作区"混用(工作区只是仓的本地克隆路径,仓才是身份)。

**仓平台 / Repo Provider**:
建仓时选的托管平台。今天真通的有两个:GitHub 与 CodeHub(GitLab v4 兼容,绿/黄/内源三域名,V1 创建流以它为主体)。未接的平台如实标注、不假装可用;暂时连不通的域名(如未登录)仍可点,失败时给人话报错,绝不静默吞掉。它是一个选择器,不是抢先做好的多平台抽象。
_Avoid_: 说「唯一真通的是 GitHub」(CodeHub 已真通);把「未接」的选项假装已支持;把「连不通就灰置不可点」当现行口径(已被穿刺批次 PF1 决议改为可点+人话报错)。

### 创建流(快、能退、能续)

**创建流 / Creation Flow**:
一个项目从"提交意图"到"正式运营"之间的引导过程,V1 起只有两卡:仓 → 意图(早先的快速问题/起草/确认三卡已裁撤,节奏等字段直接用库默认值)。刻意做得快(一气呵成、秒级);中途能退回项目墙,中断后能续上;失败不落死路。
_Avoid_: 按旧五步(仓→意图→快速问题→起草→确认)描述它——那是 V1 之前的形态;把它当成五阶段之一(它是正式运营前的引导,不是运营阶段)。

**起草 / Drafting**(历史词,入口已裁撤):
创建流早期的"体系起草"那一跑——生成北极星草案、指标框架、五阶段激活,永久用演示替身(mock)跑。V1 把创建流收成两卡后,界面已没有起草入口;相关命令只剩历史验证脚本还在调。
_Avoid_: 在新文档里把起草当成创建流的现行步骤;把它和标配 Issue 里的真实起草工作混为一谈。

**标配 / Starter**:
项目创建落地时自动配好的默认内容(一批 Issue、配套的 Skill、一个采集用的定时任务)——不是用户自己发明的起手式,也不是内置方法论,是"开箱即有"的起点。
_Avoid_: 和"五角色的内置方法论"混为一谈(那是永久性的打法,标配是一次性的起手配置)。

**标配 Issue 三件套 / Starter Issue Trio**:
挂仓项目创建落地时自动建好的三张起手 Issue,依赖序:竞品分析 → 找指标 → 绑数据,各自挂一个配套 Skill。是否开工由人在看板上显式点——创建流不再问"立即让队友开工第一件?"(那是旧末卡的交互,已随末卡一起裁撤)。
_Avoid_: 以为创建流会自动把三件套跑完(创建流本身不跑真队友);当成随便一件普通活(它们有固定依赖序和固定 Skill 绑定)。

**三出路 / Three Ways Out**(历史词,随起草一起退场):
起草失败时给的三个真实出口:重试起草 / 先用模板继续 / 退出(已完成部分留档,不丢)。起草入口裁撤后这个词只活在历史验证脚本里;但它承载的精神仍然通用:失败原因翻成人话上屏,技术原文折叠保留、不丢弃,失败态永远有出路。
_Avoid_: 在新文档里拿它描述现行创建流;失败态零按钮的死路;把技术报错原文直接当首屏文案甩给用户。

### 活与状态

**Issue / 活**:
一件要干的具体工作,派给队友去做的最小单位。"活"是口语规范词,"卡"只在指界面卡片形态时用。挂了仓的项目,一件活就是仓里一个真实的 GitHub issue(issue 号即身份);没挂仓的项目,活保留纯本地身份,如实留白、不假装已同步。
_Avoid_: 任务(与"定时任务"撞)、卡片(指实体时用"活",指 UI 时才用"卡")、ticket、工单。

**Issue 状态流转**:
Backlog → Todo → InProgress → InReview → Done 的单向明路;另有 Blocked(阻塞,必填原因)、Cancelled。**Done 的唯一入边是 InReview**。
_Avoid_: 自造中间态;把 InReview 当终态。

**PR / Pull Request**:
一件活的真实交付载体——代码活、非代码活(报告、复盘文档)都以 PR 进仓。队友只能提 PR,永远不能 merge;"提 PR"是队友的动作,"验收"是人的动作,两者不是一回事。
_Avoid_: 把"提 PR"当成活已经完成;跳过 PR 直接推主干。

**评审中 / InReview**:
队友把活干完后能到达的最远状态。到此为止,人还没确认。挂了仓的活,这个状态由"存在一个开放 PR"派生,不是另开一个信号。
_Avoid_: 完成、待办结。

**完成 / Done**:
活的终态,**永远由人显式点**——任何自动化都不许把活推到 Done。人点的这一下有两种形态,取决于这件活有没有 PR:**有 PR** → 人 merge 那个 PR(merge 后 issue 关闭是它的后果,不是要再点一次的第二步);**没有 PR**(存量活、没挂仓的项目)→ 人在评审细节里点"确认完成(人裁)"。两条路都是人的显式动作,没有第三条自动的路。落 Done 的同一刻,队友战绩、产物登记、阶段吞吐同时入账,且同一件活绝不记两次。
_Avoid_: 自动完成、agent 自判完成、执行器自己 merge;以为挂仓之后"人裁"那条路就没了(全活 PR 化是纪律,不是硬闸)。

**阻塞 / Blocked**:
活卡住了、停在原地,必须写明原因。干砸了如实停在这里,可重试,不假装前进。
_Avoid_: 失败(失败是"运行"的结局,不是活的状态)、挂起。

### 工作流

**工作流 / Workflow**:
一段可复用的多环节干活流程(每个环节由某个角色执行)。分**静态**(定版、有版本号与使用数)与**动态**(按阶段临时生成)两类。可以有一篇自己的主文档——和技能的正文同性质,人写的真实 MD,不是拿 goal/prompt 拼出来的展示文本;内置五阶段模板没有这篇文档,如实标注"结构化定义,无原始文档",不是留白占位。
_Avoid_: 流水线(会误导成"不回流")、pipeline、脚本。

**循环 / Loop**:
工作流**内部**的微循环——某个环节失败重试、迭代到达标为止(`retries` / `max_iter`)。这是"环/loop"这个词的正主:对抗式 agent 自闭环的小循环。
_Avoid_: 用 "Loop/环" 指五阶段或整个产品运作模式;operating loop。

**阶段角色 / Phase Role**:
工作流一个环节在对抗循环里的真实身份:产出(Generator)、评审门(Evaluator,唯一能打回的角色)、打磨(Optimizer,不加新范围只精修)、中性(Neutral,其余情况的诚实默认)。由真实字段声明,不再靠阶段名关键词瞎猜。
_Avoid_: 靠中文阶段名猜角色;给非评审门的环节也配打回目标。

**打回 / Reject**:
把没过关的东西退回更早的状态重做。两处都叫这个词,是同一个概念的两个层次:①**人打回**——人在评审中的活上点"打回",活退回进行中;②**评审门打回**——工作流里的评审环节把产出退回某个更早环节。评审门那层:静态工作流的打回目标是作者当初写死的,动态工作流的目标由评审队友当次真实输出决定(机器解析,绝不关键词瞎猜)。小循环撞到重试上限仍不通过,不会被自动判失败——挂活转**阻塞**,写明原因,交人定夺。
_Avoid_: 把打回等同于失败;撞到上限自动判失败(违反"结论永不自动"的同一精神);为这两层各造一个新词(它们是同一件事的两个层次,共用"打回")。

**解析为流程图 / Parse to Flowchart**:
工作流详情页的显式按钮:读这段工作流的主文档,请真实队友解析出结构化的环节/角色/打回关系,解析成功才落库(可再编辑、可重解)。解析不出来就诚实挂"未解析,仅文本",绝不自动重跑、绝不瞎猜结构。
_Avoid_: 默认工作流创建/导入时就自带这层结构(现实是多数工作流从未解析过);把解析失败当成部分成功采纳。

**Agent Loop**(产品命题层):
"1 个 Builder + Agent Loop 取代 5 个专职角色"里的运作模式——人守质量门、agent 干 80% 的活。是产品主张的说法,专用于命题语境。
_Avoid_: 与"工作流内部循环"混用;把它当某个具体代码结构。

**运行 / Run**:
一次工作流或一件活的真实执行,有成败与耗时,全结局都入账(成功、失败都记)。运行在该活专属的 issue worktree 里干活(收尾后拆除;主工作区留着提供 master 视角);同一项目同一时刻只跑一个运行(串行锁,跨项目并行不禁止)。
_Avoid_: 执行(动词可用,名词统一"运行")、跑一遍(口语)。

**中止 / Cancel (Run)**:
人主动停掉一次正在跑的运行(`CancelRun`)。中止后活如实停在进行中、`settled_at` 留空——不冒充失败,更不自动完成。
_Avoid_: 取消(留给活的 Cancelled 终态)、终止后自动补一个结论。

**交互式运行 / Interactive run**:
与一次性后台运行(`claude -p` 一把跑完)并列的第二条执行轨:claude 就嵌在工作流看板的终端里,人全程可见、可插话、要停点取消,并按真实会话号精确续跑。**技能不是开工前提**——没绑技能的活照样走这条轨,拿活的标题+描述当开场 prompt,界面如实显示「未关联技能」。交互式活不写运行史——过程就在嵌入终端里,不是没跑过。人在场就是它的兜底:不靠机器掐表,后台轨的预算封顶/超时那套不照搬。
_Avoid_: 「一个技能=一个交互式会话」(旧口径,收口后已不成立:无技能的活同样开会话);把交互式活的空运行史当「没跑过」;把后台轨的自动兜底照搬到交互轨;嵌入终端(指 UI 载体时可用,指这条执行轨时统一说「交互式运行」)。

**咨询 / Consultation**:
对已经走到「评审中」或「完成」的活继续开的会话:接着原会话号往下聊,**不占交付名额、不记账、不改活的状态**。两个入口:活刚落定而终端进程还活着时,原来的交付会话自动降级成咨询(放开交付名额,但不杀终端、不清工作区);已落定的活再点「▶ 跑」,进的也是咨询,不会重开一次交付。聊出新工作要人显式点「转成新活」建一件新活——buddy 不替你猜意图。
_Avoid_: 把咨询当重跑(咨询永远不会把活推回进行中);以为咨询会再记一次账(降级那一刻账已经结过,同一件活绝不记两次)。

**衔接层 / Bridge prompt**:
buddy 注入交互式会话的常驻系统提示,装的是产出格式契约(写哪个文件、什么结构)与通用铁律(如「合并永远是人」)。分工:衔接层不可换,技能可换——换任何业界技能当方法论,契约仍由衔接层兜住。
_Avoid_: 把契约写进技能正文(换技能就丢);把衔接层当成技能的一部分。

### 健康(难造假)

**观测 / Observation**:
一个真实数据点(来自网关日志 / CI / git / telemetry / 连接器 / GitHub 采集 / 手填)。**只追加,一个观测=一个点,绝不插值**。
_Avoid_: 记录、log(观测是结构化数据点,不是日志文本)、埋点数据。

**指标 / Metric**:
由观测派生出的、可与目标比较的度量(带目标值、比较符、黄区带)。定义本身(名字、目标值、采集方案)也有正本,见"指标正本"。
_Avoid_: 数据、统计值;不要和"观测"混用——观测是原料,指标是产物。

**指标正本 / Metrics Source of Truth**:
一个项目的北极星 + 滞后指标 + 引领指标定义,写在仓里一份机读文件(`.bw/metrics.toml`,格式见 `docs/buddy/standards/metrics.md`,不是 BW 私有配置),改指标和改代码一样走 PR 评审。BW 本地只缓存一份用于展示,缓存永远从这份正本同步,绝不反过来。同目录还有第二份机读正本 `.bw/connectors.toml`,登记脚本连接器(见「脚本连接器」)——指标定义与采集装置各有各的正本。
_Avoid_: 把 SQLite 里的缓存当成定义的正本;绕开 PR 直接在 BW 界面里"定义"一条指标。

**业务指标 / 项目指标(两层分流)**:
总览把指标分两层看:**业务指标**=用户自己的北极星/引领/滞后指标,点灯、参与健康上卷;**项目指标**=buddy 自动播种的代码仓统计(开放 Issue 数、已合入 MR 数、阶段完成),所有项目都有,只当现状数看——不点灯、不上卷。
_Avoid_: 让仓统计数字点灯(工程虚荣指标混进健康);把两层混在一张表里读。

**停用(指标)/ Archived metric**:
把一条指标整个退出派生链——不再算它的灯、不进任何上卷——但历史观测原样留着,可恢复。正本里删掉的行自动停用;手建行由人显式停。
_Avoid_: 删除(观测还在,历史不抹)、隐藏(停用发生在派生链,不是展示层筛选)。

**上卷 / Roll-up**:
低层信号聚合成上一层信号的那一步(指标→阶段→项目),取最差者:有红则红,无红有黄则黄。
_Avoid_: 平均分、加权打分(上卷不打分,只取最差)。

**先对后亮 (Right-first, Collectible-second)**:
指标起草的顺序铁律——先想清楚"对不对"(北极星该衡量什么),再管"采不采得到";每条指标定义必须附一个真实采集方案(GitHub 查询 / 连接器 / 脚本 / BW 自记账 / 手填),绑不上数据就如实显示 Unknown,并给出点亮它最便宜的路径。北极星绝不因为"暂时采不到"就退化成一个方便统计的替代数字。
_Avoid_: 反过来先看什么好采再定指标(工程虚荣指标);指标没有采集方案也能"暂时"存在。

**信号 / Signal**:
健康灯的三态结论 + 未知:绿(隐身)/ 黄 / 红 / **Unknown**。只能由真实数据派生,**无数据 = Unknown,绝不假装绿**;数据过期自动降级。
_Avoid_: 状态、健康值、分数;不要说"信号是绿的就是好的"以外的手动设定——信号从不手设。

**未知 / Unknown**:
没有数据时的诚实态,灰色。喂再多数、不设目标也不会变绿。它是产品的诚实底线,不是"待补"占位。
_Avoid_: 默认绿、N/A、待定。

**北极星 / North Star**:
每个项目唯一的顶层目标。可由工作台把用户回答组合成草稿,但绝不编造数字替用户答题。
_Avoid_: KPI、OKR、总目标;不要有多个"北极星"。

**引领指标 / 滞后指标 (Leading / Lagging)**:
引领指标=可控、可提前统计、能被真实采集的先行量;滞后指标=结果量。控制点 4 要求引领指标可控且难造假。
_Avoid_: 先行/结果指标混用不同词;过程指标。

**手填 / Manual**:
数据来源是人手工填的,带"手填"徽记,直到接上真实来源。可用,但如实标注、不冒充自动采集。
_Avoid_: 假数据、mock(mock 是另一回事,见下)、临时值。

### 验收(证据优先于断言)

**验收 / Acceptance**:
"这件东西真的做成了吗"的那一次人为确认。两个层次别混:**一件活的验收**=人 merge PR 或点"确认完成(人裁)";**一批工作的验收**=审核方亲手跑验收流、出证据报告,人看证据终审。两层共用"验收"这个词是对的——都是"人点头才算数",没有自动通过的路。
_Avoid_: 测试通过、CI 绿(那是自证材料,不是验收本身);把 agent 自报完成当验收。

**证据链 / Evidence chain**:
支撑验收的真实原值:界面截图、数据库读回的原始数字、逐步执行日志。**断言不是防线**——写实现的一方随时能绕过自己写的断言;防线是审核方独立跑一遍、亲眼看到的东西。报告里永远同时给原值和预期,预期只是陈述,读者自行对照。
_Avoid_: 拿"断言全过"当验收结论;只给结论不给原值;补一张"看起来对"的截图。

**验收流 / 考卷 (Flow)**:
一条写死了"点哪里、看到什么、最后数据库里该是什么"的可重跑验收剧本。步骤用**语义**描述("点这张卡上的开工按钮"),不写坐标、不写选择器——界面改版不会脆断。考卷是**唯一的动作来源**:实跑严格照考卷下发,绝不由操作者临时敲命令——否则被验的就成了"操作者敲了什么",考卷沦为装饰。派工时考卷先行(先定验收标准再实现);实现方可以补步骤,不可以删步骤。
_Avoid_: 坐标录制回放;跑流时临时加动作("我再点一下看看");把考卷当事后补的文档。

**真点击 / Real click**:
验收流里的每一次点击都走和人手点击**完全相同**的那条链路(界面事件 → 命令 → 内核 → 数据库),而不是绕过界面直接给内核发命令。这个等价性就是验收流的全部意义所在:它证明的是界面本身能用,不只是内核肯收命令。
_Avoid_: 为了跑通而直接调内核命令;把"内核接受了命令"当成"界面能用"。

**常青流 / Evergreen flow**:
锁住产品铁律的那几条验收流,每批都必须全绿才允许合批。与之相对的是**每票验收流**——交付时必须绿,之后要么并进常青流、要么退役。刻意不做永久累积的回归大坝:那种东西迟早烂成假绿。
_Avoid_: 把所有历史验收流都留着当回归套件;以为流越多越安全。

**证据报告 / Evidence report**:
一批验收跑完后给人看的单份报告,三态如实:**绿**=全过 / **红**=失败(附失败现场)/ **灰**=没跑成或环境中断。灰不是红也绝不算绿——和健康信号灯的 Unknown 同一个哲学。报告是给人终审用的,不是给 agent 当上下文。
_Avoid_: 把灰算成绿或红;失败了跳过继续跑然后报绿。

**对抗验收 / Adversarial acceptance**:
实现方自证(门禁全过 + 按考卷自跑通过并自报),审核方**独立**再跑一遍、读代码、出证据报告,最后人看报告终审。关键在"独立":报告出自审核方之手,实现方碰不到,这就是"对抗"的本体。
_Avoid_: 实现方自己出验收报告;审核方只读代码不跑流;跳过人的终审直接合。

**种子库 / Fixture**:
验收流用的确定性数据库,内容全部由真实命令播种而来(不是裸 SQL 塞的假数据),跑起来不碰网关、结果可复现。**只读只拷**:每次跑流都复制一份到临时路径,原种子库和用户的真实日常库永远只被拷贝、绝不被打开写入。
_Avoid_: 手工造数据当种子;直接在种子库或真实日常库上跑流。

### 自动化(只自动建活,绝不自动完成)

**定时任务 / Scheduled task (Cron)**:
到点自动触发的作业,针对某项目或全部项目。到点做的事分两类:**真的去干**(运行一个工作流 / 运行一个技能 / 跑一段裸 Prompt)或**只留痕不动手**(建一件活、或采集一条观测)——前者会真调用执行器,后者绝不派生出"完成"。
_Avoid_: 例行(那是阶段的节奏件 Routine)、计划任务、job 混称;裸 "cron" 指代整个特性时也统一叫"定时任务";以为到点只有"运行工作流/建活"两种模式。

**Autopilot / 自动建活**:
定时任务的一种模式——到点把例行活挂上看板、指派好队友,**只建活、绝不自动跑、更不自动完成**(no-hijack)。
_Avoid_: 自动执行、自动运行、自动化完成——Autopilot 从不自己完成活。

**采集器 / Collector**:
定时任务的另一种模式——到点把外部真实数据(GitHub issue/PR 计数、脚本连接器产出的指标值等)拉成一条条追加观测,配一个"立即采集"手动按钮。采集是**观测**不是**干活**:不运行任何工作流、不结算任何活,所以能自动触发而不违反"Done 永不自动"。
_Avoid_: 和 Autopilot 混为一谈(两者都不自动完成,但 Autopilot 产出活、采集器产出观测);把采集器等同于连接器(连接器是更通用的对外连接概念)。

**例行 / Routine**:
一个阶段自带的节奏件:该阶段的循环节奏 + 关注项 + 健康 feed。挂在阶段上,不是全局作业。
_Avoid_: 与"定时任务"混用;例行任务(会和 Cron 撞)。

### 复利(越用越强)

**蒸馏 / Distill**:
把一件做完的活提炼成一篇带完整做法的技能,永远记着它来自哪件活。
_Avoid_: 总结、归档、沉淀(沉淀指整个复利过程,别专指这一步)。

**技能 / Skill**:
一篇可复用的做法说明,真实形态是一个**文件夹**(SKILL.md 正文 + 任意数量的支撑文件——参考资料、脚本、跨平台包装……),不是一个拍平的文本字段。下次干同类活时按相关度自动带上(同阶段优先,有上限),用一次记一次(uses)。
_Avoid_: 文档、知识、SOP;不要和"五角色的打法"混——那是内置方法论,技能是蒸馏产物;把技能等同于它的 SKILL.md 正文(支撑文件也是它的一部分)。

**注入 / Inject**:
干活时自动把最相关的几条技能带进队友的上下文。按名解析走**就近优先**(本项目行遮蔽全局同名行),他项目的技能永不注入;记账落在被注入的那一行(记账行==注入行)。
_Avoid_: 加载、引用、喂(口语,正式文档用"注入");全库按名匹配(谁先入库谁被注入,是 plan/20 修掉的旧病)。

**复利 / Compounding**:
"做过的活沉淀成方法、越用越懂你项目"这个整体属性:蒸馏→注入→uses+1 的增益链。
_Avoid_: 积累、飞轮;专指这条链时用"复利"。

**战绩 / 胜率 (Win rate)**:
队友的真实成败统计派生出的胜率,**永远派生、绝不手设**;无战绩时是"暂无",不是 0%。
_Avoid_: 评分、rating、可信度手调。

### 资产与来源

**资产目录 / Hub**:
可复用资产的浏览目录,分三类:工作流 / 技能 / 队友(`HubKind`)。是"库/市场"性质的浏览面,不是定时任务(别被 "Cron Hub" 旧说法误导)。注意界面上「Hub」另有一义——各管理屏的通称(连接器、定时、知识、通知等屏都叫某某 Hub);本词条专指三类资产目录,两义别混。
_Avoid_: 把 Hub 和 Cron/定时混为一体;中心、枢纽;拿界面屏数当资产目录的类数。

**业界来源 / HubSource**:
一份资产(工作流/技能/队友)从哪来,统一四类:**官方选型**(BW 持续挑选引入的高分精品库,配一个子标签写明具体是哪个,如 ecc、mattpocock-skills、superpowers、mohitagw15856/pm-claude-skills——库只会越选越多,不为每个库单开一类)、**选型引入**(用户后期自选引入官方集之外的第三方,预留,今天无入口)、**自建**、**会话内**。另有一个特殊子标签 `project-assets`:项目自有工作区扫描进来的登记行,只登记可见,不进任何注入下拉。
_Avoid_: 和"观测源 (SourceKind)"混用同一个"来源"词——它们是不同轴;为每个新库单开一个来源分类(库名进子标签,不进分类本身)。

**作用域 / Scope(三层隔离)**:
一份资产或配置归谁的轴,三层:**全局层**(基础库与默认——bw-standard、外部选型库,为项目服务)、**项目层**(项目自有副本与蒸馏产物,各自演化、各自记账,随项目级联删除)、**工作目录**(即「仓」词条里的**工作区**——同一个东西,按执行现场视角称呼;现场的登记物永不冒充库资产)。铁律:**他项目的行在任何选择池、任何按名解析里都不出现、永不命中**。共享只有两条明路:全局层对项目可见,或收录复制归我。
_Avoid_: 把三层当权限系统(它是归属与可见性,不是审批);隐式跨项目共享;把工作目录扫描行当库资产。

**就近优先 / Most specific wins**:
按名解析(技能注入、定时任务按名找工作流)的唯一规则:本项目行 → 全局行 → 如实落空;项目行存在即**行级遮蔽**全局同名行。撞名守卫按作用域:同一作用域内名字唯一,跨作用域允许同名(收录复制的天然结果),由本规则消歧。
_Avoid_: 全库按名匹配;隐式合并两层;解析落空时错记到共享行(如实跳过,绝不污账)。

**收录 / Adopt**:
把全局共享资产**复制一份归当前项目**(`AdoptIntoProject`):新 id、归属本项目、uses/战绩清零立新账、描述加尾注「引入自 … · 日期」;**来源标签原样保留**(出处保真:官方库的文本仍标着那个库)。之后各改各的,与源脱钩。反向(项目资产验证后升格回全局,promote)留口未建。
_Avoid_: 引用/链接(收录是复制不是引用);收录时翻转来源标签;把收录当升格(方向相反)。「引入」一词多义要当心——界面按钮「引入本项目」与尾注「引入自」说的就是本词条的收录;「选型引入」(见业界来源)是库级的另一层动作,别混。

**空壳 / Hollow shell**:
资产目录里只有名字和一句描述、没有正文的那种条目——早期从外部库"引用"来的目录行。它们看着像有 300 条资产,实际一条也用不了。判定要多道独立闸门(没正文、零使用、没被任何东西引用……),满足才清;有真实使用痕迹的一律保留并如实标注。
_Avoid_: 把空壳当库存规模来展示;清理时只看"有没有正文"一个条件。

**连接器 / Connector**:
项目对外部真实服务(codehub、GitHub、claude CLI 等)的连接,能真探活、真同步的才叫连接,其余是登记条目、如实标未同步。V1 钉下的三层归属:对外连接器(管探活)· 业务脚本(干业务活,见「脚本连接器」)· 定时器(管调度,见「定时任务」)。本地工作区读取不算对外连接器——V1 起不再建 git-repo 连接器,存量行仅服务旧数据。
_Avoid_: 集成、插件、数据源;把本地工作区读取当成一个连接器(那是 V1 之前的旧态)。

**脚本连接器 / Script connector**:
连接器的一种(`kind=script`):把项目里任何"能产出指标值的脚本"接进来——采集时跑脚本、只读它的输出文件、按各指标的采集查询取值成观测。登记正本是仓里的 `.bw/connectors.toml`(格式见 `docs/buddy/standards/connectors.md`);「绑数据」的操作含义就是搭好这套采集装置——写一个能产出机读输出的脚本并登记进正本,不是改一下指标定义的采集字段就完。脚本自身的环境依赖(登录态、运行时……)是项目侧责任,BW 只调用、不管内部。
_Avoid_: 把它当 BW 内置采集逻辑(脚本住在项目工作区,是项目的资产);让 BW 替脚本背环境依赖;把「绑数据」说成改配置(绑数据=真搭采集装置)。

**探活 / Probe**:
对一个外部连接真连一次、看通不通;探不通就一个字节都不写。「能真同步的才叫连接」这条判据的具体动作。
_Avoid_: 把配置填好了当成连接通了;健康检查(探活是一次性动作,不是持续监控)。

**产物 / Artifact**:
队友运行真实产出的文件,分类登记(文档/代码/测试/脚本/配置/其他)。
_Avoid_: 输出、成果物、deliverable。

**mock / 流程演示**:
未接真实执行时的演示替身,产出**自我标注**【mock】。唯一目的是廉价验证管线本身,绝不冒充真实执行。
_Avoid_: 假装真实、demo 数据不标注;和"手填 (Manual)"混——手填是真人真数据,mock 是演示替身。

### 工程操作词(仓库工作方式的行话)

> 这一节收录的是"我们怎么干活"的词,不是产品概念。它们高频出现在 CLAUDE.md、plan/ 文档、commit message 和对话汇报里,却长期没有定义——本节补上。给用户写汇报时,优先用这些词条里的人话解释,而不是甩裸词。

**读回 / Read-back**:
把一个陈述涉及的数字或状态,从数据库(`sqlite3`)或工作区里**独立重新查出来**核对。是「报告不代答」纪律的核心动作:任何"已完成/数字是 X"的说法都必须能读回。
_Avoid_: 拿截图或 agent 自报当读回;复查(读回强调独立取证,不是再看一眼)。

**门禁 / Gate checks**:
每个 commit 前必须全部通过的检查组(格式化、clippy、wasm 编译、内核无 UI 守卫脚本、桌面壳编译)。本地清单与 CI 目前并非完全一致:CI 侧还跑 `cargo test`(V1 起内核层带有少量单元测试),差异以 `.github/workflows/ci.yml` 为准。本地先过门禁,CI 是远端复核。
_Avoid_: 测试基线(行为正确性以 E2E 读回为主,不建永久累积的回归大坝);把门禁和 E2E 验证混为一谈(门禁管编译期,E2E 管行为)。

**记账 / 入账 / 结算 (Settle)**:
一件活落定(完成或失败)那一刻,把它产生的事实——队友战绩、产物登记、成败耗时、阶段吞吐——写进库。**同一件活绝不记两次**(这条约束在代码里叫 settle-once)。
_Avoid_: 在给人的正文里写 "settle-once"(说"绝不记两次");统计、打点。

**落库**:
泛指任何数据写进 SQLite 持久化。比「记账」宽:记账专指活落定那笔账,落库是所有写入。
_Avoid_: 保存(口语)、sync。

**台账 / Ledger**:
持续追加的问题/任务登记表,通常带编号(如 plan/06 的「缺口台账」G1-G11)。只增不删:处理完标状态,不抹历史。
_Avoid_: 清单(一次性勾完就扔的才叫清单)、backlog 混称。

**遗留清单 / LEFTOVERS**:
实践中冒出、但不在当前窗口解的问题的唯一完整清单(`docs/v1-prototype/LEFTOVERS.md`),每条带产生窗口、现象、未决点、处置。与「台账」是近亲:台账登记设计缺口,遗留清单收实践溢出物。
_Avoid_: 散落在各设计文档里各记各的(已归拢,读遗留只看这一份)。

**留痕 / Append-only record**:
动作发生时自动留下的不可篡改记录——只追加、不修改、不删除。交棒记录、观测都是这种表。
_Avoid_: 日志/log(留痕是结构化数据点,不是文本日志)。

**封口 / Sealed (type)**:
一个类型把构造入口锁死,外部拿不到构造器,值只能经指定路径产生(如 `Derived<Signal>`:信号值只能出自重算函数)。是"信号绝不手设"的编译期保障。
_Avoid_: 封装(泛泛的 encapsulation 不是这个意思)、加锁(和并发无关)。

**收口 / Converge to one path**:
把散落的多条实现路径/多份副本收敛到唯一一条正道上(如"技能装载收口到唯一解析器一条链")。目的:同一件事只有一个地方在做。
_Avoid_: 重构(收口有特定目标——唯一路径,不是泛泛整理)。

**收窄 / Narrow (scope)**:
把一个查询/接口/选择池的可见范围改小,只暴露当前场景合法的子集(如"指派时的队友选择池收窄到本项目")。
_Avoid_: 过滤(收窄是设计层决定并有守卫把守,不是展示层临时筛选)。

**留口 / Seam for later**:
为将来的功能预留的接缝——一个字段、一个 trait、一条编译目标——现在不实现,也不假装实现(如为将来 Web 版保活的 wasm32 编译检查,真跑在 bw-core 和 ui 上;`app-web` 目录只是占位 README,刻意不参与编译)。
_Avoid_: TODO(留口是结构性预留,不是待办注释);占位 UI 放假数据(违反「留白如实标注」)。

**对账 / Reconcile**:
把两份理应一致的数据(如代码里内置的标准技能正本 vs 数据库里的行)逐项比对、以正本为准修正差异,通常在启动时自动做。
_Avoid_: 同步(对账有明确的正本方向,不是双向合并)。

**指挥器 / Conductor script**:
不开界面、直接调用内核命令走完某段完整流程的脚本或 example(如 `real_demo`)。用于 headless 验证和批量操作。
_Avoid_: runner、driver;和「监理」混用(指挥器干活,监理盯着重试)。

**监理 / Supervisor script**:
盯着一个容易被网关抖动打断的长流程、失败自动重试的守护脚本(如 `supervise-real-demo.sh`)。要求幂等:重复跑不产生重复数据。
_Avoid_: 重试循环(监理还负责判断状态、决定停不停)。

**棒次 / 交接件 (Baton leg / Handoff doc)**:
「棒次」=接力赛里的一棒,借指由一个会话(或一个人)完成的一段工作区间;「交接件」=写给下一棒的自足文档(`iterations/HANDOFF-*.md`),要求不读原对话也能接手。交接件写完即成历史档案,不描述现状。
_Avoid_: 把交接件当现状文档读;会议纪要。

**窗口 / Delivery window**:
V1 产品化的交付切片单位:一个窗口只做自己申明范围内的活,内容必须来自真实实践,不越界替别的窗口做主。三个窗口(W1 纳入项目 / W2 找指标·绑数据 / W3 总览重构)的设计事实源在 `docs/v1-prototype/`。
_Avoid_: 与「批次」混用(窗口强调边界申明,批次只是时间分组);W 编号不查 `docs/code-schemes.md` 就用(W 字母已多次撞车)。

**穿刺 / 真人穿刺 (Piercing)**:
用户拿一个真实项目从头走一遍产品,把撞到的问题按用户口径原样记下,再定位根因、出修复设计。与验收流(考卷)互补:考卷验的是写死的剧本,穿刺验的是真人真项目撞出来的东西。产出是带编号的反馈清单与决议。
_Avoid_: 当成随手试用(穿刺要求真实项目、全程留痕、逐条出决议)。

**5 步法**:
V1 每个窗口的固定推进流程:先取证现状(不基于猜)→ 对齐原型 → 开发 → 验证 → 填指南。最后一步是硬要求:功能没写进使用指导就不算完。
_Avoid_: 跳过取证直接开发;做完不填指南就交。

**使用指导 / 维护指南(两本)**:
产品指南的两本(`docs/guide/`),用不同维度组织、互为索引:使用指导按界面操作阶段写给 Builder,只写「点什么、看到什么、得到什么」;维护指南按跨阶段机制写给要改/维护 buddy 的开发。写作规范见 `docs/guide/填写规范.md`。
_Avoid_: 在使用指导里写字段名/解析器这类开发话术(那是维护指南的领地);两本混着组织(一本按操作阶段、一本按机制,是有意的)。

**铁律 / Iron rule**:
产品行为的不可违反约束(如「完成永远由人点」「信号绝不手设」),由类型、守卫和 E2E 抽查共同守住——违反即 bug,没有权衡余地。
_Avoid_: 原则(可权衡的才叫原则)、最佳实践。

**幂等 / Idempotent**:
同一操作重复执行多次,结果与执行一次相同——因此可以放心重试。标准计算机术语;BW 里常见于 schema 迁移守卫和监理脚本。
_Avoid_: 可重入(是另一个并发概念,别混用)。

**深链 / Deep link (启动直达)**:
启动应用时用环境变量(`BW_OPEN` / `BW_PANEL` / `BW_HUB` / `BW_SEL`)直接跳到指定项目/面板/资产的方式。stderr 的 `[BW_OPEN]` 日志是"真的渲染到了"的可靠证明,E2E 验证靠它。
_Avoid_: 快捷方式;URL scheme(BW 的深链走环境变量,不走 URL)。

**代码图 / CodeGraph**:
仓库共享的代码关系快照(`.codegraph/codegraph.db`),给人和 agent 查跨文件调用链与影响面用:先跑 `codegraph explore / callers / callees / impact`,别一上来把大量源码整份读进上下文;改完代码跑 `codegraph sync` 让本地查询跟上。**功能分支不提交这个 db**——PR 上由 CI 独立核验,合入 `main` 后再由 GitHub Actions 单点刷新并提交,并行 PR 因此不会围绕一个二进制文件打架。约定正本在根目录 `AGENTS.md`,日常用法见 `DEVELOPMENT.md`「共享代码图谱」一节。
_Avoid_: 索引(代码图存的是调用关系,不只是符号位置);和「读回」混用(读回核的是数据库里的运行时数字,代码图看的是源码静态关系);在功能分支里顺手 `git add .codegraph/`。

---

_命名冲突的收敛决策与代码改名台账见 [`docs/adr/0001-ubiquitous-language.md`](docs/adr/0001-ubiquitous-language.md);任务代号各系列的索引见 [`docs/code-schemes.md`](docs/code-schemes.md)。_
