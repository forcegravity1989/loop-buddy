# Builders' Workbench — 领域语言 (Ubiquitous Language)

单人构建者的项目管理工作台。本文件是**唯一的规范词表**:代码、文档、界面、对话都用这里选定的词，别再各叫各的。

> 定义原则:每个词只说它**是什么**(1–2 句)，不写实现;`_Avoid_` 列出被它取代的说法。
> 这是词表,不是规格——设计事实源仍是 [`plan/06-overall-alignment.md`](plan/06-overall-alignment.md);
> 命名冲突的收敛与改名计划见 [`docs/adr/0001-ubiquitous-language.md`](docs/adr/0001-ubiquitous-language.md)。

## Language

### 人与角色

**Builder / 构建者**:
使用工作台的那个人——端到端 own 一个项目的单人系统设计者,产品的主角。全局唯一,专指人类。
_Avoid_: user、owner、开发者;不要用 "Builder" 指构建阶段的角色(那是**构建师**)。

**队友 / Teammate**:
被指派去干活的 AI 人格,有名字、模型、真实战绩与胜率。派活的对象是队友。真实定义形态是一份 **AGENT.md**(frontmatter 声明名字/描述/工具/模型 + 一段指令正文,同 Claude Code 的 subagent 定义格式)。
_Avoid_: Agent(留给运行时执行体)、AgentCard(那是队友在目录里的一行展示)、Squad、workbuddy、multica、机器人。

**工具集 / AllowedTools**:
一个队友被真实允许调用的工具白名单,和 claude CLI `--allowedTools` 是同一件事。执行时由适配层翻译给底层 CLI,字段本身不绑定某个具体 CLI。
_Avoid_: 把它当装饰性标签——它真实生效,执行时真的会限制队友能用什么。

**Agent CLI**:
真正执行一个队友的智能体框架/CLI(Claude Code / Codex / Cursor……)。今天只有 Claude Code 真能跑;选了别的会诚实报错"本机未安装",绝不假装成功或悄悄退回 Claude Code。
_Avoid_: 和"模型"字段混为一谈(model 是模型标签,Agent CLI 是执行引擎);假装多 CLI 已支持。

**五角色 / Stage Role**:
五个阶段各自的方法论人格:原型师·构建师·优化师·运营推广师·运维师 (Prototyper / Builder-role / Optimizer / Grower / Maintainer)。每个角色带该阶段的打法与开场指令(preamble),是内置方法论,不是某个具体队友。
_Avoid_: 把"五角色"和"聊天发言方 (Author)"混为一谈——后者只有人/AI 两方;构建师的英文别写成裸 "Builder"(与构建者撞)。

**发言方 / Author**:
一条对话消息出自谁——人(Builder)还是 AI(Agent)。纯 UI 归属轴,与"五角色"无关。
_Avoid_: 用 "Role" 单指这个(Role 已被五角色占用;代码里对应类型已改名为 `Author`,见 ADR)。

### 项目生命周期

**阶段 / Stage**:
项目的五个方法论阶段:**原型 → 构建 → 优化 → 运营推广 → 运维**,运维复盘回流到原型(**闭环回流**,所以是环不是流水线)。每阶段自带该问什么、什么节奏、DoD、常见坑。这是通用方法论,不随项目现编。
_Avoid_: 用裸词 "环 / Loop / operating loop" 指五阶段——"环/循环"留给工作流内部的微循环;五阶段的成环性叫"**闭环回流**"这个属性,不当名词用。step/step-N/七步向导(已演进为五阶段)。

**交棒 / Handoff**:
从当前阶段推进到下一阶段的那一次显式动作。只留痕、不拦人——清单没勾完也能交。
_Avoid_: 审批、gate(交棒不是审批门)、阶段切换。

**带险交棒 / Risky handoff**:
DoD 清单未勾完就交棒的情形——永久记下当时缺什么、留了几件活。你有权冒险,但不能无痕。
_Avoid_: 强制完成、跳过。

**DoD / 完成清单**:
一个阶段"做到什么算完"的明确勾选清单。纯人工勾选,永不派生、永不代填。
_Avoid_: Definition of Done 缩写混用时统一写 "DoD";不要叫验收标准(那偏向 Issue 验收)。

**时期 / Maturity Period**:
项目所处的成熟度期:探索期(0→1) · 扩张期(1→N) · 成熟期(Sustain)。用户创建时声明,纯信息性——只影响项目墙的精力配比展示,不驱动任何派生。
_Avoid_: 周期 / Cycle / ProjectCycle(裸词"周期/环"太容易和五阶段闭环、工作流循环撞;代码里对应类型已改名为 `MaturityPeriod`,见 ADR)、生命周期。

**就绪状态 / Readiness**:
项目本身的就绪度:运营中(Running)还是冷启动中(创建流程未完成)。是"状态",不是"阶段"。
_Avoid_: Phase / ProjectPhase(与"阶段 Stage"撞;代码里对应类型已改名为 `Readiness`,见 ADR)。

### 项目与 GitHub(产品信息正本在仓里)

**产品信息 / 过程信息 (Product / Process)**:
一个项目切成两类信息:产品信息(代码、Spec、原型、Issue、指标定义)正本住在 GitHub 仓;过程信息(会话、运行记账、信号、交棒)住在 BW 本地库。BW 是操作台 + 信息转化层,不是又一份正本。
_Avoid_: 把 BW 当项目的唯一数据源;把仓说成"备份"或"同步目标"(它才是正本所在地)。

**仓 / Repo**:
项目背后真实的 GitHub 仓库——新建或接入。Issue、PR、指标定义的正本都挂在它上面;没挂仓的项目(存量项目、纯本地项目)如实留白,绝不假装有仓。
_Avoid_: 和"工作区"混用(工作区只是仓的本地克隆路径,仓才是身份)。

**仓平台 / Repo Provider**:
建仓时选的托管平台。今天唯一真通的是 GitHub,其余(GitLab、Gitcode……)如实灰置"未接"、不可选——它是一个选择器,不是抢先做好的多平台抽象;真要接第二个平台时再开缝。
_Avoid_: 让"未接"的选项看起来可点;不经选择就默认只有 GitHub。

### 创建流(快、能退、能续)

**创建流 / Creation Flow**:
一个项目从"提交意图"到"正式运营"之间的引导过程:仓 → 意图 → 快速问题 → 起草 → 确认。刻意做得快(一气呵成、秒级);中途任何一步都能退回项目墙,也能在中断后续上;失败不落死路。
_Avoid_: 把它当成五阶段之一(它是正式运营前的引导,不是运营阶段);创建流卡住或退不出去(是已修的真实缺陷,不是设计意图)。

**起草 / Drafting**:
创建流自己的"体系起草"那一跑——生成北极星草案、指标框架、五阶段激活。永久锁定用演示替身(mock)跑,这不是降级而是设计:真正费时间、需要真队友的活,交给标配 Issue 三件套去干,不堵创建流的路。
_Avoid_: 把起草当成一次真实的队友思考过程;把它和标配 Issue 里的真实起草工作混为一谈。

**标配 / Starter**:
项目创建落地时自动配好的默认内容(一批 Issue、配套的 Skill、一个采集用的定时任务)——不是用户自己发明的起手式,也不是内置方法论,是"开箱即有"的起点。
_Avoid_: 和"五角色的内置方法论"混为一谈(那是永久性的打法,标配是一次性的起手配置)。

**标配 Issue 三件套 / Starter Issue Trio**:
挂仓项目创建落地时自动建好的三张起手 Issue,依赖序:竞品分析 → 找指标 → 绑数据,各自挂一个配套 Skill。创建流末卡问一句"立即让队友开工第一件?",勾了就真实开工竞品分析那张。
_Avoid_: 以为创建流会自动把三件套跑完(创建流本身不跑真队友,见"起草");当成随便一件普通活(它们有固定依赖序和固定 Skill 绑定)。

**三出路 / Three Ways Out**:
起草失败时给的三个真实出口:重试起草 / 先用模板继续(跳过起草,直接看已填字段)/ 退出(已完成部分留档,不丢)。失败原因翻成人话上屏,技术原文折叠保留、不丢弃。
_Avoid_: 失败态零按钮的死路;把技术报错原文直接当首屏文案甩给用户。

### 活与状态

**Issue / 活**:
一件要干的具体工作,派给队友去做的最小单位。"活"是口语规范词,"卡"只在指界面卡片形态时用。挂了仓的项目,一件活就是仓里一个真实的 GitHub issue(issue 号即身份);没挂仓的项目,活保留纯本地身份,如实留白、不假装已同步。
_Avoid_: 任务(与"定时任务"撞)、卡片(指实体时用"活",指 UI 时才用"卡")、ticket、工单。

**Issue 状态流转**:
Backlog → Todo → InProgress → InReview → Done 的单向明路;另有 Blocked(阻塞,必填原因)、Cancelled。**Done 的唯一入边是 InReview**。
_Avoid_: 自造中间态;把 InReview 当终态。

**PR / Pull Request**:
一件活的真实交付载体——代码活、非代码活(报告、复盘文档)都以 PR 进仓。队友只能提 PR,永远不能 merge;"提 PR"是队友的动作,"验收"是人的动作,两者不是一回事。
_Avoid_: 把"提 PR"当成活已经完成;跳过 PR 直接推主干。

**评审中 / InReview**:
队友把活干完后能到达的最远状态。到此为止,人还没确认。挂了仓的活,这个状态由"存在一个开放 PR"派生,不是另开一个信号。
_Avoid_: 完成、待办结。

**完成 / Done**:
活的终态,**永远由人显式点**——任何自动化都不许把活推到 Done。人点的这一下有两种形态,取决于这件活有没有 PR:**有 PR** → 人 merge 那个 PR(merge 后 issue 关闭是它的后果,不是要再点一次的第二步);**没有 PR**(存量活、没挂仓的项目)→ 人在评审细节里点"确认完成(人裁)"。两条路都是人的显式动作,没有第三条自动的路。落 Done 的同一刻,队友战绩、产物登记、阶段吞吐同时入账,且同一件活绝不记两次。
_Avoid_: 自动完成、agent 自判完成、执行器自己 merge;以为挂仓之后"人裁"那条路就没了(全活 PR 化是纪律,不是硬闸)。

**阻塞 / Blocked**:
活卡住了、停在原地,必须写明原因。干砸了如实停在这里,可重试,不假装前进。
_Avoid_: 失败(失败是"运行"的结局,不是活的状态)、挂起。

### 工作流

**工作流 / Workflow**:
一段可复用的多环节干活流程(每个环节由某个角色执行)。分**静态**(定版、有版本号与使用数)与**动态**(按阶段临时生成)两类。可以有一篇自己的主文档——和技能的正文同性质,人写的真实 MD,不是拿 goal/prompt 拼出来的展示文本;内置五阶段模板没有这篇文档,如实标注"结构化定义,无原始文档",不是留白占位。
_Avoid_: 流水线(会误导成"不回流")、pipeline、脚本。

**循环 / Loop**:
工作流**内部**的微循环——某个环节失败重试、迭代到达标为止(`retries` / `max_iter`)。这是"环/loop"这个词的正主:对抗式 agent 自闭环的小循环。
_Avoid_: 用 "Loop/环" 指五阶段或整个产品运作模式;operating loop。

**阶段角色 / Phase Role**:
工作流一个环节在对抗循环里的真实身份:产出(Generator)、评审门(Evaluator,唯一能打回的角色)、打磨(Optimizer,不加新范围只精修)、中性(Neutral,其余情况的诚实默认)。由真实字段声明,不再靠阶段名关键词瞎猜。
_Avoid_: 靠中文阶段名猜角色;给非评审门的环节也配打回目标。

**打回 / Reject**:
把没过关的东西退回更早的状态重做。两处都叫这个词,是同一个概念的两个层次:①**人打回**——人在评审中的活上点"打回",活退回进行中;②**评审门打回**——工作流里的评审环节把产出退回某个更早环节。评审门那层:静态工作流的打回目标是作者当初写死的,动态工作流的目标由评审队友当次真实输出决定(机器解析,绝不关键词瞎猜)。小循环撞到重试上限仍不通过,不会被自动判失败——挂活转**阻塞**,写明原因,交人定夺。
_Avoid_: 把打回等同于失败;撞到上限自动判失败(违反"结论永不自动"的同一精神);为这两层各造一个新词(它们是同一件事的两个层次,共用"打回")。

**解析为流程图 / Parse to Flowchart**:
工作流详情页的显式按钮:读这段工作流的主文档,请真实队友解析出结构化的环节/角色/打回关系,解析成功才落库(可再编辑、可重解)。解析不出来就诚实挂"未解析,仅文本",绝不自动重跑、绝不瞎猜结构。
_Avoid_: 默认工作流创建/导入时就自带这层结构(现实是多数工作流从未解析过);把解析失败当成部分成功采纳。

**Agent Loop**(产品命题层):
"1 个 Builder + Agent Loop 取代 5 个专职角色"里的运作模式——人守质量门、agent 干 80% 的活。是产品主张的说法,专用于命题语境。
_Avoid_: 与"工作流内部循环"混用;把它当某个具体代码结构。

**运行 / Run**:
一次工作流或一件活的真实执行,有成败与耗时,全结局都入账(成功、失败都记)。
_Avoid_: 执行(动词可用,名词统一"运行")、跑一遍(口语)。

### 健康(难造假)

**观测 / Observation**:
一个真实数据点(来自网关日志 / CI / git / telemetry / 连接器 / GitHub 采集 / 手填)。**只追加,一个观测=一个点,绝不插值**。
_Avoid_: 记录、log(观测是结构化数据点,不是日志文本)、埋点数据。

**指标 / Metric**:
由观测派生出的、可与目标比较的度量(带目标值、比较符、黄区带)。定义本身(名字、目标值、采集方案)也有正本,见"指标正本"。
_Avoid_: 数据、统计值;不要和"观测"混用——观测是原料,指标是产物。

**指标正本 / Metrics Source of Truth**:
一个项目的北极星 + 滞后指标 + 引领指标定义,写在仓里一份机读文件(不是 BW 私有配置),改指标和改代码一样走 PR 评审。BW 本地只缓存一份用于展示,缓存永远从这份正本同步,绝不反过来。
_Avoid_: 把 SQLite 里的缓存当成定义的正本;绕开 PR 直接在 BW 界面里"定义"一条指标。

**先对后亮 (Right-first, Collectible-second)**:
指标起草的顺序铁律——先想清楚"对不对"(北极星该衡量什么),再管"采不采得到";每条指标定义必须附一个真实采集方案(GitHub 查询 / 连接器 / BW 自记账 / 手填),绑不上数据就如实显示 Unknown,并给出点亮它最便宜的路径。北极星绝不因为"暂时采不到"就退化成一个方便统计的替代数字。
_Avoid_: 反过来先看什么好采再定指标(工程虚荣指标);指标没有采集方案也能"暂时"存在。

**信号 / Signal**:
健康灯的三态结论 + 未知:绿(隐身)/ 黄 / 红 / **Unknown**。只能由真实数据派生,**无数据 = Unknown,绝不假装绿**;数据过期自动降级。
_Avoid_: 状态、健康值、分数;不要说"信号是绿的就是好的"以外的手动设定——信号从不手设。

**未知 / Unknown**:
没有数据时的诚实态,灰色。喂再多数、不设目标也不会变绿。它是产品的诚实底线,不是"待补"占位。
_Avoid_: 默认绿、N/A、待定。

**北极星 / North Star**:
每个项目唯一的顶层目标。可由工作台把用户回答组合成草稿,但绝不编造数字替用户答题。
_Avoid_: KPI、OKR、总目标;不要有多个"北极星"。

**引领指标 / 滞后指标 (Leading / Lagging)**:
引领指标=可控、可提前统计、能被真实采集的先行量;滞后指标=结果量。控制点 4 要求引领指标可控且难造假。
_Avoid_: 先行/结果指标混用不同词;过程指标。

**手填 / Manual**:
数据来源是人手工填的,带"手填"徽记,直到接上真实来源。可用,但如实标注、不冒充自动采集。
_Avoid_: 假数据、mock(mock 是另一回事,见下)、临时值。

### 验收(证据优先于断言)

**验收 / Acceptance**:
"这件东西真的做成了吗"的那一次人为确认。两个层次别混:**一件活的验收**=人 merge PR 或点"确认完成(人裁)";**一批工作的验收**=审核方亲手跑验收流、出证据报告,人看证据终审。两层共用"验收"这个词是对的——都是"人点头才算数",没有自动通过的路。
_Avoid_: 测试通过、CI 绿(那是自证材料,不是验收本身);把 agent 自报完成当验收。

**证据链 / Evidence chain**:
支撑验收的真实原值:界面截图、数据库读回的原始数字、逐步执行日志。**断言不是防线**——写实现的一方随时能绕过自己写的断言;防线是审核方独立跑一遍、亲眼看到的东西。报告里永远同时给原值和预期,预期只是陈述,读者自行对照。
_Avoid_: 拿"断言全过"当验收结论;只给结论不给原值;补一张"看起来对"的截图。

**验收流 / 考卷 (Flow)**:
一条写死了"点哪里、看到什么、最后数据库里该是什么"的可重跑验收剧本。步骤用**语义**描述("点这张卡上的开工按钮"),不写坐标、不写选择器——界面改版不会脆断。考卷是**唯一的动作来源**:实跑严格照考卷下发,绝不由操作者临时敲命令——否则被验的就成了"操作者敲了什么",考卷沦为装饰。派工时考卷先行(先定验收标准再实现);实现方可以补步骤,不可以删步骤。
_Avoid_: 坐标录制回放;跑流时临时加动作("我再点一下看看");把考卷当事后补的文档。

**真点击 / Real click**:
验收流里的每一次点击都走和人手点击**完全相同**的那条链路(界面事件 → 命令 → 内核 → 数据库),而不是绕过界面直接给内核发命令。这个等价性就是验收流的全部意义所在:它证明的是界面本身能用,不只是内核肯收命令。
_Avoid_: 为了跑通而直接调内核命令;把"内核接受了命令"当成"界面能用"。

**常青流 / Evergreen flow**:
锁住产品铁律的那几条验收流,每批都必须全绿才允许合批。与之相对的是**每票验收流**——交付时必须绿,之后要么并进常青流、要么退役。刻意不做永久累积的回归大坝:那种东西迟早烂成假绿。
_Avoid_: 把所有历史验收流都留着当回归套件;以为流越多越安全。

**证据报告 / Evidence report**:
一批验收跑完后给人看的单份报告,三态如实:**绿**=全过 / **红**=失败(附失败现场)/ **灰**=没跑成或环境中断。灰不是红也绝不算绿——和健康信号灯的 Unknown 同一个哲学。报告是给人终审用的,不是给 agent 当上下文。
_Avoid_: 把灰算成绿或红;失败了跳过继续跑然后报绿。

**对抗验收 / Adversarial acceptance**:
实现方自证(门禁全过 + 按考卷自跑通过并自报),审核方**独立**再跑一遍、读代码、出证据报告,最后人看报告终审。关键在"独立":报告出自审核方之手,实现方碰不到,这就是"对抗"的本体。
_Avoid_: 实现方自己出验收报告;审核方只读代码不跑流;跳过人的终审直接合。

**种子库 / Fixture**:
验收流用的确定性数据库,内容全部由真实命令播种而来(不是裸 SQL 塞的假数据),跑起来不碰网关、结果可复现。**只读只拷**:每次跑流都复制一份到临时路径,原种子库和用户的真实日常库永远只被拷贝、绝不被打开写入。
_Avoid_: 手工造数据当种子;直接在种子库或真实日常库上跑流。

### 自动化(只自动建活,绝不自动完成)

**定时任务 / Scheduled task (Cron)**:
到点自动触发的作业,针对某项目或全部项目。到点做的事分两类:**真的去干**(运行一个工作流 / 运行一个技能 / 跑一段裸 Prompt)或**只留痕不动手**(建一件活、或采集一条观测)——前者会真调用执行器,后者绝不派生出"完成"。
_Avoid_: 例行(那是阶段的节奏件 Routine)、计划任务、job 混称;裸 "cron" 指代整个特性时也统一叫"定时任务";以为到点只有"运行工作流/建活"两种模式。

**Autopilot / 自动建活**:
定时任务的一种模式——到点把例行活挂上看板、指派好队友,**只建活、绝不自动跑、更不自动完成**(no-hijack)。
_Avoid_: 自动执行、自动运行、自动化完成——Autopilot 从不自己完成活。

**采集器 / Collector**:
定时任务的另一种模式——到点把 GitHub 上的真实数据(issue/PR 计数等)拉成一条条追加观测,配一个"立即采集"手动按钮。采集是**观测**不是**干活**:不运行任何工作流、不结算任何活,所以能自动触发而不违反"Done 永不自动"。
_Avoid_: 和 Autopilot 混为一谈(两者都不自动完成,但 Autopilot 产出活、采集器产出观测);把采集器等同于连接器(连接器是更通用的对外连接概念)。

**例行 / Routine**:
一个阶段自带的节奏件:该阶段的循环节奏 + 关注项 + 健康 feed。挂在阶段上,不是全局作业。
_Avoid_: 与"定时任务"混用;例行任务(会和 Cron 撞)。

### 复利(越用越强)

**蒸馏 / Distill**:
把一件做完的活提炼成一篇带完整做法的技能,永远记着它来自哪件活。
_Avoid_: 总结、归档、沉淀(沉淀指整个复利过程,别专指这一步)。

**技能 / Skill**:
一篇可复用的做法说明,真实形态是一个**文件夹**(SKILL.md 正文 + 任意数量的支撑文件——参考资料、脚本、跨平台包装……),不是一个拍平的文本字段。下次干同类活时按相关度自动带上(同阶段优先,有上限),用一次记一次(uses)。
_Avoid_: 文档、知识、SOP;不要和"五角色的打法"混——那是内置方法论,技能是蒸馏产物;把技能等同于它的 SKILL.md 正文(支撑文件也是它的一部分)。

**注入 / Inject**:
干活时自动把最相关的几条技能带进队友的上下文。
_Avoid_: 加载、引用、喂(口语,正式文档用"注入")。

**复利 / Compounding**:
"做过的活沉淀成方法、越用越懂你项目"这个整体属性:蒸馏→注入→uses+1 的增益链。
_Avoid_: 积累、飞轮;专指这条链时用"复利"。

**战绩 / 胜率 (Win rate)**:
队友的真实成败统计派生出的胜率,**永远派生、绝不手设**;无战绩时是"暂无",不是 0%。
_Avoid_: 评分、rating、可信度手调。

### 资产与来源

**资产目录 / Hub**:
可复用资产的浏览目录,分三类:工作流 / 技能 / 队友(`HubKind`)。是"库/市场"性质的浏览面,不是定时任务(别被 "Cron Hub" 旧说法误导)。
_Avoid_: 把 Hub 和 Cron/定时混为一体;中心、枢纽。

**业界来源 / HubSource**:
一份资产(工作流/技能/队友)从哪来,统一四类:**官方选型**(BW 持续挑选引入的高分精品库,配一个子标签写明具体是哪个,如 ecc/mattpocock-skills/superpowers——库只会越选越多,不为每个库单开一类)、**选型引入**(用户后期自选引入官方集之外的第三方,预留,今天无入口)、**自建**、**会话内**。
_Avoid_: 和"观测源 (SourceKind)"混用同一个"来源"词——它们是不同轴;为每个新库单开一个来源分类(库名进子标签,不进分类本身)。

**空壳 / Hollow shell**:
资产目录里只有名字和一句描述、没有正文的那种条目——早期从外部库"引用"来的目录行。它们看着像有 300 条资产,实际一条也用不了。判定要多道独立闸门(没正文、零使用、没被任何东西引用……),满足才清;有真实使用痕迹的一律保留并如实标注。
_Avoid_: 把空壳当库存规模来展示;清理时只看"有没有正文"一个条件。

**连接器 / Connector**:
项目对外部真实来源(git 仓库、claude CLI 等)的连接,能真同步的才叫连接,其余是登记条目、如实标未同步。
_Avoid_: 集成、插件、数据源。

**产物 / Artifact**:
队友运行真实产出的文件,分类登记(文档/代码/测试/脚本/配置/其他)。
_Avoid_: 输出、成果物、deliverable。

**mock / 流程演示**:
未接真实执行时的演示替身,产出**自我标注**【mock】。唯一目的是廉价验证管线本身,绝不冒充真实执行。
_Avoid_: 假装真实、demo 数据不标注;和"手填 (Manual)"混——手填是真人真数据,mock 是演示替身。

---

_命名冲突的收敛决策与代码改名台账见 [`docs/adr/0001-ubiquitous-language.md`](docs/adr/0001-ubiquitous-language.md)。_
