.DS_Store
*.log
node_modules/
.vscode/
.claude/

# Rust
/target/
**/*.rs.bk
# dx (Dioxus CLI) build output
/dist/
.dioxus/

# Generated demo databases (regenerate via `cargo run -p bw-app --example
# seed_demo -- <path>` — not source, don't commit binary DB files).
/demo-data/

# Generated self-verification reports (point-in-time output, not source).
/verification-report/
demo-workspaces/

# 2026-07-20 aihot 践行:践行过程中反复重跑产生的真实 DB + 真实本地 git 工作区
# (自带嵌套 .git),不是源码,不进本仓——真实产物见 iterations/PRACTICE-AIHOT.md
# 的读回记录。2026-07-27 补:样板间该有的东西现在都在 examples/ 里了——
# DB(examples/aihot/bw-aihot.db)、工作区文件(examples/aihot/workspace/,
# 不含嵌套 .git)、技能库(examples/skill-libraries/),clone 即可用,见
# examples/README.md。这条规则挡的只是本地反复重跑的临时产物。
/practice-aihot/

# subagent-driven-development 隔离工作区(git worktree),不是源码。
/.worktrees/

# 验收动作流(plan/15)执行产物:步骤截图/run.log/readback——现场证据,
# 不是源码。终审通过后由人挑选归档进 iterations/evidence/(那份进 git)。
e2e/reports/

# 验收报告是给人看的,不进仓:仓里的东西会变成 agent 上下文,而报告是
# 人工终审用的大件(带图 4MB+)。报告留在 e2e/reports/ 本地,或挂到 PR/Release。
iterations/evidence/**/*.html
