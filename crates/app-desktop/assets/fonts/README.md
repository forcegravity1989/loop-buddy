# Font binaries land here in P2 (Noto Serif SC / Noto Sans SC / JetBrains Mono).
# Until then the shell uses a CSS system-CJK fallback stack (see src/main.rs GLOBAL_CSS
# and src/theme.rs FONT_*). Wire @font-face + asset!() against this dir when bundling.
