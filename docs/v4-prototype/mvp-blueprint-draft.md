# V4 MVP 全貌草案(第三轮,已收口):一周的剧本、屏幕、规范与建法

> **30 秒导读**:这是 2026-08-18 从 [`intake-user-brief.md`](intake-user-brief.md)(用户口述原文)+ V1-V3 实践 + 现有代码事实,**从 0 到 1** 推出来的 V4 MVP 全貌。两轮 grilling 已互相交互完:第一轮 18 条、第二轮 22 条用户都已回,本版(第三轮)按第二轮反馈收口——**22 条全部标为已定**(其中 01/20/21 按用户意见改了内容),并补上用户点名要的 **§2.6「触发与介入全景表」**。§11 只剩「设计期待细化」的四条,不阻塞。DSH / codegraph 预研已融入(第 4 站);评审子代理已对照口述原文与两轮反馈复核过一遍,抓出的 13 处(总览名片残留「类型」、运作活数量前后不一、代号撞车等)已全部改掉。**本文自此作为 V4 设计的事实源(作数)**。第三轮页面的用户反馈也已折进来:收益改成「AI Coding 时代有无 buddy」的对比(§1 / §9)、写仓一律走 MR 并挂活(§2.6 四问:铺底 / 运作活①触发 / 运作活②承载 / 改名片)、信息分三层住(§6)。用户定的顺序:**先高保真(用 buddy 自己的仓当样例)→ 反馈 → 再详细设计**;高保真见 [hifi/](hifi/)。规范铺底模块单独成篇:[standard-module-draft.md](standard-module-draft.md);预研三篇:[research/orca.md](research/orca.md)、[research/deepseek-harness.md](research/deepseek-harness.md)、[research/codegraph.md](research/codegraph.md)。看不懂的词查 [`../../CONTEXT.md`](../../CONTEXT.md);本文新用的词(周计划、发版记录、项目空间、开工工具、运作活、规范铺底、在研版本、committer、预览、鱼塘)拍板后进词表。

**读法**:先看 §1 一句话与 §2 旅程总图,再看 §2.5(指标、周目标、活的关系)与 §2.6(每件事谁触发、什么组件、什么技能、人在哪介入——全景表),再逐站看 §3;§4-§9 是旅程推出来的模块、屏幕、数据、建法与验收;§11 是收口状态。**没有「不做清单」**——不在旅程上的东西就是不在 MVP 里,不另列。

---

## §1 · V4 MVP 是什么(一句话)与三个愿景怎么落地

**一句话**:buddy 是 AI Coding 里一个人的一张工作台,让传统开发者按 Builder 的方式,把一个小项目(约三个月)从想法管到能验收的结果。V4 = 把这句话做成**每周都能真跑一圈**的最小闭环,推给公司内更多人(codehub 项目为主,GitHub 同样能用)。

**收益怎么说(第三轮用户补充,§9 的对比按此写)**:现在**用不用 buddy 都已经是 AI Coding 时代**,不存在「人写代码 vs agent 写代码」的对比;buddy 带来的是四件别的事——①**所有项目按同一套规范运作,不会无限膨胀下去**;②**所有资产都在项目代码仓,有规范、大模型可见**;③传统项目靠很多人很多角色把控,AI Coding 下很多项目 / 实验局只有 1-2 个人——buddy 让项目**不只是在 AI Coding 下开发完,还不走偏**(这就是 Builder);④**一张工作台打通所有系统**,理论上活都是 agent 干,人不用在很多工作台之间跳来跳去——不只是各开发阶段的工具和 codehub,后面还包括内部聊天系统、问题单系统;好用的组件、好用的 agent / skill 集成进来。

| 愿景(口述原文) | MVP 里落成什么 | 用户能感到的收益 |
|---|---|---|
| ①规范:所有项目都长一样;知识管理:项目所有知识都在项目代码仓,大模型皆可见 | **规范铺底模块**([standard-module-draft.md](standard-module-draft.md)):接入项目时往仓里放同一套文档与约束(章程、agent 工作约定、指标正本、周计划、发版记录、默认件),后续都写回仓;规范本身是独立、可被人深度评审、持续优化的一个模块 | 换项目不用换脑子;agent 打开仓就懂这项目;leader / committer 纳入别人的仓就能看同一张总览、审同一批活 |
| ②从开发变成小项目的 Builder(1 Builder + Agent Loop),基于 buddy 把控交付质量 | **周循环剧本**(§3):看总览 → 更新指标与定计划 → agent 干活人守门 → 验证发版 → 回到总览 | 每周有可验证增量,不靠会议和口头汇报;完成永远人点 |
| ③一张工作台,内嵌 Open Design、Claude CLI、Cursor 等成熟组件,打通所有系统 | **会话屏**(§3 第 4 站):每张活按类别选「开工工具」,原型类走 Open Design、构建类走 Claude CLI 或 Cursor,不出工作台;开工工具按「终端类 / 本机网页内嵌类」两种接法声明,新工具加一条声明一个模块;右侧代码结构侧栏(借 Orca)。MVP 打通的系统 = 开工工具 + codehub / GitHub;内部聊天、问题单系统是同一条「适配模块」路上的下一批(MVP 后,见 §7) | 少切窗;工具由 buddy 缝合,不自造;以后缝新系统(聊天、问题单、DSH 一类)不用改架构 |

**继承不再讨论的**(V1-V3 已用真实数据证明、且 V4 旅程每站都用到):四条铁律(完成永远人点且同一件活绝不记两次;健康只从真实数据推导、没数据就是灰;交棒/观测这类记录只追加;做完的活可蒸馏成技能、胜率由真战绩算)、内核 crate(状态机、信号密封、只追加表、交互式执行器、连接器、指标正本管道)。

**反命题,按第一轮反馈精确化**:buddy 里**不建成员、权限、群聊、收件箱**;不是通用看板、不是审批系统、不是云服务。**但多人协作是允许的,靠代码仓发生**:V3 实践里已经遇到 builder 没有合代码权限——解法不是在 buddy 里加成员系统,而是 **committer 把同一个仓纳入自己的 buddy**(V2 已支持同一仓多台 buddy),在自己的总览「待人处理」里看到评审中的活(代码 MR、文档、原型都算),审完点「合入」。跨人的通知靠内部聊天工具,留给内部增量包,不进 MVP 主线。详见第 5 站(待拍-13 已定)。

---

## §2 · 旅程总图:一个项目的第 0 站 + 每周六站

```
第0站 接入项目 + 规范铺底(一次)
   │
   ▼
┌─ 第1站 周一看总览:这项目是什么、health 吗  ◄───────────────┐
│     │                                                       │
│  第2站 运作活①「更新指标 + 制定本周计划」(人触发,一次 agent 会话)
│     │   → 周目标 → 业务活挂本周 / 在研版本 / 预期推动的指标      │
│     │                                                       │
│  第4站 执行:每张活按类别选开工工具(Open Design / Claude CLI / Cursor),agent 干、人守门
│     │                                                       │
│  第5站 验证闭环:评审(builder 或 committer)→ 合入 → 人点完成 → 记发版
│     │                                                       │
└─ 第6站 定时:运作活②「资产盘点与代码微重构」自动建并自动开工;下周一回到总览 ──┘
```

(第 2、3 站在第一轮是分开的「更新指标」「定计划」,按用户反馈合成一次会话,编号保留:第 2 站=更新指标,第 3 站=定计划,同一张运作活里先后做。)

**角色**:主角是 Builder。**leader** 不做专门界面——他把大家的仓纳入自己的工作台,项目墙 + 每个项目的总览就是 leader 视图。**committer**(有合代码权限的人)同样靠纳入同一仓审核与合入(第 5 站)。

---

## §2.5 · 指标、周目标、活:一张关系图(第一轮 03/05/14 的根)

第一轮里「总览不直观」「指标定不出来」「运作活算不算计划」都卡在同一件事:**指标、周目标、活三者的关系没说清**。这一节说清,并给一个例子。

**四层,从上到下:**

| 层 | 是什么 | 谁定 / 何时 | 允许为空吗 |
|---|---|---|---|
| **北极星**(1 个)| 「三个月长成什么样」的可度量说法。来自第 0 站意图卡 | 人,接入时;可改 | **不允许**(接入必填)|
| **滞后指标**(≤3)| 结果量,月/周更新(用户数、成功率、收入…)| 人 + agent,在运作活①里逐周补 | 允许(早期没有,灰)|
| **引领指标**(≤3)| 本周可控的先行量,**直接由活推动**(本周合入活数、原型评审通过数、发版次数、覆盖率…)| 人 + agent,在运作活①里逐周补 | 允许(早期没有,灰)|
| **周目标**(1 句)+ **业务活**(N 张)| 周目标 = 这周要把哪几个指标往哪推;每张业务活可标「预期推动的指标」(0..n)| 人在运作活①里与 agent 一起定,人确认才建 | 周目标必填,活可为 0 |

**关系**:活推动引领指标 → 引领指标带动滞后指标 → 滞后指标逼近北极星。总览就按这条链画:每个指标卡下面列「本周哪些活在推它」,活完成后指标的下一次观测就是它的效果。**这就是让引领/滞后指标「直观」的办法——不靠解释,靠把活挂上去。**

**早期定不出指标怎么办**:允许。接入只要北极星;运作活①每周都会尝试「补一个引领、补一个滞后」;规范里写进 DoD——「原型阶段交棒前,至少 1 个引领 + 1 个滞后有真实观测」。在此之前总览指标区就是灰,如实。

**例子(新建项目「建木」,假设值,只为说明关系)**:
- 北极星:三个月内 3 个真实团队用建木跑通一条流水线。
- 滞后:接入团队数(目标 3)、流水线一次跑通率(目标 80%)。
- 引领:本周合入的活数(目标 ≥5)、原型评审通过数(目标 ≥1)。
- 第 3 周周目标:「把 A 团队的第一条流水线跑通」→ 业务活 4 张,其中「流水线 DSL 解析器」标「推动:一次跑通率」,「A 团队接入向导原型」标「推动:原型评审通过数」。
- 运作活①②不占周目标、不标指标,在计划屏单独一栏「本周运作」。

**活的边界**(第一轮 14 问的,第三轮收紧):**活 = 任何要留痕的事项:需要一次 agent 会话的,或要往代码仓写东西的**——代码、文档、原型、指标、盘点、铺底、改名片都算,不限于提代码;只动本机库的纯人工动作(点合入、点完成、切在研版本)不是活。会话工具三选一:Claude CLI / Cursor / Open Design。**业务活**进周计划、可标指标;**运作活**是 buddy 的标准活,MVP 里**两张周期性 + 一张一次性**:①「更新指标 + 制定本周计划」(周一,人触发,含上周复盘)②「资产盘点与代码微重构」(定时,自动建并自动开工,停在评审中)③「规范铺底」(接入时自动建一次,buddy 写规范件走 MR;成熟仓多一步 agent 合并调整,见第 0 站)。

- ✅**待拍-05 已定**:周期性运作活两张,①人触发、②定时自动开工(另有一次性③「规范铺底」,接入时出现)。用户追问「自动开工走什么机制、活是不是只有 issue 一种承载、人怎么介入」——答案在 §2.6。
- ✅**待拍-14 已定**:活的边界按上面定义。用户纠正一处:**周目标不是人「写」出来的**——人只在首尾(触发、确认),周目标是在 Claude CLI 会话里由技能引导、结合代码与上周记录交流出来的,再落成活并带属性,UI 只呈现。上文与第 2/3 站已照此改。
- ✅**待拍-03 已定**:总览按 §2.5 的链画(指标卡挂活)+ 项目名片(这项目干什么、现在什么样);高保真两版后定为一列横块、health 在第一块右侧、待办移到「通知」。

---

## §2.6 · 触发与介入全景表(用户第二轮点名要的「清晰全景图」)

**三条不变的规矩**(第三轮按用户四问收紧):①**凡是要留痕的事,承载都只有一种——活(issue)**:需要一次 agent 会话的是活;**要往代码仓里写东西的也是活**(写仓一律走分支 + MR,MR 必挂在一张活上,这样才能出现在「待人处理」、才能让 committer 合入)——所以铺底、编辑项目名片这类「buddy 替人写仓」的动作也建活(轻量,无 agent 会话);只动本机库、不碰仓的纯人工动作(合入、点完成、切在研版本)不建活;唯一例外:新建的空仓,首提交直推(没有可以开 MR 的默认分支)。②触发方式 MVP 只有两种——**人触发**、**定时触发**;「事件触发」(远端新 issue → 自动建活、CI 红 → 自动建活)是 MVP 之后;③**自动触发的活最远只到「评审中」,人永远在首尾**——首 = 触发或确认,尾 = 评审、合入、点完成。

**用户四问的答案**:(1)**站 0 铺底也走 issue + MR**——接入的人未必有合入权限,所以铺底 = 一次性运作活③「规范铺底」:buddy 建分支写规范件 → 提 MR → 有权限的人(自己或 committer)在工作台合入;成熟仓已有手写 README / CLAUDE.md / AGENTS.md 时,这张活多一步 agent「合并调整」再提 MR。(2)**运作活①不绑周一**——判据是「当前周没有 `docs/plan/` 文件」:铺底合入后总览立刻提示「本周还没有计划 → 开始本周」;任何时候进总览看到本周无计划都能点。**触发入口在总览,逻辑在活里**:点「开始本周」= buddy 建这张运作活①并跳到会话屏 ▶开工,和其它活一样;计划屏 / 会话屏里也能直接建它、开工它,总览的按钮只是最顺手的入口。(3)**运作活②也是一张活**:定时到点 buddy 自动建 issue(来源 = 自动建)并自动 ▶开工(走同一个交互式执行器),它出现在会话屏活列表和总览「本周运作」栏,人随时能点进去看终端。(4)**编辑项目信息也建活走 MR**:名片「编辑 → 保存」= buddy 建轻量活 + 分支提交 PROJECT.md / `.bw/project.toml` + MR → 待人处理 → 合入 → 完成;有合入权限的人两下点完。

**agent 会话里的「规范手册」怎么渐进加载**(每张活开工时一样,五层,不用字母代号以免与已登记的度量派生链 L0-L6 撞车):第 0 层 buddy 系统提示词(固定、短,V2 已有)→ 第 1 层 仓内 `AGENTS.md`(agent 进仓自己读;里面写着「先读什么、活怎么做、指标怎么碰」)→ 第 2 层 本活的技能 `SKILL.md`(按活注入)→ 第 3 层 规范件按需(AGENTS.md 指引下,agent 需要时才 Read 具体规范:指标规矩、活的约定、目录约定)→ 第 4 层 项目知识(PROJECT.md、本周 `docs/plan/`、`docs/metrics.md`、codegraph 索引)。**不一次性灌全部规范**,靠 AGENTS.md 当目录。

| 旅程里的事 | 触发 | 承载 | 组件 | 技能 / 队友 | 注入什么(在五层之上)| 产出 | 人在哪介入 | 怎么通知 |
|---|---|---|---|---|---|---|---|---|
| 接入项目 + 规范铺底 | 人(项目墙两卡)→ buddy **自动建**一次性运作活③「规范铺底」| 活(轻量:buddy 自己写模板,**不起 agent**)| 工作台 | — | — | 仓:分支上的规范骨架 + MR;库:project 行 | 首(填两卡)· 尾(合入、点完成)| 总览待人处理;合入后提示「开始本周」|
| 成熟项目已有 README / CLAUDE.md / AGENTS.md | 铺底时自动检测 → 同一张运作活③多一步 agent「合并调整」| 活 | Claude CLI | 「规范合并调整」技能 | 已有文件 + 规范里的「agent 工作约定」「目录与知识结构」两件 | MR:把 buddy 约定合进已有文件(与铺底件同一个 MR)| 尾(评审、合入、点完成)| 总览待人处理 |
| 周一看总览 | 人 | 纯 UI(只读)| 工作台 | — | — | — | 全程 | — |
| 运作活①更新指标 + 制定本周计划 | 人(总览「开始本周」,只要当前周没有计划就能点;计划 / 会话屏也能建)| 活 | Claude CLI(嵌入终端)| 「更新指标与周计划」技能 | 上周 `docs/plan/`、发版记录、`.bw/metrics.toml`、规范里的「指标与数据」「活的约定」「运作节律」三件 | MR:`.bw/*` 变更 + 本周 `docs/plan/`;活草稿(人确认后 buddy 真建活 + 远端 issue)| 首(触发)· 中(终端里对话、改草稿)· 尾(确认建活、评审 MR、点完成)| 总览「本周运作」栏 |
| 业务活开工 | 人(会话屏 ▶开工)| 活 | 按类别:Open Design / Claude CLI / Cursor | 按类别默认(原型师 / 构建师;superpowers、grillme)| 活说明、预期推动的指标、类别对应规范件 | 代码 / 文档 / 原型 + MR;运行记录、战绩 | 中(终端里对话、停)· 尾(评审、合入、点完成)| 总览待人处理 |
| 运作活②资产盘点与代码微重构 | **定时**(默认周五晚)自动建 issue(来源 = 自动建)+ 自动 ▶开工 | 活(会话屏列表里能看到)| Claude CLI | 「资产盘点与微重构」技能 | 规范里的「目录与知识结构」「默认件与鱼塘」两件、`docs/plan/`、codegraph 索引 | 盘点报告写进本周 `docs/plan/` 尾段 + 微重构 MR | 尾(周一评审、合入、点完成)| 总览待人处理 + 本周运作栏 |
| 评审 / 合入 / 点完成 | 人(builder 或 committer)| 纯 UI 动作(合入按钮、完成按钮)| 工作台(+ 会话屏看 diff / 文档 / 原型;+ **预览**未合入效果,见待拍-21)| — | — | 库:`settled_at`、合入人 | 全程 | 各自 buddy 的待人处理;跨人通知走内部聊天工具增量包 |
| 发版本 | 人(计划屏)| 纯 UI(agent 起草说明可选一次会话)| 工作台(+ Claude CLI 可选)| — | 本周完成的活 | 仓 `docs/releases.md` + 可选 tag;库 release 行 | 首尾 | 下周总览 |
| 蒸馏 | 人(会话屏)| 活的附属动作(一次会话)| Claude CLI | 「蒸馏」技能 | 该活的会话与产物 | 仓 `.claude/skills/` 新技能(记来源活)| 首(触发)· 尾(看一眼)| 项目空间可见 |
| 编辑项目信息 | 人(总览名片「编辑」)| 轻量活(无 agent 会话)+ MR | 工作台 | — | — | 仓 PROJECT.md + `.bw/project.toml`(分支 + MR)| 首(改)· 尾(合入、点完成)| 待人处理 |
| (MVP 后)事件触发 | 远端新 issue / CI 红 / 指标过期 | 自动建活、不自动开工 | — | — | — | — | 尾 | 待人处理 |

**为什么运作活①是人触发而不是定时**:它的产出(周目标、本周活)必须有人在场确认才有意义;定时建好一张等人开工也可以,但「开始本周」这一下就是人对这周负责的仪式,先保留。**为什么运作活②可以自动开工**:它只碰 `docs/`、`.bw/`、小范围重构,产出全在 MR 里等人审。

---

## §3 · 逐站:看到什么 / 做什么 / 留下什么 / 沿用什么 / 不带什么

「留下什么」分**仓**(进项目代码仓,LLM、leader、committer 可见)和**库**(buddy 本机 SQLite,只做记账与推导)。

### 第 0 站 · 接入项目 + 规范铺底(一次)

- **看到什么**:项目墙(所有已纳入项目,每卡一盏灯 + 一句话)→「接入项目」两张卡:**项目地址**(新建 / 已有:codehub 或 GitHub,可搜索或粘贴地址;**不问分支、不问本机目录**——分支用远端默认分支,目录落在设置里配好的工作区根目录下,「关联已有 clone」不进 MVP)→ **基础信息**(四个字段):项目名称(默认取仓名,可改)、你想做什么(一句话)、最像的对标(产品或仓)、三个月长成什么样(= 北极星,可度量)。**不问「类型」**——「新建还是成熟」的区别只是仓里有没有代码,buddy 自己探测,不让用户选一个后面用不上的属性(待拍-01 按用户意见去掉)。
- **做什么**:人填两卡点完成;buddy 自动建一张一次性运作活③「规范铺底」:建分支、往仓里写**全套核心件**([standard-module-draft.md](standard-module-draft.md))、提一个 MR(标题写明规范版本),活停在评审中 → 有合入权限的人(自己或 committer)在工作台点合入 → 点完成;然后进总览(第一次多半是灰,如实,并提示「本周还没有计划 → 开始本周」)。**为什么走 MR 而不是直接提交**:接入的人未必有合入权限(第三轮用户指出);唯一例外是新建的空仓,首提交直推。若仓里已有 `.bw/project.toml`(别人先接过),读正本预填、不重铺(V2 已有)。若仓里已有人手写的 README / CLAUDE.md / AGENTS.md(成熟项目),**不跳过、不覆盖**:同一张运作活③多一步——agent 把 buddy 约定合进已有文件后再提 MR(待拍-20 按用户意见:纳入管理就要相信管理;规范绝大多数内容在独立目录,正常不冲突;运作活②每周还会持续调节)。
- **留下什么**:仓:规范骨架(PROJECT.md 章程、AGENTS.md/CLAUDE.md、`.bw/*`、`docs/plan/`、`docs/releases.md`、默认技能等);库:project 行 + 所用规范版本 + 在研版本(新建默认 `v0.1`)。
- **沿用现有**:两卡创建流(V3 同事用过,已修列仓 999/可搜索/`claude.cmd`/探活三区);codehub 与 GitHub 连接器;`.bw/project.toml` 正本与后来者接入;PROJECT.md 章程写入机制;北极星/项目身份更新命令。
- **不带**:接入时自动生成「竞品分析→找指标→绑数据」三张活(找指标+绑数据并入运作活①;对标改由意图卡直接问)。
- ✅**待拍-01 已定(按用户改)**:意图卡四字段,去掉「类型」;有没有代码由 buddy 探测。
- ✅**待拍-02 已定**:规范铺底整套进仓、规范独立成模块([standard-module-draft.md](standard-module-draft.md))。
- ✅**待拍-20 已定(按用户改)**:成熟项目也铺全套核心件;铺底本身走一次性运作活③ + MR;已有文件在同一张活里多一步「合并调整」,不跳过。

### 第 1 站 · 周一看总览:这项目是什么、health 吗

- **看到什么**:一屏「总览」,**一列横块、只呈现信息、不编号**(高保真两版反馈后定):①**项目信息**(名称、想做什么、对标、三个月长成什么样——全部来自 PROJECT.md;右侧 **health 面板**:大灯绿/黄/红/灰 + 状态词 平稳 / 需要关注 / 阻塞 / 无数据 + 三条理由全部可见,一眼知道项目什么情况);②**北极星**(1 张);③**滞后**(N 张横排);④**引领**(N 张横排)——每个指标卡:目标、现值、保鲜期、趋势、**本周哪些活在推它**(§2.5);⑤**项目指标 · 代码仓级**(仓统计现状数,不点灯);⑥**本周计划进度**(目标、进度、运作活①②③状态);⑦**在研版本与发版记录**。**待人处理不在总览**——它在左栏「通知」入口(项目墙卡片上有 ⚑ 数);本周运作并入计划屏 / 会话屏。这些信息全部自动获取(项目信息 ← PROJECT.md,指标 ← `.bw/metrics.toml` + 观测,挂活 ← 活的推动指标属性,进度 ← 活状态与远端 MR,版本 ← 发版记录,仓统计 ← git / codegraph,灯是推导),没数据就灰。
- **做什么**:只看;要处理的事从左栏「通知」进(合入 / 点完成 / 去补数据);点「开始本周」触发运作活①(第 2/3 站)。
- **留下什么**:总览本身不写东西。灯与理由**读时现算**(库里不存灯)。
- **health 推导规则(推荐,只用真实数据)**:三个输入——(a)本周有周目标且业务活有进展;(b)业务指标在保鲜期内有真实观测;(c)上周有交付(合入或发版)。**绿** = 三项都真;**黄** = 任一项缺;**红** = 连续两周无进展,或任一业务指标为红;**灰** = 没数据。手填的观测带「手填」徽记。
- **沿用现有**:今天进度面板的指标卡与灰卡诚实、信号密封推导、周信号快照概念(`weekly_review` 表,注意减负线删了它,V4 按新模型重建)。
- **不带**:五阶段轴与阶段舱、进度趋势(手工维护)、五阶段 × 健康灯矩阵。
- ✅**待拍-04 已定**:不做多版本线;一个「在研版本」(计划屏顶部可切,新建默认 `v0.1`),总览只看它;旧版本 = 发版记录里的行,维护活打旧版本标签 + 类别标签「运维/运营推广」。

### 第 2/3 站 · 运作活①「更新指标 + 制定本周计划」(一次会话,人触发)

- **看到什么**:周一在总览点「开始本周」→「计划」屏顶部出现运作活①(状态待做,开工工具 Claude CLI,技能「更新指标与周计划」),右侧是嵌入终端。计划屏三栏:左=周列表(本周置顶)+ 在研版本;中=本周:周目标(可编辑)+ 业务活清单 + 「本周运作」栏;右=选中活详情。
- **做什么**:人点 ▶开工 → agent 在一次会话里按顺序:(1)读上周 `docs/plan/` 与发版记录做**复盘**;(2)**更新指标**:读 `.bw/metrics.toml`,补缺的引领/滞后、给绑不上的找最便宜数据源、改 `connectors.toml`、跑一次采集;(3)**引导出本周**:技能结合指标、代码现状与上周记录,与人在终端里交流出周目标一句 + 业务活清单(每张带:标题、说明、类别标签、预期推动的指标、建议开工工具与技能)——**人不「写」周目标,人在首尾:触发与确认**;(4)人在同一屏改、确认 → buddy 真建活(同时建到 codehub / GitHub issue),`.bw/*` 与 `docs/plan/YYYY-Www.md` 的变更**走 MR**(待拍-21 按用户改),活推评审中 → 人评审(可先**预览**未合入效果,见下)→ 合入 → 点完成。
- **预览未合入的效果**(待拍-21 用户提出):运作活的 MR 合入前,总览 / 计划屏可切到「预览:活 X 的 worktree」——buddy 从该活的 worktree 读 `.bw/metrics.toml`、`docs/plan/` 渲染,顶部横幅「预览 · 未合入」;人看着效果回到终端指挥 agent 调整,满意再合。每活一 worktree(v1 已有)让这件事只是「换个目录读文件」。观测数据不预览(它在库里、只追加)。
- **留下什么**:仓:`.bw/metrics.toml`、`connectors.toml`、采集脚本变更;`docs/plan/YYYY-Www.md`(周目标、活清单、上周完成情况);库:观测(只追加)、issue 行(`week_of`、`version`、`tool`、`kind`、`origin`、`metric_ids`)、运作活记账。
- **沿用现有**:指标正本管道、脚本连接器与采集、`north-star-discovery` + `metrics-binding` 两技能(合成一个)、Issue 创建与远端同步、按活选技能(V2-①)。
- **不带**:找指标 / 绑数据两张分开的活;五种采集方式(收成 脚本 / 手填 两种)。(**六列看板保留**——高保真第二版反馈:V3 那种按状态分竖条的看板是我们喜欢的形式;计划屏 = 周视角 + 六列看板一个界面,不拖拽。第一轮「本周清单代替看板」的说法作废。)
- **项目信息更新**:总览名片区「编辑 → 保存」→ buddy 建轻量活(无 agent 会话)、分支提交 PROJECT.md + `.bw/project.toml`、提 MR → 待人处理 → 合入 → 完成(第三轮:写仓一律走 MR 并挂活)。
- ✅**待拍-06 已定**:周计划正本进仓 `docs/plan/YYYY-Www.md`。
- ✅**待拍-07 已定**:活保留「类别标签」(原型/构建/优化/运营推广/运维——老 buddy 的方法论),它决定默认开工工具与默认技能。
- ✅**待拍-08 已定**:agent 拆活只出草稿、人确认才建;和所有活一样走**同一范式**——buddy 系统提示词 + 技能 + 与人结伴,工具因类别而异(构建类 Claude CLI / Cursor,原型类 Open Design)。
- (第一轮「更新指标直接提交还是走 MR」已按用户第二轮意见定为**走 MR**,见上文与待拍-21。)

### 第 4 站 · 执行:每张活按类别选开工工具,agent 干、人守门

- **看到什么**:「会话」屏三栏,**按 Orca 做**(高保真第一版反馈:要让人在这里就能干好活,不用自己开 PowerShell):**左列 = 会话按目录 / worktree 分组**——仓根(main)常驻一个纯 shell 会话;每个活的 worktree(真实约定:主工作区旁 `<仓名>-issue-<n>`,分支 `bw/issue-<n>`)一个会话,行上有活标题、分支、agent 状态(运行中 / 等你输入 / 空闲 / 已推评审)、最近时间;同一 worktree 正常只有一个会话,「+ 新会话」少用。**中栏 = 标签页**:终端(嵌入,可续接,**内容可复制**)/ 从右栏点开的文件(只读代码视图)/ 改动文件的 diff /(原型类活)内嵌 Open Design。**右栏 = 代码结构侧栏**:文件树**可点开、点文件即在中栏打开**、改动文件(点开 diff)、git 状态、MR 卡(号、状态、检查、合入)。顶部一行:活标题、状态、▶开工 / ■停止 / 推到评审 / 蒸馏。
- **做什么**:人点 ▶开工 → buddy 按活的开工工具分发:Claude CLI / Cursor 活注入 buddy 系统提示词 + 该活选的技能(构建类默认 superpowers / grillme,先把需求打磨清楚再写代码);Open Design 活打开设计器让人与 agent 一起出原型;agent 干完最远推到「评审中」;人在同一屏看、追问、验收。
- **留下什么**:库:每次运行的成败与耗时、产物登记、队友战绩、MR 号;仓:活的产物本身(代码、设计文件、文档)随 agent 的提交进仓。
- **开工工具 × 类别 映射(默认,规范里可改)**:

  | 类别标签 | 默认开工工具 | 默认技能 / 队友 |
  |---|---|---|
  | 原型 | Open Design | 原型师 + 原型设计技能 |
  | 构建 | Claude CLI(装了 Cursor 的人可选 Cursor)| 构建师 + superpowers / grillme |
  | 优化 / 运维 | Claude CLI 或 Cursor | 构建师 + 相应技能 |
  | 运营推广 | Claude CLI | 构建师;无默认技能,从鱼塘里挑(运营推广类活初期少,不预置)|
  | 运作活 | Claude CLI | buddy 自建:更新指标与周计划 / 资产盘点 |

- **沿用现有**:交互式执行器 + 嵌入终端(Windows 主线可跑;macOS 后端在减负线);Open Design 本机探活与嵌入(V3);buddy 系统提示词 + 按活选技能(V2-①);hook 回收(活推评审中);蒸馏;Cursor 执行器设计稿(`docs/v3-prototype/cursor-agent-executor.md`,未落地,V4 落)。
- **不带**:旧聊天式工作流引擎与旧聊天面板(减负线 `cut-legacy-engine` 分支已删)、「工作流」面板、阶段视角的定时任务面板。**口述愿景③里的「聊天组件」不是被砍了**:与 agent 的对话体验由会话屏里的嵌入终端(Claude CLI / Cursor 会话)承载,删掉的是那套结构化的旧聊天面板。
- ✅**待拍-09 已定**:开工工具三个——Open Design、Claude CLI、**Cursor**;活按类别选工具、录默认技能/队友。
- ✅**待拍-10 已定**:**业界优先、自建最少、其余进鱼塘**:默认件只带 superpowers(构建流程)、grillme(需求打磨)、buddy 自建两个运作技能(更新指标与周计划、资产盘点与微重构)、队友两个(原型师、构建师);其他几十个技能/队友进「鱼塘」(buddy 仓里的未严选目录,**不做界面**;要用就在项目配置里「导入」);全部可拔插。(名单按使用证据持续增删)。
- ✅**待拍-11 已定(借模式)**(预研见 [research/orca.md](research/orca.md)):`stablyai/orca` 是 Electron + React + node-pty + xterm 6,MIT,桌面应用 + `orca` 命令行;**它已没有可单独复用的守护进程**(本机 PTY 就跑在 Electron 主进程里),所以「复用其守护进程」这条路不存在;整体嵌进 buddy 与我们「单个原生二进制」的形态冲突。**推荐:借模式,不整体嵌**,具体借四样:①复制——xterm 6 原生选区 API + 可选「选中即复制」(我们的终端只需升 xterm 版本并接剪贴板);②agent 状态——不靠猜屏幕,靠 Claude Code 的 hooks / statusLine 往本机回环 HTTP 端口汇报(buddy 已有 hook 回收,对齐即可);③右侧栏——Orca 的右侧栏就是「文件树 + git 状态 / diff / PR / 检查」,**没有符号级大纲**,与待拍-22 首版内容一致;④每活一个 worktree(buddy v1 已有 worktree 隔离,保持)。Orca 与 buddy 的核心想法(周计划、推导 health、记账、蒸馏)零重叠。**用户附加要求**:借鉴的东西必须做好模块管控——每个借鉴/缝合的外部能力是一个独立模块,以后持续借鉴也不会散弹修改(老 buddy 走不下去的原因之一就是 lib.rs 动辄一万多行);落到 §7 建法。

- **开工工具怎么接、DSH 怎么看**(预研见 [research/deepseek-harness.md](research/deepseek-harness.md)):`deepseek-ai/deepseek-harness`(dsh)是 MIT 的 TS/Node 插件框架,`npx @deepseek-ai/dsh web` 起本机网页(127.0.0.1:3080),自带完整 agent 循环、接多家模型;插件是同进程 TS 模块、UI 靠模块联邦拼进宿主页,**没有 iframe 隔离**,且处于 developer preview、破坏性变更频繁。核实了一件事:**不是 Open Design 是 DSH 的插件,而是 Open Design 把 dsh 当作它能拉起的 coding agent 之一**(与 Claude Code / Cursor 同级),两者源码零引用——BW 现有「本机探活 + WebView 内嵌 Open Design」的接法不受影响。DSH 没有项目、周计划、健康、活、worktree、技能市场——BW 的地盘完全空着。**结论:不整体嵌、不做成 DSH 插件,只借三条接口设计判断**(声明式工具清单、能力显式依赖、安装期权限对人可见),用在 BW 自己的「开工工具」注册上。落地:开工工具在规范「活的约定」件的 `issue-policy.toml` 里按**两类接法**声明——**终端类**(PTY:Claude CLI、Cursor)与**本机网页内嵌类**(探活拿 URL 后嵌 WebView:Open Design;将来 dsh 一类的 agent 网页也走这条,先进鱼塘不进 MVP)——新工具只需加一条声明 + 一个适配模块(§7 模块管控)。

- **codegraph(代码图)怎么用**(预研见 [research/codegraph.md](research/codegraph.md)):本仓已在 CI 里用它(校验 + 把索引快照推回 main),但 agent 会话只靠 AGENTS.md 里一句「可以用 codegraph 命令」——没有强制、没有校验。V4 的用法:①规范「agent 工作约定」件的 AGENTS.md 固定一段「代码图怎么用」(`codegraph explore / node / callers / impact`),这是 §2.6 渐进加载的第 4 层;②第 0 站铺底时若本机有 codegraph 就跑一次索引(没有就跳过、如实提示);③右侧代码结构侧栏首版仍是文件树 + 改动文件 + 分支(待拍-22),将来加「符号级大纲」只是一次 `codegraph node -f <文件> --symbols-only` 子进程调用,留在侧栏模块里增量;④运作活②用 `codegraph files -j` 找该拆的大文件;**不做**「零调用者就当死码删」——它对 `dyn Trait` 分发看不见,会误判(预研实测)。MCP 注册与否留到设计期定。

### 第 5 站 · 验证闭环、发版本(builder 或 committer)

- **看到什么**:活在「评审中」(远端 MR 开着,或文档/原型已提交)出现在总览「待人处理」和会话屏顶部;评审人(builder 自己,或纳入同一仓的 committer)在会话屏 / 项目空间里看代码 diff、文档、原型 → 点「合入」(工作台里,V2 已有)→ 点「完成」。周末在「计划」屏点「发版本」:选本周完成的活 → 填版本号 → 生成发版记录(agent 起草说明,人确认)。
- **做什么**:人验收、合入、点完成;人发版本。
- **留下什么**:仓:项目自己的 `docs/releases.md` 追加一行(版本、日期、包含的活/MR)+ 可选 git tag;库:release 表一行、活的 `settled_at`、合入人。下周一总览第⑤块显示它。
- **沿用现有**:「评审中」由真实开着的 MR 推导、合入按钮与忙态、完成只能从评审中进、同一件活绝不重复记账、同一仓多台 buddy(V2-②)。
- **不带**:产物面板与版本面板作为独立屏(产物随活登记、版本进发版记录,两者在「项目空间」里能翻到)。
- ✅**待拍-12 已定**:发版本 = 仓记录 + 可选 tag;打包流水线看后面实践。
- ✅**待拍-13 已定**:**committer 流程进 MVP**:committer 纳入同一仓 → 他的总览「待人处理」列出该仓评审中的活(代码 MR / 文档 / 原型)→ 打开看 → 点合入 → 点完成。buddy 里不存「角色」,谁有合入权限谁点;通知留内部聊天工具增量包。合入后「完成」由 committer 顺手点。

### 第 6 站 · 定时:运作活②「资产盘点与代码微重构」,回到总览

- **看到什么**:每周固定时间(默认周五晚,可改)buddy 自动建运作活②并自动开工(前提 buddy 在运行;错过则下次启动补建);周一总览「本周运作」栏能看到它停在评审中等人看。
- **做什么**:agent 盘点仓内产物(新增文档/设计/技能是否登记、`docs/plan/`、`docs/releases.md` 是否齐)、做小范围代码微重构(死码、格式、命名),提 MR;人周一评审、合入、点完成。
- **留下什么**:库:定时任务触发记录;活带「自动建」来源;仓:盘点报告写进本周 `docs/plan/` 尾段 + 微重构 MR。
- **沿用现有**:真实调度器、「建活」模式、交互式执行器(自动开工走它,**不是**已删的旧引擎)。
- (自动开工不越线:铁律是「完成永远人点」,自动开工最远到评审中;机制见 §2.6。)

---

## §4 · 规范铺底模块 → 单独成篇

内容、大类、每类怎么做、正本住哪、怎么升级、怎么被人评审与持续优化——见 [standard-module-draft.md](standard-module-draft.md)。本文只留结论:**规范 = 仓内 `standard/` 独立目录,有版本、有 CHANGELOG、每类一份模板 + 一份说明;铺底 = 按 manifest 把核心件写进项目仓的分支、提 MR、人合入(一次性运作活③);升级 = 算差异、人确认、同样走 MR;校验 = buddy 对账缺什么、过期什么并在项目空间提示。**

- ✅**待拍-15 已定**:AGENTS.md 正本 + CLAUDE.md 一行导入。
- ✅**待拍-16 已定**:八大类与核心/扩展先按 standard-module-draft.md;后续按业界与内部实践持续修。

---

## §5 · 信息架构:侧边栏与屏幕

**顶层(不进项目时)**:项目墙(所有纳入的项目,**沿用 V3 卡片**——标签行 + 灯 + 名 + 一句话 + meta + 进度条,外加 V3 的「本机环境」条与「健康概览」条;leader 视图就是它)+ 设置(齿轮,左栏底部:**工作区根目录**——默认 `D:\bw-workspaces`(macOS `~/bw-workspaces`),一次配好,接入旅程里用户不再关心项目落哪;claude / Cursor / Open Design 路径;codehub / GitHub 登录;**没有预算项**——Claude CLI 内嵌,预算是它自己的事)。项目墙右侧有一个默认收起的**「指南」抽屉**(四章:环境准备 / 接入项目 / 一周一圈 / 常见问题,正文随包出、V4 重写),「本机环境 · 测一下」的失败项带「怎么处理 →」跳对应章;「问题上报」先留图标位。**没有「资产库」**(高保真第一版反馈:技能 / 队友就是配置,要换就导入;鱼塘不做界面,它只是 buddy 仓里的未严选目录)。

**项目内五个入口**(替代今天的阶段轴 + 六面板 + 十个图标):

| 入口 | 一屏是什么 | 对应站 |
|---|---|---|
| **总览** | **一列横块、只呈现信息、不编号**(高保真第二版反馈):项目信息(右侧 health 面板:大灯 + 状态词 + 三条理由全部可见,一眼知道项目什么情况)→ 北极星(1 张)→ 滞后(N 张横排)→ 引领(N 张横排)→ 项目指标 · 代码仓级(V3 那块,不点灯)→ 本周计划进度(目标 · 进度 · 运作活①②③ chip)→ 在研版本与发版记录 | 1、6 |
| **计划**(= 周视角 + **V3 六列看板**,一个界面)| 左栏周列表(本周置顶 · 历史周 · 全部)+ 在研版本;中栏 = V3 六列看板(待办池 → 待办 → 进行中 → 评审中 → 已完成 → 阻塞,卡片结构与按钮语义照 V3:▶开工 / ■停止 / 续聊 / ⬇合入 / ✓点完成 / ⛔阻塞),选中周时看板上方一条「周头」(周目标 · 进度 · 运作活①②③ · 新建活 · 发版本 · 预览开关),选「全部」时换成筛选条;右侧滑出活详情 | 2/3、5 |
| **通知**(左栏入口,未读徽章)| 上段待处理:评审中待合入、已合入待点完成、指标过期、带险交棒、名片修改待合入(动作在行内);下段事件流:agent 等你输入、定时活已建并开工、MR 检查、蒸馏完成、采集完成;项目墙卡片上显示 ⚑ 数。**不是铃铛**(第二版反馈:放左边栏) | 5、6 |
| **会话** | **按 Orca 做**:左列会话按目录 / worktree 分组(仓根常驻一个 shell 会话,不用自己开 PowerShell;每个活的 worktree 一个会话,标 agent 状态);中栏标签页(终端 / 打开的文件只读视图 / diff / 内嵌 Open Design);右栏文件树(可点开、点文件即打开)+ 改动文件(点开 diff)+ git 状态 + MR 卡 | 4 |
| **配置** | 旧 Hub 的精简版,**单页四段**:①开工工具映射最上(类别 → 工具 → agent → skill,下拉里含导入的资产)②skill 表 ③agent 表 ④连接器 + 定时;资产来源**只两种:buddy 内置 / 项目导入**;每段「导入」(本机目录 / git 仓 / 从另一个项目)。**「队友」改叫 agent**(界面与文档,词表随后改) | 支撑 2-6 |
| **知识库**(原「项目空间」)| AI Coding 项目的资产知识库,三页签:**知识**(仓内文档树 + 渲染)/ **代码图**(codegraph:模块依赖图、大文件榜、符号影响面搜索)/ **资产**(项目自有与蒸馏 skill、agent、运作 skill、产物登记、仓统计指标) | 0、5,愿景① |

**去留表**(判据:MVP 旅程用不用得到;用不到的不带,登记进 `docs/LEFTOVERS.md`,不删设计史料):

| 今天有的 | 去向 |
|---|---|
| 阶段轴(总览 + 五阶段按钮)、阶段舱、五阶段进度面板 | **不带**;方法论内容进规范扩展件 `docs/method/`,阶段降为活的类别标签(已定) |
| 进度面板(全部阶段) | 重做为**总览** |
| Issue 看板(六列) | **保留**,进**计划**屏(周视角 + 六列看板一个界面,卡片与按钮语义照 V3) |
| 版本面板、产物面板、项目自己的技能/队友/工作流盘点、仓统计指标 | 并入**项目空间「资产」页签**(发版记录、产物登记、项目自有与蒸馏技能、队友、运作技能、仓统计指标);仓统计同时在总览一小条 |
| 工作流面板、旧聊天式引擎、旧聊天面板 | **不带**(减负线 `cut-legacy-engine` 分支已删;对话体验由会话屏嵌入终端承载) |
| 定时任务面板(阶段视角) | 并入**配置**的定时任务列表 |
| 十个图标:技能库/队友库/工作流库/定时/连接器/知识/活动/通知/设置 | 技能/队友/定时/连接器 → 项目内**配置**(带导入;没有全局资产库);工作流库 **不带**(运作技能接管);知识 → **项目空间**;活动/通知 → 顶栏**待办**抽屉;设置 保留 |
| 嵌入终端、Open Design 嵌入 | 进**会话**(+ Cursor;+ 代码结构侧栏) |
| 蒸馏 | 进**会话**顶部动作 + 项目空间可见 |

**关于口述里的「默认 workflow 严选、可拔插」**:V4 不再有独立的「工作流」概念与工作流库——它在旅程里的位置由两样东西接管:**技能**(`SKILL.md`,按活注入,严选 + 鱼塘,可拔插)与**运作节律**(三张运作活,规范里可改)。旧的聊天式工作流引擎与工作流库不带,原因是旅程用不到,不是把「可拔插」这条诉求砍掉。

---

## §6 · 数据模型增量(库)

| 表 | 增什么 | 为什么 |
|---|---|---|
| `issue` | `week_of`(挂周)、`version`(在研版本标签,可空)、`tool`(开工工具:claude_cli / cursor / open_design)、`kind`(业务活 / 运作活)、`origin`(人建 / 自动建 / agent 拆)、`metric_ids`(预期推动的指标,0..n)。口述里的「周 / 版本 / 里程碑」三级,MVP 只落前两级:**里程碑不单建实体,版本就是里程碑**(发版记录里的一行);更粗的节点等实践里真需要再加 | §2.5、第 3/4/6 站 |
| `release`(新)| project、版本号、日期、说明、包含的活 | 第 5 站 |
| `project` | `standard_version`(规范版本)、`current_version`(在研版本)| 第 0/1 站 |
| `week_plan`(新;仓文件为正本,库存索引)| project、`week_of`、周目标、仓文件路径 | 第 2/3 站 |
| 观测、交棒、运行记录、产物、队友战绩 | 不变 | 铁律 |

**信息住哪(第三轮用户要求「谨慎数据库」)**:三层,按「能进仓的进仓、库只放记账与推导」分:

| 层 | 住什么 | 特点 |
|---|---|---|
| **仓**(项目代码仓,正本)| 章程、AGENTS.md、规范件、`.bw/project.toml`、`.bw/metrics.toml`、周计划、发版记录、技能、产物 | 人 / agent / leader / committer 全可见;变更走 MR;换机器不丢;**不需要在 buddy 里特别关注** |
| **本机文件**(不进仓)| claude / Cursor / Open Design 路径与预算、codehub / GitHub 登录态、每活的 worktree、终端会话缓存 | 一台机器一份;丢了重配,不影响项目 |
| **本机库**(SQLite,只做记账与推导)| project 行(指向仓)、issue 行(与远端 issue 对应 + `week_of` 等属性)、观测(只追加)、运行记录 / 产物登记 / 队友战绩、`settled_at` 与合入人、release 行、`week_plan` 索引、定时触发记录、信号缓存 | 数字都能 `sqlite3` 读回;丢了大部分能从仓 + 远端 issue 重建(观测与运行记录是例外——它们是过程数据,见细化 (e)) |

迁移守则不变:每加一列同时改 `schema.sql` 并加 `add_column_if_missing`;退役表用 `DROP TABLE IF EXISTS` 迁移。

---

## §7 · 建法:新壳 + 旧内核,减负线先合,模块管控

- ✅**待拍-17 已定**:**新壳**——桌面壳按 §5 五入口在新目录重写,旧屏幕一律不搬,V4 壳跑通后删旧壳;内核 crate 沿用并按 §6 增量。
- ✅**待拍-19 已定**:高保真原型做成**可点击的 HTML**(任何机器能开,后续 Dioxus 实现直接照抄结构与样式);不做 Open Design 文件。
- ✅**待拍-18 已定**:减负线(PR #102 + 第二轮 `cut-legacy-engine-2026-08-18`)**先收尾合入**——交给那条线作者的 prompt 见 [handoff-prompt-legacy-cut-merge.md](handoff-prompt-legacy-cut-merge.md)(要点:合 main 解 7 文件冲突、文档体系合一、别拔交互式执行器 / Open Design / 连接器 / 指标管道 / 定时建活与采集 / 蒸馏 / 合入命令这些 V4 器官、过门禁、push、更新 PR)。
- **模块管控(待拍-11 用户附加要求,升为建法原则)**:老 buddy 走不下去的原因之一是 `lib.rs` 动辄一万多行、改一处散弹一片。V4 新壳与所有借鉴件立三条规矩:①**一屏一模块**(总览 / 计划 / 会话 / 配置 / 项目空间各自独立目录,只通过内核的命令与事件通信,不互相 import);②**一个外部能力一个适配模块**(嵌入终端 xterm、Claude CLI、Cursor、Open Design、codegraph、DSH 插件……各自一个模块 + 一份 README 记「借自哪个项目哪个文件、借了什么、没借什么」),以后持续借鉴只在该模块内增量,不散弹;③**文件行数守卫**:单文件超过约定上限(建议 1500 行)CI 报警,`guard-*.sh` 加一条,和现有「内核不依赖 UI」守卫并列。
- **不推倒 V3 的视觉与好用的组件**(高保真第一版反馈:「为什么完全推倒了之前版本的东西」):代码是新写的,但项目墙卡片、本机环境条、健康概览条、指标卡与灰卡诚实文案、Issue 列表行、嵌入终端面板、Hub 列表行、创建流两卡、暖纸 token,一律沿用 V3 的样子;新壳只重排信息架构。
- 8 月 10-11 日那条 `next/` 重建线(未合入)里可参考的:六段总览推导、多家 agent CLI 注册表思路;**只借想法不搬代码**。

---

## §8 · 验收:什么叫 MVP 做完

1. 一个真实内部项目(点名四个之一,用户在内部选)在 V4 上**跑完两个完整周循环**:两份周计划文件、两轮运作活①②、至少一次发版记录,都在仓里。
2. 总览灯**不是灰的**,且灯的每条理由、每个数字都能用 `sqlite3` 或打开仓文件核对出来;至少一个引领指标卡下挂着推动它的活。
3. 至少一张活用 Open Design 开工、至少一张用 Claude CLI 开工(装了 Cursor 的机器再加一张 Cursor),产物真进了仓;终端内容能复制;右侧代码结构侧栏能看到本活改动的文件。
4. 第二台 buddy 纳入同一仓:leader 看到同一张总览;committer 在「待人处理」看到评审中的活并成功合入。
5. Windows 安装包 `0.4.0-v4` 在同事机器上装得上、跑得起;GitHub 仓走同一流程能接入。
6. 每站至少一条 E2E 读回记录(深链启动 + SQL 读回 + 截图)进 `docs/v4-prototype/`。

---

## §9 · 「为什么在 buddy 上运作项目」:前后对比怎么给

不编数字。一页对比放在项目墙的空态与安装包的使用指南里,证据来自试点项目的真实周记录。**对比对象不是「传统方式」,是 AI Coding 时代里「没有 buddy」的做法**(第三轮用户纠正:现在人人都在 AI Coding,不存在人写代码 vs agent 写代码的收益):

| AI Coding 时代,没有 buddy | 有 buddy(V4) | 证据从哪来 |
|---|---|---|
| 每个项目、每个人一套做法,agent 的产物越堆越多、没人管,项目无限膨胀 | 所有项目按同一套规范运作:同样的章程、约定、目录、节律;运作活②每周盘点与微重构,不让它膨胀 | 多个仓的同一套骨架;`docs/plan/` 尾段的盘点记录 |
| 资产散在聊天记录、本机目录、各人脑子里,换个 agent / 换个人就看不见 | 所有资产都在项目代码仓、有规范位置,任何大模型打开仓就看得见 | 仓文件本身 |
| 1-2 个人的项目 / 实验局没有角色把控,做完了但走偏了、没人知道 | Builder 一个人 + 周循环:指标从真实数据推导、周目标挂着活、完成永远人点、健康灯不假绿——项目不走偏 | 总览截图 + SQL 读回;`.bw/metrics.toml` 变更史 |
| 在 codehub、终端、设计器、聊天工具、问题单之间跳来跳去 | 一张工作台:活按类别在工作台里用 Open Design / Claude CLI / Cursor 开工;评审、合入、发版都在同一张台面上;聊天、问题单是下一批接进来的适配模块 | 会话屏 + 运行记录 + MR |
| 好用的组件 / agent / skill 靠各自摸索 | 严选的默认件随规范铺进每个项目,鱼塘里挑,用过就记账 | 技能使用记录、战绩 |

---

## §11 · 收口状态(第三轮):22 条全部已定 + 高保真期新增 1 条(23),剩五条「设计期待细化」

| 编号 | 站 | 结论(已定) |
|---|---|---|
| 01 | 0 | 意图卡四字段:名称 / 想做什么 / 最像的对标 / 三个月长成什么样;**去掉「类型」**,有无代码由 buddy 探测 |
| 02 | 0 | 规范铺底整套进仓、独立成模块([standard-module-draft.md](standard-module-draft.md)) |
| 03 | 1 | 总览 = 项目名片 + health + 指标挂活 + 本周进度 + 在研版本与发版 + 待人处理 + 本周运作 |
| 04 | 1 | 不做多版本线;一个在研版本;旧版本维护活打标签 |
| 05 | 2/3、6 | 周期性运作活两张:①更新指标 + 定计划(人触发,判据 = 当前周无计划)②资产盘点与微重构(定时自动建 + 自动开工);一次性③规范铺底(接入时,走 MR;成熟仓多一步合并调整);机制见 §2.6 |
| 06 | 3 | 周计划正本进仓 `docs/plan/` |
| 07 | 3 | 活保留类别标签(五阶段方法论) |
| 08 | 3 | agent 拆活只出草稿人确认;所有活同一范式(系统提示词 + 技能 + 人结伴),工具因类别而异 |
| 09 | 4 | 开工工具三个:Open Design / Claude CLI / Cursor;类别 → 工具 → 默认技能/队友映射表 |
| 10 | 4 | 默认件业界优先(superpowers、grillme)+ 自建两运作技能 + 两队友;其余进鱼塘;可拔插 |
| 11 | 4 | Orca 借模式不整体嵌;借鉴件必须模块化管控(§7) |
| 12 | 5 | 发版本 = 仓记录 + 可选 tag;打包流水线看后续实践 |
| 13 | 5 | committer 纳入同仓审核合入;合入后 committer 顺手点完成;通知走内部聊天工具增量包 |
| 14 | §2.5/§2.6 | 活的边界 = 需要一次 agent 会话就是活;人在首尾;全景见 §2.6 |
| 15 | §4 | AGENTS.md 正本 + CLAUDE.md 一行导入 |
| 16 | §4 | 规范八大类与核心/扩展先按 standard-module-draft.md,后续按实践修 |
| 17 | §7 | 新壳 + 旧内核 |
| 18 | §7 | 减负线先收尾合入(prompt 已给) |
| 19 | §7 | 高保真 = 可点击 HTML |
| 20 | 0/§4 | 成熟项目也铺全套核心件;铺底走一次性运作活③ + MR;已有 README/CLAUDE.md/AGENTS.md 在同一张活里多一步 agent 合并调整,不跳过不覆盖 |
| 21 | 2/3 | 运作活的变更**走 MR**;合入前工作台可**预览**该活 worktree 里的效果(总览/计划切「预览 · 未合入」) |
| 23 | §5 | (高保真两版反馈新增)左栏入口 = 总览 · 计划(周 + 六列看板)· 会话 · 通知 · 配置 · 知识库;顶层 = 项目墙(含指南抽屉)+ 设置(含工作区根目录);「队友」改叫 agent;资产来源只两种(buddy 内置 / 项目导入);同事贡献走 buddy 仓 `standard/` 的 PR(AGENTS.md 模板、内置 skill/agent)与适配模块(内部通信工具 → 每个项目在聊天工具里有个助手),不占 MVP |
| 22 | 4 | 右侧代码结构侧栏首版 = 可点开的文件树(点文件即打开只读视图)+ 本活改动文件与 diff + git 状态 + MR 卡;「符号级大纲」= 每个文件里函数/类/结构的清单,首版不做,模块化留口,按需从 Orca / codegraph 增量借(见 [research/codegraph.md](research/codegraph.md)) |

**设计期待细化(不阻塞,进设计稿时定)**:(a)「预览 · 未合入」的具体形态:哪几块支持预览、切换入口放哪;(b)一次性运作活③「规范铺底」里 agent「合并调整」一步的技能内容与验收;(c)事件触发(远端新 issue / CI 红 / 指标过期 → 自动建活)的接口留口,MVP 后;(d)运营推广类活的默认技能——先不预置,从鱼塘挑,等有真实活再定;(e)观测与运行记录只在本机库、换机器会丢——是否把每周的观测快照回写进仓(如 `docs/metrics/`)让 leader / committer 也看得见,MVP 后按试点反馈定。

**下一步**(预研融入与评审复核已完成):设计 md(逐屏规格 + 数据 + 命令/事件)→ 设计 html → 高保真可点击原型 → 规范铺底模块落地 → 数据增量 → 新壳逐站落地(每站 E2E 读回)→ 内部试点 → 出包 `0.4.0-v4`。
