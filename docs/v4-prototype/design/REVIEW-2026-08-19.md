# V4 详细设计交叉复核(2026-08-19)

> **30 秒导读**:这是一次**只读**交叉复核——把 `design/01-09` 九篇详细设计稿、`design/README.md`、四篇新预研(`research/{workflow-skill-packages,chat-group,legacy-backfill,kanban-drag-dioxus}.md`)、母文档 `mvp-blueprint-draft.md`(第四轮,作数)与 `standard-module-draft.md` 逐篇对照读了一遍,找它们互相之间、以及跟母文档之间对不上的地方。**给谁看**:要拍板这批设计的人(你)。**怎么用**:这份文件只登记问题,不改动任何设计稿——每条问题给「文件:行号」「一句话问题」「一句话建议改法」,你确认要改的,回头我(或下一个会话)照着改。产出于 2026-08-19,复核的是当天下午写完的稿子;这批设计稿之后若再改动,这份复核的行号会漂移,需要重新核对。

## 一句话总评

九篇的**旅程叙事、旅程分工、母文档四问的落地**基本一致、可读性好,达到「详细设计第一版」的骨架水准;但**数据模型层(schema)有多处真实冲突**(见第 2 节)、**01 篇作为「命令/事件总表」已经明显滞后**于 03/04/05/06/07/08 各自的最新决定(见第 1 节)、`agent` 表怎么处理这一件事两篇给出了**相反的建议**(02 软退役 vs 04 硬删)——这些不是措辞问题,是会让下一步写代码的人无所适从的真实分歧,**建议先收敛这批冲突再进入开发**,不建议原样交付「详细设计第一版」给下一步的会话直接抄。其余六个维度(文件格式、母文档冲突、写作纪律、重复漏项)问题密度低,基本可用。

---

## 1 · 命令 / 事件名对齐

**结论:01 篇作为「总表」已经过时,存在真实的同事异名和遗漏。**

| # | 文件:行号 | 问题 | 建议 |
|---|---|---|---|
| 1.1 | `01-architecture.md:299-300` vs `05-session-screen.md:106` | 01 篇定的会话屏新命令叫 `OpenFile{path}`/`ShowDiff{issue_id,path}`;05 篇(会话屏的正主)实际给出的是三条不同名字的命令 `OpenFileTab{issue_id,path}`/`OpenDiffTab{issue_id,path}`/`ExpandTreeDir{issue_id,dir_path}`——**同一件事(打开文件/打开 diff)在两篇里叫了不同的名字**,且 05 篇多给了 01 篇完全没提的 `ExpandTreeDir`。 | 以 05 篇(领域正主)为准,01 篇改名并补 `ExpandTreeDir`。 |
| 1.2 | `01-architecture.md:117` vs `06-plan-screen.md:194-196` | 01 篇的 `PreviewIssueWorktree{id}`(只有「开」)在 06 篇被明确改名为 `TogglePreview{id: Option<IssueId>}`(能开能关),06 篇原话「01 篇据此改名」——但 01 篇至今没改。同样,06 篇把 01 篇的 `ScheduleIssue`+`UnscheduleIssue` 合并成一条 `ScheduleIssue{id, week_of: Option<String>}`(06 篇 `:178-181`),01 篇仍是两条分开的命令(`:286-287`)。 | 01 篇按 06 篇回填:删 `UnscheduleIssue`,`PreviewIssueWorktree`→`TogglePreview`。 |
| 1.3 | `01-architecture.md` 全篇 | 01 篇完全没有 06 篇新增的 `SetCurrentVersion{version}`(06 篇 `:200`,「切在研版本」)——06 篇自己写明「01 篇未列,本篇新增,01 篇据此回填」,但 01 篇没回填。 | 补进 01 篇总表。 |
| 1.4 | `01-architecture.md` 全篇 vs `03-standard-and-backfill.md:169-171` | 03 篇新增的 `BackfillHistory{project_id}`、`ReconcileStandard{project_id}`、`UpgradeStandard{project_id,files}` 三条命令,01 篇总表里完全没有(01 篇只列了 `RunStandardBootstrap`)。 | 补进 01 篇总表。 |
| 1.5 | `01-architecture.md` 全篇 vs `04-tools-and-workflows.md:189-192` | 04 篇新增的 `SetIssueWorkflow`、`SaveToolMapping`、`ProbeTool`、`MarkEntrySkill` 四条命令,01 篇总表里一条都没有。 | 补进 01 篇总表。 |
| 1.6 | `04-tools-and-workflows.md:189` vs `06-plan-screen.md:110,202` | 同一条命令 `SetIssueWorkflow` 字段名不一致:04 篇是 `{id: IssueId, workflow_ref: String}`,06 篇是 `{id: IssueId, workflow: String}`——`workflow_ref` vs `workflow`,两篇各自「新命令」标注,像是各写各的没对过。 | 统一成一个字段名(建议 `workflow`,和 `issue.workflow` 列名一致,减少心智负担),04/06 两篇同步改。 |
| 1.7 | `01-architecture.md:294` vs `07-notify-and-chat-group.md:191` | `SyncNotifyToChat` 字段名不一致:01 篇 `{id: IssueId, event_kind: String}`,07 篇(此命令的正主)`{issue_id, event_type}`——`id`/`event_kind` vs `issue_id`/`event_type`,两处都不一样。 | 以 07 篇为准(它是这条命令的正主),01 篇同步改;同时看 2.5 条——`chat_outbox` 表列名本身也在 `event_kind`/`event_type` 上分裂,两处要一起定。 |
| 1.8 | `01-architecture.md:295` vs `07-notify-and-chat-group.md:192` | `FetchChatDigest` 签名不一致:01 篇 `{since: String}`,07 篇 `{project_id, since, until}`——07 篇更完整(01 篇甚至缺 `project_id`,没法知道给哪个项目拉历史)。 | 以 07 篇为准,01 篇改。 |
| 1.9 | `01-architecture.md` 全篇 vs `07-notify-and-chat-group.md:193` / `08-overview-derivation.md:202-203` | 01 篇的事件列表里没有 07 篇新增的命令 `MarkNotifySeen{project_id,at}`,也没有 08 篇新增的两个事件 `ProjectCardEditPending`/`ProjectCardMerged`。 | 补进 01 篇总表。 |
| 1.10 | `01-architecture.md:292` | `EditProjectCard` 的伪码签名是 `{name: String, kind: String, descr: String}`——`kind` 字段是**母文档已经明确删掉的「项目类型」字段**(见第 4 节 4.1),`descr` 这个字段名在 02/08 两篇的实际文件格式里从未出现(02 篇 `.bw/project.toml` 样例字段叫 `brief`,08 篇 §2.5 描述的可改字段是「想做什么/对标/北极星一句/项目群」)。这条命令签名看起来是从 V3 旧签名直接照抄下来的,没有跟着 V4 的字段改。 | 按 08 篇 §2.5 实际字段重写这条命令签名,去掉 `kind`。 |

---

## 2 · 表名 / 列名对齐

**结论:这是本次复核发现的最大问题——`release`/`issue` 两张核心表在 02(数据总表)和 06/08(消费方)之间存在直接的 schema 冲突,`agent` 表怎么处理两篇给出相反建议。这些必须在开工前拍板,否则实现时会有一半代码抄 02、一半代码抄 06/08。**

| # | 文件:行号 | 问题 | 建议 |
|---|---|---|---|
| 2.1 | `02-data-and-files.md:31,51-60,489` vs `06-plan-screen.md:111,146,150` vs `08-overview-derivation.md:71,81,221` | **`issue_metric` 关联表 vs `issue.metric_ids` JSON 列,两种方案在用**。02 篇(数据总表)设计了专门的 `issue_metric` 关联表并给出完整理由(支持「指标反查活」的索引查询),但同时把这个方案标成**待拍的开放问题**(02 篇 `:489`「请用户拍板」);06 篇和 08 篇没等这个拍板,直接在各自的 schema 增量里写了 `issue.metric_ids TEXT NOT NULL DEFAULT '[]'` 当一个 JSON 列用(06 篇 `:146`,08 篇 `:221`),而且 08 篇 §2.3 的读回 SQL 用的是 `metric_ids LIKE '%<metric_id>%'`(`:81`)——这种字符串匹配连 02 篇自己否决 JSON 方案的理由(「JSON 文本列做不到索引查询」)都验证了。 | **必须拍板**:要么采纳 02 篇的 `issue_metric` 关联表(06/08 两篇的 schema 片段和读回 SQL 都要改成 JOIN),要么正式采纳 JSON 方案(02 篇删掉 `issue_metric` 表设计,03/09 两篇里提到 `issue_metric` 索引行的地方也要改)。不要两种方案并存到开发阶段。 |
| 2.2 | `02-data-and-files.md:30,107-125` vs `06-plan-screen.md:165-171` vs `08-overview-derivation.md:223` | **`release_issue` 关联表 vs `release.issue_ids` JSON 列,同一个分裂模式重演**。02 篇设计了 `release_issue(release_id, issue_id)` 关联表(理由与 2.1 同源:「总览要展开某版本包含哪些活」需要反查);06 篇和 08 篇的 `release` 表定义里都是 `issue_ids TEXT NOT NULL DEFAULT '[]'`(JSON 数组),完全没有 `release_issue` 表。 | 与 2.1 同一次拍板一起定;两处方案要保持一致(`issue_metric`/`release_issue` 要么都建关联表,要么都改 JSON——02 篇给这两张表的理由本来就是同一条,不该一个建表一个不建)。 |
| 2.3 | `02-data-and-files.md:114` vs `06-plan-screen.md:167,239` vs `08-overview-derivation.md:135,155,223` | **`release` 表的时间列名字和类型不一致**:02 篇是 `released_at INTEGER NOT NULL`(unix 秒);06 篇和 08 篇都写的是 `date TEXT NOT NULL`(字符串日期,读回 SQL 也是 `SELECT ... date ...`)。列名(`released_at` vs `date`)和类型(INTEGER vs TEXT)都不一样。 | 拍板一个,三篇统一;建议参考 02 篇(与 `issue.created_at` 等既有时间戳列的类型习惯一致,unix 秒方便排序与跨时区展示)。 |
| 2.4 | `02-data-and-files.md:46` vs `06-plan-screen.md:146,212` | **`issue.sort_order` 类型冲突**:02 篇明确写 `INTEGER NOT NULL DEFAULT 0`;06 篇采纳了 `kanban-drag-dioxus.md` 预研的「浮点数插入排序」方案,写的是 `sort_order REAL`(06 篇 `:212` 有专门一段解释为什么用浮点数)。这不是措辞差异——INTEGER 类型物理上无法支持「新卡片插进两张卡之间取中间值」这个 06 篇依赖的排序算法。 | 采纳 06 篇的 `REAL`(有预研背书、有具体算法理由),02 篇改类型。 |
| 2.5 | `02-data-and-files.md:152-163,460` / `01-architecture.md:294` vs `07-notify-and-chat-group.md:122,191,236,248` / `08-overview-derivation.md:226` | **`chat_outbox` 表的列名分裂**:02 篇(数据总表,含完整 DDL)用 `event_kind`+`subject_kind`+`subject_id`(为了同时容纳 issue 和 release 两类通知主体,02 篇专门设计了 `subject_kind` 这个字段);07 篇(此表的主要消费方)和 08 篇都只用 `event_type`+`issue_id`(隐含假设每条通知都挂在一个 issue 上)。01 篇的命令签名也用的是 `event_kind`(与 02 一致)。 | 拍板一个命名;并且要连带确认一件事——07 篇设计的发版通知(`release` 事件)到底是挂在「发版本 vX」这张轻量活的 `issue_id` 上(那 `issue_id` 就够用,07/08 的简化是对的),还是需要独立指向 `release.id`(那 02 篇的 `subject_kind`/`subject_id` 设计是必要的,不能简化掉)——这个决定权在 06 篇 §2.4 的发版本流程设计里已经暗含答案(发版本本身也建了一张轻量活、有 `issue_id`),建议顺着这条直接采纳 07/08 的简化版,02 篇改字段名对齐。 |
| 2.6 | `02-data-and-files.md:44,477` / `06-plan-screen.md:148` / `09-ops-workflows.md:57,189` vs `08-overview-derivation.md:221` | **`issue.origin` 枚举值拼写不一致**:02、06、09 三篇一致用 `agent_split`;08 篇唯独写成 `agent_draft`(`08-overview-derivation.md:221`)。 | 08 篇按其余三篇改成 `agent_split`(三对一,`agent_split` 明显是既定值)。 |
| 2.7 | `02-data-and-files.md:214` vs `04-tools-and-workflows.md:93` | **`.bw/issue-policy.toml` 里「运营推广」类别的取值不一致**:02 篇给的完整文件样例用 `category = "growth"`,04 篇的映射表用 `category: grow`。这两个字符串如果真按文件写进配置,解析器按哪个匹配都会漏掉另一边——`build`/`optimize`/`prototype` 等其余取值两篇是对得上的,只有这一个词分裂。 | 选一个(建议 `growth`,是完整单词,和其余取值 `prototype`/`build`/`optimize` 的构词风格更一致),两篇统一。 |
| 2.8 | `02-data-and-files.md:105,445,490` vs `04-tools-and-workflows.md:164-169` | **`agent` 表怎么处理,两篇建议相反**:02 篇明确建议「**软退役,不物理 DROP**」,三列(`runs`/`wins`/`win_rate`)冻结但表留着可读回历史;04 篇明确建议「**同一次改动里 `DROP TABLE IF EXISTS agent`**」,连 `CreateAgent`/`UpdateAgent`/`ImportAgentDefinition` 三条命令和 `agent_import.rs` 整个模块一起删。两篇都各自标了「这是开放问题,不是本篇能拍板的事」,但给出的**默认建议方向完全相反**——这不是同一件事的两种措辞,是两个互斥的技术方案(留表可回滚 vs 删表不可逆)。 | **必须拍板,建议列进第 6 节高优先级**。如果选 04 篇的硬删,02 篇 §2.4/§3.2 与验收 SQL(`02-data-and-files.md:485`,读 `agent` 表的那条读回)要整段删除或改写;如果选 02 篇的软退役,04 篇 §2.10 要撤回建议、`issue.assignee` 的处理方式也要重新定(04 篇当前假设是配合硬删走的)。 |
| 2.9 | `02-data-and-files.md:490` vs `04-tools-and-workflows.md:166`(「退场依赖标注给 02 篇」) | **`issue.assignee` 的去留,两篇互相甩锅**:02 篇开放问题 2 写「`issue.assignee` 是否被完全取代…需要和 04 篇协同定案,本文不越权拍板」;04 篇 §2.10 写「`issue.assignee`…退场依赖标注给 02 篇」。两篇都把决定权推给对方,没有一篇真正给出处理方案。 | 这是一个需要用户/下一个设计会话直接拍板的真空点,建议和 2.8 一起处理(`agent` 表怎么处理决定了,`issue.assignee` 的答案基本也就跟着定了)。 |
| 2.10 | `04-tools-and-workflows.md:34-52` vs `04-tools-and-workflows.md:87-95` | **同一份文件里,「category」这个字段名被用来表示两件不同的事**:`[[tool]]` 段的 `category` 指「终端类/本机网页内嵌类」(`terminal`/`web_embed`,04 篇 `:36`);`[[mapping]]` 段的 `category` 指「活的五阶段类别标签」(`prototype`/`build`/…,04 篇 `:87-95`)。两段在同一个 `.bw/issue-policy.toml` 文件里都叫 `category`,含义完全不搭。 | 不算 bug,但建议改名减少读文件时的混淆——比如把 `[[tool]]` 段的字段改叫 `kind`(工具接法类型)或 `interface`,把 `category` 这个词让给 `[[mapping]]`(活的类别)专用。 |
| 2.11 | `02-data-and-files.md:71` vs `06-plan-screen.md:173` | **列的归属被错误引用**:06 篇写「`project` 表加 `current_version TEXT`(在研版本)与 `standard_version TEXT`(**03 篇范围**,不重复)」——但 `standard_version` 这一列实际是在 **02 篇** §2.3 定义的(02 篇 `:67`),03 篇本身没有定义任何 `project` 表的列。 | 06 篇这句引用改成「02 篇范围」。轻微,但会误导下一个读 06 篇去 03 篇找定义、找不到。 |
| 2.12 | `02-data-and-files.md:66-72` vs `08-overview-derivation.md:225` | 08 篇 §3.2 的「数据结构增量」表里,`project` 表新增列只列了 `standard_version`/`current_version`/`chat_provider`/`chat_group_id` 四个,漏了 02 篇定义的第五个 `chat_notify`(项目群同步哪些事件的 JSON 数组)——07 篇的 `[chat] notify` 字段(07 篇 `:120`)需要这一列才能落库。 | 08 篇补上 `chat_notify`,或明确说明这一列由 07 篇负责(但目前 07 篇 §3 也没有专门重复这张表,是隐式假设它存在)。 |

---

## 3 · 文件格式对齐

| # | 文件:行号 | 问题 | 建议 |
|---|---|---|---|
| 3.1 | `04-tools-and-workflows.md:29-52` vs `02-data-and-files.md:191-231` | 04 篇引入的 `.bw/issue-policy.toml` `[[tool]]` 段(声明 `claude_cli`/`cursor`/`open_design` 三个开工工具的接法类型、探活方式、能力清单)在 02 篇给出的「完整样例」(02 篇自称「每个给一份可以直接抄的样例」)里**完全没有出现**——02 篇 §2.8 的 `.bw/issue-policy.toml` 样例只有 `[[mapping]]`/`[review]`/`[cadence]`/`[kanban]` 四段,漏了 `[[tool]]`。 | 02 篇的样例补上 `[[tool]]` 段(内容抄 04 篇 `:34-52`),否则「完整样例」名不副实,照抄这份样例的实现会漏掉工具注册这一段。 |
| 3.2 | 全篇引用链 | **大量过时的「尚未成稿/未写」标注**:02 篇 §0 写「03 篇尚未成稿」「04 篇尚未成稿」「05/06/07 尚未成稿」(`02-data-and-files.md:9`),02 篇 `:303,491,493` 三处也仍标 09/03/07 「尚未成稿」;03 篇 `:11` 仍标「09 篇尚未成稿」「04 篇尚未成稿」;05 篇 `:9,11,98` 三处仍标「04 篇尚未写」「01 篇尚未写」「02 篇尚未写」;06 篇 `:9` 仍标「09 篇未写」「04 篇未写」。但实际上这九篇现在**全部已成稿**(文件都在、都有完整内容,`ls` 与本次通读可核实)。这些标注是写作时的真实状态(各篇确实是按顺序陆续写的),但现在已经全部滞后于事实。 | 做一次全篇扫描,把「尚未成稿/未写」字样按实际完稿状态批量更新;这类标注具有误导性——读者会因为某篇写着「04 篇尚未成稿」就不去 04 篇核对,从而漏看本复核第 1/2 节列出的那些真实冲突。 |
| 3.3 | `02-data-and-files.md:191-231` vs `standard-module-draft.md:75` | 规范草案(`standard-module-draft.md` 第 5 类)描述 `.bw/issue-policy.toml` 时写的是「三列(第四轮专家反馈去掉 agent 列)」,与 02/04 两篇的三列映射(`category`/`tool`/`workflow`)一致,**未发现冲突**,列此条只为记录已核对过。 | 无需改动。 |

---

## 4 · 与母文档的冲突

| # | 文件:行号 | 问题 | 建议 |
|---|---|---|---|
| 4.1 | `01-architecture.md:292` | 见 1.10——`EditProjectCard` 命令签名带 `kind: String` 字段,与母文档待拍-01「去掉『类型』;有没有代码由 buddy 探测」(`mvp-blueprint-draft.md:124`)直接冲突。这是九篇里唯一一处「代码签名开倒车」的地方。 | 修 01 篇,理由同 1.10。 |
| 4.2 | 全篇交叉核对 | **母文档 §2.6 三条不变的规矩、五层加载、待拍-01~28 与九篇的落地基本一致,未发现系统性偏离**:发版本已按母文档改成「轻量活 + MR」(06 篇 §2.4 明确写了这是「母文档 §2.6 一处自相矛盾…被按规矩①改正后的口径」);`agent` 名单退场在 01/04 两篇都有落地(尽管 2.8 条指出处理方式冲突,但「不维护 agent 名单」这个方向本身两篇一致);拖拽只排期(06 篇 §2.3 与母文档 §3 逐句对应);回填不算战绩不点灯(02/03/08/09 四篇一致);群消息不进库不进仓(07/09 两篇一致)。 | 无需改动,列此条说明已系统核对过。 |
| 4.3 | `mvp-blueprint-draft.md:100` vs 九篇 | 母文档 §2.6 全景表「评审 / 合入 / 点完成」这一行提到「预览未合入效果」,九篇里 06 篇(`§2.5`)和 08 篇(`§2.6`)都各自给出了预览机制,但**两篇对「能不能点」的判据表述略有出入**:06 篇 `:99` 说「当前选中周存在至少一张 `kind='ops'` 的活处于 `InReview` 且 MR 未合入才能点开」;08 篇 `:192` 只说「运作活①的 MR 合入前」,没有明确提到「或③的合并调整/历史回填」这个更宽的口径(06 篇 `:99` 明确写了「本篇按更宽口径设计,覆盖运作活①和③」)。08 篇没有跟进这个「更宽口径」的决定。 | 08 篇 §2.6 补一句,与 06 篇口径对齐(覆盖运作活①和③,不只①)。 |

---

## 5 · 写作纪律

CLAUDE.md 明确规定「实现术语(settle-once、derive-only 这类)只进第 3 节」,README.md 的写作规矩重申了这条。逐篇检查后,发现两个术语在「第 3 节:工程对照」之外的正文里出现:

| # | 文件:行号 | 问题 | 建议 |
|---|---|---|---|
| 5.1 | `07-notify-and-chat-group.md:54,82,88` | `settle-once` 出现在 §2.1(表格)与 §2.4(正文)三处,都在「第 2 节:设计」里,不在第 3 节。 | 这三处的正文改说人话(「同一条不重复记账的路径」/「同一件事绝不记两次」),`settle-once` 这个词挪到第 3 节工程对照(如果那里需要提它)。 |
| 5.2 | `01-architecture.md:137,331` | `Derived<Signal>` 出现在 §2.6(第 2 节「设计」)和 §4(边界与失败)两处,都不在第 3 节。 | CLAUDE.md 原文明确把 `Derived<Signal>` 列为「只出现在代码、代码注释和工程对照表里」的实现机制词——这两处改成人话(「信号密封类型」或直接说「信号只能从数据推导这条铁律」),类型名挪到第 3 节。 |
| 5.3 | `08-overview-derivation.md:177,267` | 同上,`Derived<Signal>` 出现在 §2.4(第 2 节)与 §4(边界与失败),不在第 3 节;`08-overview-derivation.md:211` 那一处在真正的「## 3 · 工程对照」里,合规,不用改。 | 同 5.2,§2.4 和 §4 两处改人话。 |
| 5.4 | 未发现问题 | 全篇 grep 未发现 `derive-only`、`no-hijack` 两个词(CLAUDE.md 举例的另外两个术语)出现在任何design文档正文——没有违规。 | 无需改动。 |

**未发现的问题**(按要求列出):代号首现不解释——未发现明显违例,九篇对「运作活①②③」「第 N 步」「五层」等代号首次出现时基本都带了解释。新造词未进词表——`workflow`(=SOP 类技能包)、`鱼塘`、`项目群`、`回填`、`委托`等词母文档已声明「拍板后进词表」,九篇沿用同一批词、未见私自新造未解释的词。

---

## 6 · 重复与漏项

### 6.1 母文档承诺、没有任何一篇接住的东西

| # | 缺口 | 证据 | 建议 |
|---|---|---|---|
| 6.1.1 | **知识库屏(`kb`)没有专篇设计**。母文档 §5 六入口表明确列了「知识库」入口,含三个页签(知识/代码图/资产),但 01-09 九篇里没有一篇像 05(会话屏)、06(计划屏)、07(通知屏)、08(总览屏)那样系统设计这一屏——「知识库」只是零散地被 02(`:15,304`)、03(`:21,134,136`)、07(`:55,63`)、09(`:78`)当作别处功能的落点提到(「登记进知识库资产页」「知识库屏顶部有提示」),但知识库屏本身的三个页签——「知识」页签怎么渲染仓内文档树、「代码图」页签的模块依赖图/大文件榜/符号影响面搜索长什么样、「资产」页签具体列哪些字段——**没有任何一篇给出结构、命令、验收清单**。 | 建议补一篇 `11-knowledge-base.md`(或并入某一篇的新章节),否则「六入口全部跑通」这条删除旧壳的判据(01 篇 §2.11)缺一个入口的设计依据。 |
| 6.1.2 | 母文档「三条不变的规矩」第①条提到的「唯一例外:新建的空仓,首提交直推」——03 篇(`:33,55-58`)与 09 篇(`:136`)都给出了这个例外的处理机制,**未发现遗漏**,列此条说明已核对。 | 无需改动。 |

### 6.2 同一件事两篇各说各的(未在第 2/4 节列出的其余重复)

| # | 文件:行号 | 问题 | 建议 |
|---|---|---|---|
| 6.2.1 | `05-session-screen.md:180`(开放问题 2)vs `09-ops-workflows.md`(第 0 层系统提示词段) | 05 篇开放问题 2 问「hooks 端口策略」——`Notification`/`PreToolUse` 事件比 `SessionStart`/`Stop` 高频得多,要不要节流。09 篇 §2.4「系统提示词要不要加一句」讨论的是运作活②「无人在场」场景的提示词,**两篇实际在从不同角度处理『运作活②这类长时间无人agent会话』的同一类边界情况**(一个管 hook 上报频率、一个管系统提示词怎么让 agent 别卡住等回应),但互相没有引用对方——不算冲突,但设计上是同一个问题的两个侧面,合并考虑可能更省。 | 非必须,建议下一版把这两处的开放问题合并成一条,一起回答「运作活②无人值守时,hook 上报和系统提示词该怎么配合」。 |
| 6.2.2 | `07-notify-and-chat-group.md:201`(开放问题 2)vs `02-data-and-files.md` | 07 篇提议给 `issue.kind` 加第三个取值「轻量活」,并明确写「这不是本篇能拍板的字段,留给 02 篇确认」——但 02 篇的 `issue.kind` 定义(`02-data-and-files.md:43`)至今仍只有 `'business' | 'ops'` 两个取值,02 篇没有回应或采纳这个提议(02 篇全文未提「轻量活」这个 kind 取值)。这类似 2.9 条的「互相甩锅」模式,但这次只有 07 篇提了问题,02 篇连回应都没有(不是互相矛盾,是单向的悬空引用)。 | 02 篇需要显式回应:采纳(加第三个取值)或拒绝(说明改用别的字段组合区分,如 07 篇自己提到的备选方案「靠 `workflow` 是否为空」)。 |
| 6.2.3 | `09-ops-workflows.md:249-259`(§3.4) | 09 篇自己在正文里写「这不是本篇能拍板的事,差异留 §6」(`09-ops-workflows.md:251`),指的是 02 篇 `workflow_credit` 表 vs 04 篇 `skill_package`/`skill` 战绩列这处分歧——但翻到 09 篇真正的「## 6 · 开放问题」列表(`:302-308`),**这条分歧并不在里面**,09 篇 5 条开放问题里没有一条是这个。09 篇自己说了要在 §6 列出来,但没有真的列。 | 09 篇 §6 补一条,把 §3.4 提到的「02 vs 04 战绩记账口径分歧」正式列进开放问题清单(而这条分歧本身,建议直接并入本复核 2.8 条一起拍板——因为 04 篇的 `skill_package` 方案本来就是为了取代 `agent` 表记账逻辑而生的,和 2.8 条是同一个决定的两个层面)。 |

---

## 7 · 各篇第 6 节开放问题汇总

九篇合计 44 条开放问题(01×5、02×5、03×5、04×5、05×5、06×4、07×5、08×5、09×5)。下表按篇归类列出,「建议默认答案」是我的一次性判断,标「建议」,供你醒来拍板用;和第 2/6 节已经详细讨论过的冲突(如 2.8「agent 表去留」)不重复展开,只在这里点名对应哪条。

| 篇 | 问题(一句话)| 建议默认答案(建议) |
|---|---|---|
| 01 | 新 crate 最终叫什么名字(`app-shell` vs `app-v4`)| 建议:`app-shell`,理由已在 01 篇写清楚(不把版本号焊进长期名字) |
| 01 | 文件行数守卫阻断还是只报警 | 建议:阻断(新壳从第一天开始守规矩,没有存量借口) |
| 01 | 旧壳删除的时间点判据要不要更严格(要不要求「试点两周」也算进去)| 建议:要,母文档 §8 验收本身就要求两周试点,删旧壳判据应该和它对齐,不要六屏验收一过就删 |
| 01 | `BW_SCOPE` 环境变量去留 | 建议:退役(V4 计划屏没有对应概念),等 06 篇明确要不要新的「按类别筛选」深链变量再说 |
| 01 | `CreateAgent`/`UpdateAgent`/`ImportAgentDefinition` 退场后存量数据怎么处理 | 见本复核 2.8/2.9 条,需要和 `agent` 表整体去留一起拍板 |
| 02 | `issue_metric` 关联表还是 `metric_ids` JSON | 见本复核 2.1 条,**高优先级,必须拍板** |
| 02 | `agent` 表最终物理退役方式与 `issue.assignee` 存亡 | 见本复核 2.8/2.9 条,**高优先级,必须拍板** |
| 02 | `.bw/managed.toml` 指纹算法与对账触发时机 | 03 篇已经回答了这条(03 篇 `:134`:SHA-256 全字节哈希、知识库屏渲染时现算 + 运作活②每周算一遍)——02 篇这条开放问题**已被 03 篇回答,只是 02 篇没有更新掉这条**,建议 02 篇标记已解决并引用 03 篇 |
| 02 | `release_file.rs` 解析老项目已有发版记录的鲁棒性边界 | 03 篇已回答(`:138`:不解析不改写,只在文件末尾追加标准格式的历史段)——同上,02 篇这条也已被回答,建议标记已解决 |
| 02 | `chat_outbox` 失败重试策略 | 07 篇部分回答但仍留作开放问题(07 篇开放问题 4「继承预研开放问题 4」),两篇口径一致(留给实现时定退避节律),建议:保留为一条统一的开放问题,不用两篇各留一份 |
| 03 | 回填的 issue 后续被真实推进后,「回填活数」会不会随时间减少 | 建议:不特殊处理,`origin` 字段永久不改就是设计原则本身,总览上加一句脚注解释「回填数不含后续被真实推进的活」即可,不需要加新字段 |
| 03 | 目录黑名单要不要项目可配置 | 建议:MVP 先用硬编码默认清单(`vendor/`/`node_modules/`/`dist/`/`build/`/`target/`/`.venv/`),不做可配置,等试点反馈真的撞到再加 |
| 03 | 决策记录回填要不要留钩子 | 建议:不留,按「不为向后兼容留旧路径」的一贯原则,需要时再加不难 |
| 03 | codehub 侧命令未在真实环境验证,谁来协调验证 | 建议:定稿后立刻找一个真实 codehub 项目跑一遍(不要等到有老项目真的要接入才发现命令是错的) |
| 03 | 空仓例外下运作活③走「确认完成(人裁)」的具体入口 | 建议:04/05 篇设计会话屏/活详情交互时顺带确认,不需要单独立项 |
| 04 | `.bw/issue-policy.toml` 改动要不要每次都走活+MR(可能太重)| 建议:先按母文档规矩①走活+MR,试点两周后如果真的觉得太重,再退一步做「直接写本机+运作活②被动核对」的降级路径——先严后松比先松后严安全 |
| 04 | `agent` 表退役前要不要先归档存量战绩 | 见本复核 2.8 条,**高优先级**;如果最终选硬删,建议归档(跑一次性导出写进各项目 `docs/plan/history.md`),不要静默丢弃 V1-V3 已经产生的真实战绩数据 |
| 04 | Open Design 怎么定位到具体活的 worktree | 建议:列为一次穿刺任务,不在详细设计阶段猜答案(04 篇自己也是这个态度) |
| 04 | Cursor 侧 workflow/技能注入的真实机制 | 建议:等 Cursor 真正接通那天补穿刺,MVP 第一版可以先如实标「未验证」 |
| 04 | 判据 C(入口技能)人工补标交互细节 | 建议:导入后随时可补,不做成导入向导的必填一步(减少导入摩擦) |
| 05 | xterm 5.3.0→6 要不要顺带升配套插件 | 建议:先只做复制功能需要的部分,其余插件按需再加 |
| 05 | hooks 端口高频事件要不要节流 | 建议:需要(`PreToolUse` 每次工具调用都触发,不节流大概率刷屏),具体节流策略留实现时定 |
| 05 | Cursor 状态退化文案要不要拆细 | 建议:先用一句「未知」,不拆分,以后接类似工具再看是否需要通用退化文案 |
| 05 | 「+ 新会话」要不要限并发数 | 建议:先不设上限,等真实使用数据 |
| 05 | 单文件 diff 该 diff 工作树还是分支相对主干 | 建议:进行中的活 diff 工作树(所见即所得),评审中的活 diff 分支相对主干合并基点(评审者想看这次 MR 改了什么)——两态给不同默认值,而不是全局固定一种 |
| 06 | `sort_order` 再平衡算法 | 建议:先不展开,留到实现阶段按需处理(浮点数精度用尽是低频问题) |
| 06 | 「agent 起草发版说明」底层机制(要不要一种新的「无活会话」通道)| 这条 04/05 篇目前都没覆盖,建议下一版详细设计里由 04 或 05 篇正式接住,不要一直空转 |
| 06 | `TogglePreview` 并发场景(两张评审中运作活同时挂着选哪张)| 建议:MVP 先只支持「当前唯一一张待预览的活」,多张时按 `created_at` 取最新一张,不做选择器 |
| 06 | 「全部」视角要不要开放拖拽 | 建议:不开放(06 篇自己的理由——不按周分列,拖拽改哪一周不直观——是对的),等试点反馈 |
| 07 | 「带险交棒」在 V4 有没有触发路径(旧阶段轴屏幕不搬迁)| 建议:V4 第一版通知屏里保留这一行类型(如实、大概率长期为空),不要专门隐藏——隐藏了以后有新触发点接入时容易忘记打开 |
| 07 | `issue.kind` 要不要加「轻量活」取值 | 见本复核 6.2.2 条,需要 02 篇正式回应 |
| 07 | `chat_outbox` 去重键粒度够不够 | 建议:MVP 先用 `(issue_id, event_kind)` 两维,「同一 issue 先后开两个 MR」这类边界情况留到真的撞见再加维度 |
| 07 | 通知失败重试节律 | 建议:退避 3 次(如 5min/30min/2h),之后转「需要人手动重试」,不要无限重试刷日志 |
| 07 | 群摘要截断上限具体数字 | 建议:2000 字符或 80 行(取先到者),图片类占位不整条丢弃 |
| 08 | 「周信号快照」存不存 | 建议:不存(08 篇本身倾向这个方向),「过去 8 周灯色时间线」这类需求等真的有人要再评估要不要引入快照 |
| 08 | 「进展」判据 (a) 够不够严(要不要扩大到较弱信号)| 建议:先只认「真实提交」,不扩大——覆盖面广的弱信号更容易被误判为「有进展」,与「难造假」的产品哲学冲突 |
| 08 | ⑤块(仓统计)刷新频率要不要加缓存+定时后台刷新 | 建议:先不加,等真实耗时数据说话 |
| 08 | 北极星「metric 化」现在做还是缓一步 | 建议:现在做(02 篇本来就在设计 `issue_metric`/`role='north_star'` 这条链,缓一步会让北极星卡在「没法反查活」的半成品状态,和其它两层指标体验不一致) |
| 08 | health 判据 (a) 要不要给接入第一周豁免 | 建议:要豁免——接入第一周大概率只做了运作活③,不豁免会让新项目第一周就显示黄/红,打击接入体验,与「先对后亮」精神也不冲突(不是假装绿,是给「还没到第一个周计划」一个专属的中性态) |
| 09 | 历史回填里项目群历史具体产出什么 | 建议:按 09 篇暂定方案(只做参考、不产出独立文件)定案,不需要额外产出 |
| 09 | 指标候选落点(`docs/metrics.md` 候选小节)缺格式规范 | 建议:03 篇下一版补一个最小格式(标题+一行表格即可,不需要复杂结构) |
| 09 | 旧技能文件(`north-star-discovery`/`metrics-binding`)何时删 | 建议:同一次改动删除(不留并存版本),按「不为向后兼容留旧路径」原则 |
| 09 | 系统提示词要不要加「无人在场」那一句 | 建议:加,措辞可以更短:「这次会话可能无人在场,能做的先做,拿不准的写进报告」 |
| 09 | 运作活③纯模板路径「评审中」探测延迟 | 建议:MVP 先接受 5 分钟兜底轮询的延迟,不为这条边路径单独加速 |

---

## 建议我优先修的前 10 条(按影响排序)

1. **2.8** — `agent` 表怎么处理,02 篇「软退役」vs 04 篇「硬删」两个相反建议,必须先拍板再开工(牵连 2.9、01 的开放问题 5、04 的开放问题 2)。
2. **2.1** — `issue_metric` 关联表 vs `issue.metric_ids` JSON 列,02 篇自己标了开放问题但 06/08 已经绕过它直接用了 JSON 方案,三篇不统一。
3. **2.2** — `release_issue` 关联表 vs `release.issue_ids` JSON 列,同一模式重演,和 2.1 一起拍板。
4. **2.3** — `release` 表的 `released_at`(INTEGER)vs `date`(TEXT)列名与类型冲突,三篇不统一。
5. **1.1/1.2/1.3/1.4/1.5** — 01 篇作为「命令/事件总表」已经明显滞后,建议做一次专门的回填 pass,把 03/04/05/06/07/08 各自新增和改名的命令/事件补全,否则「总表」名不副实。
6. **2.5** — `chat_outbox` 的 `event_kind`/`subject_kind` vs `event_type`/`issue_id` 列名分裂,连带 1.7 的命令签名分裂。
7. **6.1.1** — 知识库屏(`kb`)没有专篇设计,母文档承诺的六入口缺一个,补一篇或补一节。
8. **2.4** — `issue.sort_order` 类型 INTEGER vs REAL,建议直接采纳 06 篇(有预研背书)。
9. **4.1/1.10** — `EditProjectCard` 命令签名带已被母文档删除的 `kind` 字段,是九篇里唯一一处「设计开倒车」,容易被下一步写代码的人直接照抄进真实代码。
10. **3.2** — 大量「尚未成稿/未写」的过时标注,虽然本身不是设计错误,但会让读者误以为某些冲突点(尤其 1/2 节列出的那些)还没写、不用核对,建议批量更新掉,降低漏审风险。

---

## 复核期间发现的并发改动(非本复核所改,特此记录)

写这份复核的过程中,`mvp-blueprint-draft.md`、`standard-module-draft.md`、`docs/v4-prototype/README.md`、`09-ops-workflows.md` 四个文件被**另一个进程**(不是这次复核)同步改动了,复核收尾时用 `git status`/`git diff`/重新 `grep` 发现的,记录如下,避免你照着下面这两条已经不存在的问题去找:

- **6.2.3 条已经被改掉**:当时读到的 `09-ops-workflows.md` §6(开放问题)只有 5 条,不包含 §3.4 提到的「02 篇 `workflow_credit` 与 04 篇 `skill_package` 战绩口径分歧」;复核收尾时重新核对,现在 §6 第 2 条已经补上了正是这条分歧(原话「02 篇 `workflow_credit` 表与 04 篇 `skill_package`/`skill` 战绩列口径不一致…」)。6.2.3 条给出的建议已经被采纳,**不用再改**。
- **母文档「群历史算不算回填原料」这处口径分歧也已收敛**:`mvp-blueprint-draft.md` §2.6 原来把「项目群历史」列为老项目回填的四种原料之一,与 `03-standard-and-backfill.md` §4「群历史不进仓不进库,和历史回填这条产线不搭界」的口径不一致——这条分歧本来就是复核时 `09-ops-workflows.md` §6 第 1 条自己点出来的(它当时也确实是一处真实的母文档冲突,本该被本复核第 4 节收录,是这次复核的一个遗漏);现在母文档已经改成「三种原料」并加了「群历史不当回填原料」的说明,与 03/09 两篇口径一致,**也不用再改**。

以上两条只是提醒「复核完成时,源文档还在被同步改」,不代表本文件第 1-7 节和前 10 条清单里的其余条目失真——已经逐条重新 `grep` 抽查过 2.1/2.2/2.3/2.4/2.5/2.8/3.2/6.1.1 及 1.10/4.1 的 `EditProjectCard`,现状与文中描述一致,仍然成立。如果你看到这份复核时,设计稿又经过了几轮改动,稳妥做法还是按文件开头写的——重新核对一遍行号和内容,不要直接照抄清单动手。

---

_本复核未改动 `design/`、`research/`、`mvp-blueprint-draft.md`、`standard-module-draft.md` 任何一个文件,只新增了这一份文件(以及收尾时补充的这一节说明)。行号对应 2026-08-19 复核时刻的文件内容,设计稿改动后需要重新核对。_
