# 二次握手清单(2026-08-20 早):我替你做的判断 → 用户已回三轮,处置见首节

> **30 秒导读**:这是昨夜按专家评审改文档、开子代理做详细设计之后,**需要你拍板的事**——都是我在没法问你时先按「默认答案」做下去的判断。给你看的方式:每条一个编号、一句问题、我的默认答案、备选。**你回复时只需写编号 + 「默认」或改法**(例:「1 默认,4 改成 B,7 先不定」),我据此改文档。全部默认也行,回一句「全部默认」。已定的决策不在这里重开(见母文档 §11);这里只有新判断与设计期冒出来的分叉。整体阅读顺序见文末。

## 用户回复(2026-08-20)与处置——已全部落进文档

| # | 用户怎么说 | 处置 |
|---|---|---|
| 1 | 拖动要**统一**,都能拖;动作性的拖完弹窗二次确认;卡面更清爽 | 改:所有列都能拖,排期直接生效、状态动作弹窗确认、不合法转移弹回、卡面不摆按钮(待拍-25 改写;设计 06、高保真、§3.5 同步)|
| 2 | 是,agent 暂不作为单体维护:小事 skill 干,大事 workflow 带着 agent 和脚本干 | 按默认(`agent` 表硬删)|
| 3 | 默认 **mattpocock-skills**;workflow 自产文档与知识库重叠怎么办——规范先做好 buddy 必需的基础限制,其它不过度设计,看第一版实践 | 改默认;第 4 站加「workflow 自产文档不额外管」一段(规范稿第 6 类同步)|
| 4 | 回填不主动喂群历史 | 已一致 |
| 5 | 发版本 = 做好分支归纳 + 项目管理信息记录? | 是:把本周完成的活归到一个版本号下 + 记进仓(`docs/releases.md` + 可选 tag);活的分支早在各自 MR 合入时归到主干,发版不做分支合并、不做打包(第 5 站补一句)|
| 6 | 老项目回填是不是就是初次纳入时的资产盘点?盘点不只代码;微重构可做可不做,要不要分开? | 改:运作活②改名**「资产盘点」**,范围 = 仓内全部资产;**微重构拆出**,只作为盘点产出的建议活草稿、人勾选才建;**历史回填 = 同一盘点 workflow 的首次模式**(运作活③第三步跑它)|
| 7 | 任何提代码的都走 issue + PR/MR | 是,规矩①无例外(含 buddy 自己写仓;唯一例外空仓首提交)|
| B1 | 按推荐 | crate `app-shell`、1500 行阻断、`issue_metric` 关联表、health 三判据不豁免不存快照、运作活①不拆、②周五晚 |
| B2 | 库是本地还是远端?多人看一个项目,本地信息别人看不到? | 库是**本机**的(仓是正本、库只记账与推导,不是云服务);别人看不到的只有过程数据(运行记录、战绩、本机采集 / 手填观测),所以两台机器的灯可能不同;MVP 不做云库 / 同步;加一小步:运作活①把「本周指标读数」写进周计划文件随 MR 进仓(新 待拍-29)|
| C1 | WeLink 由内部增量开发补函数实现 | 不管;接口与对接骨架在设计 07 / 预研 chat-group §5 |
| C2 | 试点连不到 codehub,就用 buddy 自己 | §8 第 1 条改:试点 = buddy 自己的仓(GitHub);codehub 接入留内部试用时验 |

**第二轮回复(2026-08-20,用户看完第五轮后)——已落进文档**

| # | 用户怎么说 | 处置 |
|---|---|---|
| 六-1 | B2「值得商榷」:信息是否都在库里、哪些在本地磁盘 / 数据库,要**盘点一下**;存就是为了取,没价值的不要复杂化;表名列名不继承老数据 | 改:母文档 §6 整块重写成**信息住哪盘点表**(仓是正本 / 库只放过程数据与推导缓存 / 没人取的不存)——`release` / `week_plan` / `issue_metric` 表、`project` 五个副本列、`docs/plan/history.md` 全部取消,`issue` 只加八列;**V4 不兼容老库**(新库文件、schema 从新写、不写迁移)= 待拍-29 改写 + 新 待拍-30;设计 02/06/07/08/09 同步 |
| 六-2 | WeLink 一旦登录基本永久、可探活;探活函数留位待填;「测一下」加 welink-cli 探活;设置不要登录按钮,不归我们管 | 改:设置去掉「聊天工具登录」,「本机环境 · 测一下」加 welink-cli 探活项(函数留位)= 新 待拍-31;高保真 v6、设计 07 同步 |
| 六-3 | 总览不要多一坨「历史运作(回填)」;期望资产盘点发现老项目没有周计划 md / 发版本 md 就**回溯补起来**,总览 / 计划 UI 不特殊处理,本身对照资产渲染 | 改:回填产出 = **同格式**的历史周文件 `docs/plan/YYYY-Www.md`(`origin: backfill`)+ `docs/releases.md` 历史版本行;总览⑧块删除、`history.md` 取消;历史周出现在计划屏周列表、历史版本出现在总览发版记录,只带回填徽记(待拍-27 改写;高保真 v6、设计 03/06/08/09 同步)|
| 六-4 | 设计稿太大不看;按聊过对齐的做;控制代码量、做好模块规划 | 记下:开工守 design/01 的一屏一模块 + 1500 行阻断;任务书里写代码量预算 |
| 六-5 | 不反馈的按默认;过于不好确认的按简单 / 规范的来;非核心 MVP 小功能可先不管 | 记下:REVIEW §7 剩余开放问题全按建议答案;任务书列「先不管」清单 |
| 六-6 | 设计期文档乱,落地时要修正;md 维护按块处理——大胆删一小块、按实际重写,不局部加 | 记下并照做:本轮 §6 就是整块重写;开工后每刀结束按块重写对应 design 篇,不追加补丁 |
| 六-7 | 都设计完了,别被老项目过度干扰 | 记下:新壳按设计建,V3 只抄视觉与好组件,不迁就旧结构 |
| 六-8 | 三阶段一个个开发;开发可正常开 subagent;验证正常验证——用户旅程全面点击由用户次日人工做并反馈,不要我漫长点击截图决策 | 记下 → 任务书:一晚一刀;E2E = 指挥器 + sqlite 读回 + 深链 stderr,不做 computer-use 点击巡航 |

**第三轮回复(2026-08-20,用户逐条盘点数据库)——已落进母文档 §6 与 design/**

用户对着「哪些存仓 / 哪些存库 / 哪些在磁盘」的盘点逐张表质询,结论:**库从 20 张表掉到 4 张**。

| # | 用户怎么说 | 处置 |
|---|---|---|
| 七-1 | `observation` 有什么用?采集脚本能采历史;就算记录也记到**仓**里(数据量不大);指标粒度「能采一周的就能采一天的,实践看」 | 删 `observation`:可重算的每次现算,不可重算的写进周计划文件「本周指标读数」段随 MR 进仓(顺带解决多机灯不一致);粒度不写死 |
| 七-2 | `metric` 有用么,信息不冗余么 | 删:定义在 `.bw/metrics.toml`、读数在周计划文件,库里那份是第三副本 |
| 七-3 | 本地有 `issue` 表,别人看到的信息不就不一致?不能给远端 issue 加标签 / 状态么?codehub 有标签 | 排期正本改成**周计划文件的活清单**(顺序 / 类别 / 工具 / workflow / 指标写在行上,走 MR,别人 pull 就看到);远端标签**打上但只是镜像**;`issue` 表降级成本机缓存 |
| 七-4 | `workflow_run` 还有用么?claude cli 多轮,耗时成败怎么判定 | 删:判定本来就弱;活干没干成看**远端 MR 合没合入**;会话线索由 `claude_conversation` 一张表接住。**代价**:「运行成败自动入账」这条铁律在 V4 没有持久载体,用户知情 |
| 七-5 | `artifact` 为什么要,git 指令就能看到 | 删:`git log --name-only` 就是产物登记 |
| 七-6 | `cron_task` + `connector` 之前就觉得复杂,重新思考 | 都删:定时写在 `issue-policy.toml` 节律段(「本周建过没有」查活)、远端 = git remote + 钥匙串令牌、探活是即时结果 |
| 七-7 | skill / workflow 统计不是从 issue 属性就能算?技能不是都在规范的仓目录下、扫描就得到? | 删 `skill` / `skill_file` / `skill_stage`:扫 `.claude/skills/` + 从活的 `workflow` 属性现算 |
| 七-8 | `workflow_credit` 暂时不要,不统计战绩,都不清楚什么是成功什么是失败 | 不建 |
| 七-9 | `chat_outbox` 鸡肋,别搞,实际用了再说 | 不建;代价:不做发送去重,极端情况重发一条 |
| 七-10 | 导入之后就是项目自己的,放代码仓目录,不要记录在表里;预置的随 buddy 在 buddy 里面 | 不建 `skill_package`;**预置包随 buddy 出厂,铺底时复制进项目仓 `.claude/skills/`**(Claude CLI 才加载得到,扫项目仓即得)= 新 待拍-32 |
| 七-11 | 后续三块(磁盘 / 分歧点 / 代价)不看了,前十条你应该理解我意思 | 按前十条推到底:库只剩 `project`(定位 + 项目墙显示缓存)/ `issue`(远端缓存)/ `claude_conversation` / `app_meta` |
| 七-12 | 数据库变动周边文档也要变;这波结束 commit + 合 PR;给一个 prompt 去新窗口挂今晚任务;今晚争取全做完,没完成明天再等 | 母文档 §6 整块重写、design 十二篇同步、`12-build-plan.md` 改成一次挂连着做 A→B→C |

---

## A · 专家反馈落法上的判断(影响产品行为)

| # | 问题 | 我的默认答案 | 备选 / 说明 |
|---|---|---|---|
| 1 | 看板拖拽做到什么程度? | **只用于排期**:待办池 ⇄ 待办 = 排进 / 移出本周,列内拖动排序;进行中 / 评审中 / 已完成 / 阻塞 四列不能拖入,松手提示「用卡片上的按钮」。列头下写定义:待办池 = 未排进任何一周,待办 = 已排进本周等开工 | B:连状态也能拖(拖到「已完成」= 点完成)。我不推荐:▶开工要真起 agent 会话、✓完成是铁律里人显式点的那一下,误拖一下代价大;预研证实 Dioxus 下技术上都能做,差别只在产品选择 |
| 2 | 「去掉 agent」去到什么程度? | **buddy 不再维护任何 agent 名单**(配置屏没有 agent 表;V2 的「原型师 / 构建师」退场);agent 由 workflow 包自己在 SKILL.md 里调度;**战绩改记在 workflow / 技能上**(用了几次、挂在它下面的活成败几何),「胜率由真战绩算」这条铁律不变、只是主体换了 | B:保留一张只读的 agent 表给老数据看。若选默认,`agent` 表怎么退:B1 直接 DROP(仓的原则「不为向后兼容留旧路径」)/ B2 冻结只读不删。**我默认 B1**,但不可逆,请你点头 |
| 3 | 构建类活的默认 workflow 选哪个? | **superpowers**(高保真与映射表里默认它),mattpocock-skills 在下拉里可换 | B:mattpocock-skills 为默认(专家点名它是「完整的开发项目工作流」)。两套都是完整开发工作流,选一个当默认只是起点,活上随时可换 |
| 4 | 项目群在哪里配? | **接入两卡不加字段**;总览名片有「项目群」一行,空着显示「未配 · 配置」一键补;群号写仓正本 `.bw/project.toml [chat]`;聊天工具**登录**在设置(本机);默认同步三类事到群:评审中 / 已合入 / 发版 | B:接入卡二加一个可选「项目群」字段(多一个字段,但省一次回头) |
| 5 | 群历史用来干什么、进哪里? | 只做两件事:①运作活①定计划前拉**上周**群消息生成本机摘要喂给 agent(不进仓、不进库、用完可删);②回填老项目时**不**把群历史当原料(设计 03 与 09 已按此写;母文档第 0 站原来把「群历史」列进了回填原料,已改成一致) | B:回填也读群历史(把「群里讨论过什么」写进 PROJECT.md 背景)。我不推荐:隐私边界不清、也没法复算 |
| 6 | 发版本怎么走? | **轻量活 + MR**:计划屏三步(选活 → 版本号 → 确认)→ buddy 建活「发版本 vX」+ MR(`docs/releases.md` 追加一行)→ 有权限的人「合入并完成」一下 → 合入时写 release 行、可选打 tag。原因:母文档规矩①「凡写仓都是活 + MR」,之前那行写「纯 UI」自相矛盾,设计 06 抓出来后我按规矩①改了 | B:发版直接写仓不走 MR(少一下,但规矩①要开例外) |
| 7 | 老项目回填放哪、怎么算账? | **运作活③的第三步**(探测到仓有历史才有),与写核心件 / 合并调整同一个 MR;产物全标「回填」;远端 issue 同步成 issue 行 `origin=backfill`、**不算任何战绩**;总览多「历史运作(回填)」块,**不点灯**;唯一允许流入 health 的是「上周有交付」——从 git 合入记录推(那也是真数据) | B:回填单独一张活、单独一个 MR(评审更清楚,但接入多一步) |
| 8 | 映射表(`.bw/issue-policy.toml`)调参也走活 + MR 吗? | **走**(规矩①不开例外;有权限「合入并完成」一下) | B:映射调参直接写本机 / 直推,理由是可能高频。设计 04 提出的疑问,请你定 |

## B · 设计期的技术判断(影响怎么建)

| # | 问题 | 我的默认答案 | 备选 / 说明 |
|---|---|---|---|
| 9 | 新壳 crate 叫什么? | **`crates/app-shell`**(不把版本号焊进名字;仓根 `standard/` 放规范正本) | B:`crates/app-v4` |
| 10 | 单文件行数守卫怎么定? | **1500 行,CI 阻断**(和「内核不依赖 UI」守卫并列;只查新壳不追溯旧壳) | B:只报警不阻断 / 上限改 2000 |
| 11 | 活 ↔ 指标的关系怎么存? | **关联表 `issue_metric`**(能按指标反查活;有先例) | B:`issue.metric_ids` JSON 一列(简单) |
| 12 | health「进展」判据多严? | (a)本周有周目标**且**有真实 git 提交(靠运行记录前后 head 变化,不靠状态被点一下);(b)业务指标 7 天内有真观测;(c)上周有合入或发版。三真绿 / 缺一黄 / 连续两周无进展或任一指标红 = 红 / 全无数据 = 灰;灯读时现算不存 | B1:接入第一周豁免(只做了运作活也不判黄);B2:进展也认「MR 有新评论 / 活状态前进」;B3:存周信号快照以便看「过去 8 周灯色」。**我默认三个 B 都不做**,先跑起来看 |
| 13 | 运作活①要不要拆成两次会话(更新指标 / 定计划)? | **不拆**,一次会话按顺序做(母文档已定),会话里 agent 会在两段之间停下来问一次 | B:拆两张活 |
| 14 | 运作活②默认几点自动开工? | **周五晚(如 20:00,本机时区)**,配置里可改;错过则下次启动补建 | B:周日晚 / 周一早 |

## C · 需要你提供的信息(不是判断)

| # | 事 | 说明 |
|---|---|---|
| 15 | **WeLink 对接人与时点** | 设计 07 与预研 `research/chat-group.md` §5 已写了给同事的对接骨架(要实现的两个函数、错误约定、模块位置、不用真群怎么自测);公开文档查不到 WeLink 参数级细节,需同事对着真环境核实 |
| 16 | **试点项目选谁**(母文档 §8:一个真实内部项目跑两个完整周循环;第四轮加:至少一个有历史的老仓验回填) | 你在内部候选里选;有老仓最好 |
| 17 | **Windows 机器**(安装包 `0.4.0-v4` 在同事机器上装得上、跑得起;拖拽在 Windows 需要壳里开一个开关,预研已核) | 谁的机器、什么时候 |

## D · 你会看到的东西(阅读顺序)

1. **高保真 v4**:本机已起静态服务 → 浏览器开 `http://127.0.0.1:8766/hifi/index.html`(或直接双击 `docs/v4-prototype/hifi/index.html`);顶栏「重置」可回到样例初态。变化点:配置屏三列映射 + workflow / skill 表;计划屏拖一次待办池 → 待办;总览名片「项目群 · 配置」与最底下「历史运作(回填)」;通知「合入并完成」;发版本三步后去通知合入。规格与「专家意见 → v4 决定」表在 [`../hifi/README.md`](../hifi/README.md) §0。
2. **母文档第四轮**:[`../mvp-blueprint-draft.md`](../mvp-blueprint-draft.md) 30 秒导读第一段列了改动位置;新增 §3.5(每站几下)与 §11 待拍-24~28。
3. **详细设计**:[`README.md`](README.md)(目录 + 七节模板)→ 01 架构 → 02 数据与文件 → 03 铺底与回填 → 04 工具与 workflow → 05 会话屏 → 06 计划屏 → 07 通知与项目群 → 08 总览推导 → 09 运作活剧本 → 10 验收(在写)。每篇第 6 节是开放问题;交叉复核清单 [`REVIEW-2026-08-19.md`](REVIEW-2026-08-19.md) §7 把九篇 44 条开放问题汇成一张表(每条带建议默认答案),前 10 条真冲突(agent 表退法、活↔指标关联表、发版表时间列、命令总表滞后等)已按本清单的默认答案修进各篇——**上面 A/B 两节没列的开放问题,默认按 REVIEW §7 的建议答案走**,你若不同意某条,报编号即可。
4. **预研四篇**:[`../research/`](../research/) workflow-skill-packages / chat-group / legacy-backfill(+ buddy 自己仓的真跑样例)/ kanban-drag-dioxus。

**状态**:草案第四轮、四篇预研、高保真 v4 已 commit 在分支 `claude/v4-round4-expert-feedback`(未 push、未开 PR);设计稿 01–10 等交叉复核修完再 commit。**没有动任何 crates/ 代码**——按你的交代,先把设计与技术串好。
