# buddy 指南 · 填写规范

> 本文件是 `buddy-guide.html` 原型的**填写规范**。HTML 是可复用的原型样式模板，本规范说明两本的组织方式、每屏结构模式、组件用法、如何扩展。后续实践推进时照着填即可，无需改样式。**未来这是整个产品指南的种子。**

---

## 1. 两本的组织方式

两本用**不同的维度**组织，互为索引：

- **使用指导** = 按**界面操作阶段**组织。实践推进到哪、能见到哪，就写到哪。不预先猜未来阶段。
- **维护指南** = 按**跨阶段机制**组织，站在高 level 汇总。一个机制往往被多个阶段共用（如 connector 同时服务纳入与采数；Issue 调度同时服务找指标与绑数据；预制技能同时被找指标/绑数据加载），所以按机制成章，并在章首标注「用到此机制的阶段」。

> 为什么不按阶段切维护指南？因为同一个机制会被多个阶段共用，按阶段切会把它拆散到多屏、重复盘点。按机制汇总才是一份能长期生长的产品指南。

### 当前已写入的内容

V1 产品化按**三个窗口**推进（W1 纳入项目 / W2 找指标·绑数据 / W3 总览重构），**各窗口只填自己实践得来的内容**。截图在本地 `docs/guide/img/`（gitignore）。

使用指导（操作阶段屏）：

| # | 阶段 | 窗口 | 状态 |
|---|---|---|---|
| 00 | 前置准备 | 第一包 | ✅ 终端用户跑 `BuildersWorkbench-Setup.exe`；装包前必有 Claude Code / git / WebView2 Runtime；`cargo run` 标开发 |
| 01 | 纳入项目 | W1 | ✅ 实体 + 截图（V2-② Phase A + IntentUX + Issue 读回：首到 PR 合入 / 后来者只读预填意图 + 拉 open Issue） |
| 02 | 找指标 | W2 | ✅ 图文友好 + 截图（计划≠有数；嵌终端首段≠系统提示词；V2-① 注入已实践确认） |
| 03 | 绑数据 | W2 | ✅ 图文友好 + 截图（和模型核对三件事；一条每日采集跑全部脚本） |
| 04 | 总览呈现 | W3 | ✅ 图文友好 + 截图（灰卡诚实；立即采集补近 30 天仓史；删项目清库+自建工作区） |
| 05 | 建活 · 选技能 | V1 遗漏对齐 | ✅ 图文友好；截图 defer（创建流实跑 2026-08-06；终端会话重构已落地，「▶跑后终端」截图等收口后补） |

维护指南（跨阶段机制章）：

| # | 机制章 | 状态 | 口径 |
|---|---|---|---|
| 01 | 架构与铁律 | ✅ 图优先实体 | 分层栈 + 两不可妥协 + 四铁律 + 事实源落点表 |
| 02 | Hub 全局库 | ✅ 图优先 + 实跑 | 三层 + CollectMetrics 全 script + connectors.toml 契约表 + 删项目清工作区 |
| 03 | Issue 与调度 | ✅ 图优先实体 | 状态机 + 一活一会话 + issue 字段 + V2-②-I 远端 open 读回/收起 |
| 04 | 技能与 Prompt | ✅ 图优先实体 | 衔接层不可换 / skill 可换；可见位置 prompt vs 不可见 `--append-system-prompt`（V2-① 实跑） |
| 05 | 执行与证据 | ✅ 图优先 + 实跑 | 两轨 + Done 时统一 pull/Sync + live PTY / 产物快照 |
| 06 | 指标与健康 | ✅ 图优先 + 实跑 | 计划≠有数 + Sync* 装表 + 采数链 + 近 30 天 history + 总览分层 |

**使用指导读者**：最终用户（Builder）。只写「点什么、会看到什么、得到什么」；坑用界面结果描述（灰卡、侧栏没多出连接器、报错提示），**不**写解析器/字段名/P 编号/「静默失败」「响亮报错」「装表」等开发话术。机制细节用 `.linker` 指维护指南。

**维护指南读者与 altitude**：读者 = 组内要改/维护 buddy 的开发。人读是为了理解与决策，再驱动 AI 大规模读代码。正文写可理解的机制终态（是什么 / 怎么运转 / 边界），不写窗口流水、Phase、偏差/defer、决策过程；概念卡 `.src` 可指模块/目录级路径。未解问题进 `docs/LEFTOVERS.md`，不在本章堆过程文。

> 跨阶段通用基础（命令门禁、设计 token）暂未单独成屏。等某机制章需要它时再补，或并入「架构与铁律」章。不提前猜。

---

## 2. 使用指导 · 操作阶段屏结构

每屏四段式：**操作 → 背后 → 得到什么**（+ callout 坑 + linker + foot）。「背后」讲这一步做完 buddy 自动干了啥（白话），「得到什么」讲用户手上多了啥、关注啥。

```
<eyebrow>   使用指导 · 阶段 0X
<h1.disp>   阶段名
<lede>      一句话定位（用户视角，讲心智不是讲机制）
<h2.sec> 操作        → .ops 有序步骤
<img>       截图按步放（每步下面放该步的截图，不堆在末尾）
<h2.sec> 背后        → .res 三轴白话（见下）
<h2.sec> 得到什么    → .res 用户手上多了啥
<callout>           可选：该阶段坑/边界
<p.linker>          指向对应维护指南机制章
<.foot>             事实源脚注
```

### 必填内容
- **操作**：界面上的真实操作步骤，按序号写，能照着点。**截图按步放**——每步下面放对应截图，不在末尾堆连续截图；本质上同一 UI 界面的步可共用一张。
- **背后**：点完关键操作（如「确认」）buddy **一次性**在背后干了啥。用**白话三轴**组织，**不按 `Command::` 代码命令、不用 CRUD 黑话**：
  - **对远端代码仓**（GitHub/CodeHub）：建仓 / 开 Issue / 推提交……
  - **对用户的机器**：clone / 写文件 / 探活……
  - **对 buddy 数据库**：落 project 行 / 建阶段 / 挂连接器 / 排定时器 / 建 Issue / 种 metric……
  - 给个地址 + 一笔核心信息描述即可，代码级细节（具体表名/字段/函数）放维护指南机制章，这里只讲用户能感知的。
- **得到什么**：用户建完手上多了啥、该关注啥（不是"看到界面呈现"那种流水账，是"你得到了什么能力/资产"）。

### 规范
- **窗口边界**：只填本窗口实践得来的阶段屏。非本窗口的阶段屏**只留章节名占位**（`<h1>` + 一句「待 X 窗口棒补」+ `.foot` 指 `plan/08`），不预先填内容。
- 只写已实践/可见的，不猜、不发明指标。
- 表名、文件名、命令、状态值用 `code` 包裹（真实值）。
- 每屏末 `.foot` 标事实源（代码仓文件名用 `code`）。
- `.linker` 指向该阶段涉及的维护指南机制章（可多个，用 `go('mX')`）。

---

## 3. 维护指南 · 机制章结构

每章 = 一个跨阶段机制。**图优先**：先让人看懂机制与落点，再决定让 AI 读哪块代码。概念卡不是正文主角。

```
<eyebrow>   维护指南 · 机制 0X
<h1.disp>   机制名
<lede>      一句话定位 + 跨阶段关系
<.stages>   用到此机制的阶段徽
<h2.sec>    主图（.diagram / .pipe / .stack / .split2）
            短注（.bullets / .note-line）
            必要时：.ktable 关键字段 · .ex 例子 · .where 落点
<.foot>     指回其它章 / 补充
```

### 可读性组件（维护指南优先用）

| class | 用途 |
|---|---|
| `.diagram` + `.cap` | 主图外框 + 图注 |
| `.stack` + `.ly` | 分层栈（架构） |
| `.pipe` + `.box` + `.arr` | 横向流程（可 `.lock` / `.warn`） |
| `.split2` + `.pane` | 两栏对照 |
| `.ktable` | 关键表/字段/目录 |
| `.ex` + `.eh` / `.ed` | 例子 |
| `.where` | 实现落点（目录 / crate） |
| `.bullets` | 图下短注 |
| `.cc` | 降级：仅当需要词条索引时用，勿堆成长文 |

### 规范
- **机制终态 + 当前实现感**：读完应明白「怎么运转」和「大概落在哪」（目录/表/关键字段）。不写窗口流水、Phase、偏差辩论。
- **图 > 表 > 短注 > 长卡**：能用流程图/对照栏就不用长段落；字段用表，场景用 Example。
- 一章一机制，章首 `.stages` 必填。
- 跨章概念放主机制章，其它章 linker 指回。
- 未建能力一句「未建，留口」。
- altitude：系统×行为；`.where` 指到模块/目录即可，不堆函数名行号。
- 允许为可读性增加本表组件的少量 CSS（暖纸 token 内）。

---

## 4. 组件清单（class 名 + 用途）

| class | 用于 | 出现位置 |
|---|---|---|
| `.ops` + `.li` | 操作有序步骤（带序号圆点） | 使用指导·操作 |
| `.res` + `ul/li` | 结果清单（落库与产出） | 使用指导·结果 |
| `.see` | 看到（界面呈现） | 使用指导·看到 |
| `.stages` + `.s` | 用到此机制的阶段徽 | 维护指南·章首 |
| `.diagram` / `.stack` / `.pipe` / `.split2` | 主图（分层/流程/对照） | 维护指南 |
| `.ktable` | 关键表字段 / 目录对照 | 维护指南 |
| `.ex` / `.where` / `.bullets` | 例子 / 落点 / 图下短注 | 维护指南 |
| `.cc` + `.h/.d/.src` | 概念卡（词条索引，非正文主角） | 维护指南（少用） |
| `.linker` | 跳转到对应维护机制章 | 使用指导屏末 |
| `.callout` `.warn/.note/.danger` | 提示框（坑/说明/铁律） | 通用 |
| `.cards` + `.card` | 卡片网格 | 通用 |
| `.flow` + `.node` | 横向流程链 | 通用 |
| `.lights` + `.light` | 健康灯四态 | 总览呈现 |
| `.rules` + `.rule` | 铁律/控制点四宫格 | 架构与铁律 / 概览 |
| `.checklist` + `.chk` | 安装清单 | 前置准备 |
| `pre.code` | 代码块 | 前置准备等 |
| `.foot` | 事实源脚注 | 每屏末 |

> 新内容优先复用上表组件；维护指南为可读性可增 `.diagram` 等组件（须落在暖纸 token 内）。禁止为单页发明第三套视觉语言。

---

## 5. 扩展规范

两本用不同维度，所以**加新阶段**和**加新机制**是两件独立的事，互不强制绑定：

### A. 加新操作阶段（使用指导）
实践推进到新阶段（如「阶段交棒」「Hub 全局库管理」）：
1. 复制一个现有阶段 `<section class="screen" id="uX" data-title="阶段名" data-sec-name="使用指导">`，按三段式填。
2. 侧栏「使用指导」组加一行 `<div class="nav-item" data-screen="uX">`。
3. 若该阶段引入了**新机制**，去 B；否则只用 `.linker` 指回已有机制章。

### B. 加新机制章（维护指南）
某机制跨多个阶段、值得独立汇总（如「阶段与交棒」「autopilot 定时」「蒸馏复利」单独深化）：
1. 复制一个现有机制章 `<section class="screen" id="mX" data-title="机制名" data-sec-name="维护指南">`，填 `.stages` + 概念卡。
2. 侧栏「维护指南」组加一行 `<div class="nav-item" data-screen="mX">`。

`data-screen` 指向 `id`，JS 自动接管切换 + localStorage 持久化（key `buddy-guide-screen`）。无需改 JS。

---

## 6. 内容规范

- **使用指导窗口边界**：使用指导按实践窗口推进，**只填本窗口实践得来的操作屏**。非本窗口的阶段屏只留章节名占位、标「待 X 窗口棒补」+ `.foot` 指 `plan/08`。
- **维护指南机制终态**：m1–m6 写正式机制说明，不写过程文；遗留/未决统一进 `docs/v1-prototype/LEFTOVERS.md`，不在指南正文堆。
- **真实实践优先**：使用指导只写已实践/已可见的操作；维护指南只写已落地机制，未建留口不假装。
- **使用指导用用户语言**：按钮名、界面结果、白话后果；开发契约/字段/缺陷编号只进维护指南。
- **背后事务用白话三轴**（对远端代码仓 / 对用户机器 / 对 buddy 数据库），不按 `Command::` 命令、不用 CRUD 黑话；机制细节留维护指南。
- **事实源脚注**：每屏末 `.foot` 必填；使用指导脚注宜短（实跑/设计文档即可）；维护指南可标模块路径。
- **术语统一**：以 `CONTEXT.md` 为唯一规范词表（Builder / 队友 / 阶段 / 交棒 / Issue / 观测 / 指标 / 信号 / 北极星 等），不自造同义词。
- **诚实口径**：无数据 = Unknown ≠ 绿；Done 永不自动；manual 带徽；数字可 sqlite3 查证。涉及健康处必须重申。
- **跨章概念不重复**：放主机制章，其它章指回。

---

## 7. 视觉规范摘要（buddy 自有设计系统）

- **色**：暖纸底 `#EFEBE2` / clay 主色 `#C5654A`（文本用深调 `#A84A30`）；信号四态：Green 隐身、Amber 黄、Red 红、Unknown 灰。绿色不出声，只有红黄出声。
- **字**：Noto Serif SC（display）/ Noto Sans SC（body）/ JetBrains Mono（code·数字）。
- **字距**：ALL CAPS ≥ 0.06em；display 标题 -0.02em；body 行高 1.6。
- **accent 节制**：clay 作结构色（序号/eyebrow/激活态/阶段徽），不泛用为 CTA。
- token 全在 `:root`，改一处全局生效。
