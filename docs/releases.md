# 版本登记（出包与运作）

> **30 秒导读**：给推广和出包的人看。一行一个已出或正在用的版本：**版本号、出包日、这一版是什么、修过什么**。安装器 `AppVersion` 必须和这里对齐。当前：V3 修 bug，V4 在规划。边界见 [`doc-boundaries.md`](doc-boundaries.md)。

仓内 `Cargo.toml` 工作区版本仍是 `0.0.0`，**对外以安装器版本为准**。安装脚本在仓外 `D:\2026\buddy-setup`，不进本仓。

## 现在用的

| 版本号 | 出包日 | 阶段 | 这一版是什么 | 相对上一版多了 / 修了什么 |
|---|---|---|---|---|
| **0.3.0-v3** | 2026-08-14 首包；2026-08-17 起按 V3-use-fix 重出 | V3 修 bug / 推广 | 第一份给同事的 Windows 安装包（`BuildersWorkbench-Setup.exe`）+ 开发包 | **能力**：原型进度内嵌本机 Open Design（V3-OD-embed）。**修**：结束页正文、Windows 子系统、`WebView2Loader.dll`、藏 cmd 闪窗、唤醒环境变量带 `=` 被拒。**2026-08-17 起还要打进重出包**：安装器/探测认 `claude.cmd`、`.cmd` 开工走 `cmd.exe /c`、创建流拉 999 / 下拉画 30 / 可搜索、程序图标。设计：[`v3-prototype/onboard-list-and-claude-resolve.md`](v3-prototype/onboard-list-and-claude-resolve.md) |

重出包后把「出包日」改成实际编译日，不要另起一个没出过的号。

## 已过的能力切片（当时没有单独的安装器版本号）

V1 / V2 是能力切片，合进主线后才有 0.3.0-v3 安装包。下表方便对照「这版产品会什么」，不是安装器流水。

| 切片 | 大约收口 | 特性（人话） | 主要修过的问题 |
|---|---|---|---|
| **V1** | 2026-08-06 前后三窗口合入 | 纳入项目（创建流两卡）；找指标 / 绑数据走嵌入终端；总览灰卡诚实、指标分层 | 创建防连点；clone/采集时序；黄区未登录人话；终端会话重构（一活一会话、切卡、咨询态）；合入/Done 后不冻死在「进行中」 |
| **V2** | 2026-08-10 起设计，随后落地（当时记在 `v1` 分支） | 所有 Issue 必带 buddy 系统提示词 + 按活选技能（V2-①）；同一仓可被多台 buddy 纳管（正本在仓、后来者只读预填、open Issue 读回）（V2-②） | 手填观测不跨机器（已知边界，见遗留清单）；提 MR 后看板进评审中过慢 + merge 无忙态（已修） |

## 还没出包的规划

| 版本 | 状态 | 记在哪 |
|---|---|---|
| **V4** | 特性规划期，清单未齐 | [`v4-prototype/README.md`](v4-prototype/README.md) |

V3 里已设计、还没进安装包能力面的（Cursor 执行器、cowelink 旁路）进 [`LEFTOVERS.md`](LEFTOVERS.md)，不在这里假装已发布。

## 出下一包时怎么填

1. 改仓外 iss 的 `MyAppVersion`（与本表版本号相同）。
2. 在「现在用的」更新出包日，或加一行新版本（只在你决定升号时）。
3. 「多了 / 修了」只写这一包用户能感知的，不写 commit 哈希当正文。
